# AP-278 — Grant de `anon` em SECURITY DEFINER que muta dado: proteção por acidente evapora no próximo refactor

**Status**: active · **Layer**: warm (`data_and_schema`) · **Criado**: 2026-07-11 (044 F1, PR #735)
**Trigger count**: 2

## Sintoma

`restore_stock_for_log` — `SECURITY DEFINER`, **muta estoque** (devolve saldo, escreve
`stock_adjustments`) — tinha `EXECUTE` concedido a `anon` (grant legado, classe AP-275).

Não era explorável. Mas **não por controle de acesso**: a função resolve o dono por `auth.uid()`,
que vem NULL para anon, e aborta com "Usuário não autenticado". Ou seja, estava protegida **por
acidente** — por um detalhe interno de implementação, não por uma decisão de segurança.

## Por que é grave mesmo "não sendo explorável"

O acidente **evapora no primeiro refactor legítimo**. A task T007b (044 F2) ia justamente fazer a
função aceitar `p_user_id` (para poder rodar server-side, onde não há sessão):

```sql
v_user_id := COALESCE(p_user_id, auth.uid());   -- refactor correto, motivado, aprovado
```

Com o grant de anon ainda aberto, esse refactor — sozinho, sem nenhum erro nele — **cria um bypass
real**: um anônimo chama a função passando o `p_user_id` de terceiro e restaura estoque alheio.
O commit que introduz a vulnerabilidade seria *inocente*; a mina já estava plantada no grant.

## Fix

```sql
REVOKE EXECUTE ON FUNCTION public.restore_stock_for_log(uuid, text) FROM PUBLIC, anon;
GRANT  EXECUTE ON FUNCTION public.restore_stock_for_log(uuid, text) TO authenticated, service_role;
```

Fechar o grant **antes** do refactor que o tornaria explorável.

## Regra que fica

**Guard interno e grant são camadas INDEPENDENTES — nenhuma pode ser a única.**
Para TODA função `SECURITY DEFINER` que escreve, auditar explicitamente:

```sql
SELECT p.proname, has_function_privilege('anon', p.oid, 'EXECUTE')
FROM pg_proc p JOIN pg_namespace n ON n.oid = p.pronamespace
WHERE n.nspname = 'public' AND p.prosecdef;   -- anon deve ser false
```

Nunca aceitar "o grant está aberto mas a função falha mesmo assim" como resposta. A pergunta certa
não é *"dá para explorar hoje?"* — é *"o que acontece quando alguém mexer nela amanhã?"*.

## Como foi detectado

Review #735 (Gemini) apontou 3 CRITICAL: o guard `auth.role() = 'authenticated' AND auth.uid() <>
p_user_id` não bloqueia `anon`. **A auditoria de grants que a sugestão provocou** é que achou o
furo real — nas 3 RPCs apontadas o cenário NÃO era explorável (anon não tem EXECUTE nelas), mas na
função vizinha sim. Lição de processo: tratar o achado do revisor como *ponta de fio*, não como
item de checklist — verificar o grant real em vez de aceitar ou recusar a alegação no papel.

## Recorrência #2 (spec 057, 2026-07-23) — o mecanismo exato do "por quê"

`upsert_device_activity` (nova, `upsert_notification_device` como espelho de segurança) recebeu
`REVOKE ALL ... FROM PUBLIC` seguido de `GRANT EXECUTE TO authenticated` — texto que parece
suficiente e é exatamente o que a regra deste AP pede. Auditoria pós-`apply_migration` (a mesma
query acima) mostrou `anon.can_exec = true` mesmo assim.

**Causa raiz identificada via `pg_default_acl`:** este projeto tem `ALTER DEFAULT PRIVILEGES` no
schema `public` que concede `EXECUTE` **diretamente** a `anon`/`authenticated`/`service_role` no
momento da criação de qualquer função (`defaclobjtype='f'`, ACL `{postgres=X,anon=X,authenticated=X,
service_role=X}`) — não via o pseudo-papel `PUBLIC`. `REVOKE ... FROM PUBLIC` revoga o que foi
concedido a `PUBLIC`; não toca um grant que já está no ACL direto de `anon`. Por isso a REGRA deste
AP ("auditar `has_function_privilege`") continua sendo a única defesa confiável — o TEXTO do
`REVOKE FROM PUBLIC` não é evidência de que `anon` está de fato bloqueado.

**Fix**: `REVOKE EXECUTE ON FUNCTION ... FROM PUBLIC, anon;` — sempre nomear `anon` explicitamente,
nunca confiar em `FROM PUBLIC` sozinho neste projeto. (`upsert_notification_device`, criada antes
deste default privilege existir/nesta forma, não tem `anon` na sua `proacl` — não é proteção que
uma função nova herda por semelhança de código com uma função antiga.)

## Relacionados

- [[AP-275]] — tabela nova recebeu grants de anon/authenticated por *default privileges* legados
  apesar do "sem grant automático"; mesma raiz (o grant real diverge do presumido)
- [[AP-277]] — o outro bug desta mesma migração (fail-open por `SELECT INTO`)
