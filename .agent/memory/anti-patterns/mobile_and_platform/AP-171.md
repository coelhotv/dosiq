---
title: Intl.NumberFormat (moeda) não confiável no Hermes — usar formatação manual
summary: Intl.NumberFormat (moeda) não confiável no Hermes — renderiza errado em device sem erro;
layer: warm
status: active
applies_to:
  paths:
    - apps/mobile/**
  diff_triggers:
    - Intl.NumberFormat
    - formatDatePtBR
    - Intl.DateTimeFormat
    - decPart
    - groupedInt
    - intPart
    - intPart.replace
    - Math.abs
  keywords:
    - intl
    - numberformat
    - moeda
    - confiável
    - hermes
    - usar
    - formatação
    - manual
---

# [AP-171] `Intl.NumberFormat` (moeda) não confiável no Hermes — usar formatação manual

## Description
Usar `Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' })` em helper compartilhado web↔mobile (`@dosiq/core/utils/stock.formatBRL`). O Hermes (engine RN) não garante suporte completo a ICU, então a formatação de moeda pode renderizar incorretamente em dispositivo (símbolo, separadores ou casas erradas) — sem lançar erro, apenas saída errada.

## Why it's harmful
- **Falha silenciosa em device**: passa nos testes (Node/V8 tem ICU completo) mas renderiza errado no app — não é pego por CI.
- **Família AP-155 / R-199**: mesma raiz que `Intl.DateTimeFormat` no Hermes; aqui o sintoma é saída incorreta em vez de crash.

## Correct Approach
Formatação manual determinística (idêntica web↔mobile). Tratar o sinal **antes** do símbolo para preservar contrato (`-R$ 50,00`, não `R$ -50,00`).

## Example
### ❌ Wrong
```javascript
return new Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' }).format(n)
```

### ✅ Right
```javascript
const sign = n < 0 ? '-' : ''
const [intPart, decPart] = Math.abs(n).toFixed(2).split('.')
const groupedInt = intPart.replace(/\B(?=(\d{3})+(?!\d))/g, '.')
return `${sign}R$ ${groupedInt},${decPart}`
```

## Adendo 2026-07-12 (044 F4a, PR #739) — a mesma armadilha em DATA

`toLocaleDateString('pt-BR')` cai no formato **US** no Hermes pelo mesmo motivo (ICU incompleto).
Usar `formatDatePtBR` de `@dosiq/core` (`utils/dateFormat.ts` — tabela manual de meses).

O impacto pode ir MUITO além do cosmético quando a data é insumo de decisão: no sheet de
reconciliação do estoque o rótulo é "Retomar o saldo de {data}", então um congelamento de **12 de
junho** apareceria como **06/12/2026** — o usuário escolheria entre retomar e ZERAR o saldo lendo
uma data errada. Regra prática: no mobile, trate qualquer `toLocale*` como bug até prova em
contrário, inclusive datas. Precedente enganoso: existe `toLocaleDateString('pt-BR')` em telas
antigas que "parecem funcionar" — não use isso como licença.

## Referências
- Relacionado: [[AP-155]] (Intl.DateTimeFormat module scope), R-199 (Intl memoization)
- Detectado por Gemini em PR #581 (Fase 3 estoque); fix commit 33441ebe.
- Variante de data detectada por Gemini em PR #739 (044 F4a); fix commit bcc5e56a.
