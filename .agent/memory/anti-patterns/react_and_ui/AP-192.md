---
title: View da SPA reseta no refresh de token (refocus da aba)
summary: >-
  ✅RESOLVED(F3.2d) SPA reseta a view no refresh de token (refocus da aba) — effect de roteamento
  URL→view depende de session;
layer: warm
status: active
applies_to:
  paths:
    - apps/web/**
  diff_triggers:
    - onAuthStateChange
    - setCurrentView
    - App.jsx
    - didInitRef
    - isPasswordRecovery
    - user.id
    - useEffect
    - window.location.pathname
  keywords:
    - view
    - spa
    - reseta
    - refresh
    - token
    - refocus
    - aba
incident_count: 4
last_referenced: "2026-05-30"
---

# AP-192 — View da SPA reseta no refresh de token (refocus da aba)

**Status:** resolved (F3.2d, web 3.6.2) · **Layer:** warm · **Domain:** react_and_ui
**Descoberto:** 2026-05-30 (smoke F3.2c — usuário no HealthHistory, troca de janela e volta → PWA "dá refresh" e cai no dashboard "hoje")
**Resolvido:** 2026-05-30 (F3.2d) — effect de roteamento gateado a `session?.id` em vez do objeto `session`.

## Sintoma
Estando em qualquer view ≠ dashboard (ex: HealthHistory), ao trocar de janela
(IDE) e voltar ao browser, o app navega de volta pro dashboard. UX frustrante.

## Causa (App.jsx:92-124)
```js
useEffect(() => {
  if (!session) return
  const path = window.location.pathname   // '/' ou '/today' na maioria do tempo
  ...
  else if (path === '/today' || path === '/') setCurrentView('dashboard')
}, [session])   // ← depende de session
```
- A navegação interna é **state-based** (`setCurrentView`), a URL **não muda** (fica `/`).
- Ao refocar a aba, o Supabase **renova o access token** → `onAuthStateChange` →
  `setSession(novo objeto)` → o effect re-dispara → lê `path === '/'` →
  `setCurrentView('dashboard')` → reseta a view.
- O token refresh (mesmo usuário) NÃO deveria re-rotear.

## Regra / Fix
- Effects de roteamento inicial devem rodar por **identidade do usuário**
  (`session?.user?.id`) ou um guard `didInitRef`, não por toda mudança de `session`.
- Idealmente: sincronizar a URL com a view interna (history.pushState por view) p/
  refresh/deep-link consistentes — mas o mínimo é não re-rotear em token refresh.

## Fix aplicado (F3.2d)
`App.jsx`: dep do effect de roteamento `[session]` → `[session?.id]`. O objeto
`session` armazenado é o `user` (`setSession(session?.user ?? null)`), então
`session?.id` = `user.id`. Token refresh do MESMO usuário não muda o id → effect
não re-dispara → view preservada. Transições `null→id` (login, confirmação de
e-mail, reset de senha via links Supabase) continuam disparando. O fluxo de reset
de senha é state-driven (`isPasswordRecovery`, setado em `onAuthStateChange` com
`return` early) — não depende deste effect, logo intacto.
Sync URL↔view (history.pushState por view) fica como melhoria maior futura.

## Relacionados
[[feedback_workbox_supabase_logout]] (outro efeito de auth/SW no foco)
