---
title: Missing Specifier em Re-export de Workspace Sub-path
summary: Re-export de sub-path de workspace sem wildcard — "Missing specifier" em Vite 7/Vitest.
layer: warm
status: active
applies_to:
  paths:
    - api/**
  diff_triggers:
    - package.json
  keywords:
    - missing
    - specifier
    - export
    - workspace
    - sub
    - path
legacy_tags:
  - infra_and_deploy
  - monorepo
  - vite
description: >-
  Re-export de sub-path de workspace sem wildcard no exports field — "Missing specifier" em
  Vite/Vitest
expiry_date: "2027-05-01"
incident_count: 0
name: AP-128
trigger_count: 1
type: anti-pattern
---

# AP-128 — Missing Specifier em Re-export de Workspace Sub-path

**Sintoma:** `Error: Missing "./schemas/X.js" specifier in "@dosiq/core" package` em Vite/Vitest.

**Causa:** `package.json` do workspace package tem `exports` field sem o sub-path wildcard. Vite 7 respeita o `exports` field mesmo quando um alias de path está configurado para o package.

**Fix:** Adicionar ao `package.json` do package:
```json
"./schemas/*": "./src/schemas/*.js"
```

**Regra associada:** R-213
