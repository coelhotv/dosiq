---
title: Zod-SQL Schema Synchronization
summary: >-
  Zod schemas and DB schemas MUST be synchronized. Any CHECK/NOT NULL/enum in DB must have
  corresponding Zod validation. Update both in the same commit.
layer: warm
status: active
applies_to:
  paths:
    - packages/**
    - "**/schemas/**"
  diff_triggers:
    - categorySchema
    - z.enum
  keywords:
    - zod
    - sql
    - schema
    - synchronization
legacy_tags:
  - zod
  - supabase
bootstrap_default: true
id: R-082
incident_count: 23
last_referenced: "2026-06-21"
legacy_pack: schema-data
review_due: "2026-06-30"
tags:
  - zod
  - supabase
  - data
  - safety
---

# R-082 — Zod-SQL Schema Synchronization [CRITICAL]

## Rule
Zod schemas and database schemas MUST be synchronized. Any CHECK constraint, NOT NULL constraint, or enum in the database MUST have a corresponding validation in Zod. When one changes, the other must be updated in the same commit.

## Common Mismatches
- DB has CHECK constraint but Zod allows any value
- Zod has default value that DB doesn't accept
- DB column is NOT NULL but Zod field is optional
- Zod enum values don't match DB CHECK constraint

## Example
```javascript
// CORRECT — Zod matches DB CHECK constraint
// DB: CHECK (category IN ('estilo', 'bug', 'seguranca', 'performance', 'manutenibilidade'))
const categorySchema = z.enum(['estilo', 'bug', 'seguranca', 'performance', 'manutenibilidade'])

// WRONG — 'geral' not in DB CHECK constraint
const categorySchema = z.enum(['estilo', 'bug', 'seguranca', 'performance', 'manutenibilidade', 'geral'])
```

## Source
Sprint 7 Gemini Integration (PR #146, #152) — multiple constraint violations

## Consolidated anti-patterns
- `AP-S09` — let Zod and SQL constraints drift apart

Use this rule as the canonical guidance for application and database validation parity. The AP remains as a quick negative label.
