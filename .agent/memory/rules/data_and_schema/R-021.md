---
title: Zod Enums in Portuguese
summary: All Zod schema enum values must be in Portuguese. Always export labels for UI display.
layer: warm
status: active
applies_to:
  paths:
    - packages/**
    - "**/schemas/**"
  diff_triggers:
    - dias_alternados
    - quando_necessário
    - FREQUENCY_LABELS
  keywords:
    - zod
    - enums
    - portuguese
legacy_tags:
  - zod
bootstrap_default: false
id: R-021
incident_count: 12
last_referenced: "2026-06-14"
legacy_pack: schema-data
review_due: "2026-06-30"
tags:
  - zod
  - design
---

# R-021 — Zod Enums in Portuguese [HIGH]

## Rule
All Zod schema enum values must be in Portuguese. English values cause UI inconsistencies. Always export label maps for UI display.

## Why
PR review catch — English enum values leaked into UI labels, causing inconsistent UX in a Portuguese-language app.

## Example
```javascript
// Correct
const FREQUENCIES = ['diário', 'dias_alternados', 'semanal', 'personalizado', 'quando_necessário']
export const FREQUENCY_LABELS = {
  diário: 'Diário',
  dias_alternados: 'Dias Alternados',
  semanal: 'Semanal',
  personalizado: 'Personalizado',
  quando_necessário: 'Quando Necessário'
}

// Wrong
const FREQUENCIES = ['daily', 'weekly']
```

## Consolidated anti-patterns
- `AP-006` — use English values in Zod enums

Use this rule as the canonical guidance for enum language consistency. The AP remains as a historical negative example.
