---
title: Path Alias Verification
summary: Always verify path aliases in vite.config.js before assuming @feature/x resolves to a given path.
layer: warm
status: active
applies_to:
  paths:
    - scripts/**
    - docs/**
  diff_triggers:
    - vite.config.js
  keywords:
    - path
    - alias
    - verification
legacy_tags:
  - all
bootstrap_default: true
id: R-002
incident_count: 21
last_referenced: "2026-08-03"
legacy_pack: file-integrity
review_due: "2026-06-30"
tags:
  - file-management
  - safety
---

# R-002 — Path Alias Verification [HIGH]

## Rule
When you see `@adherence/services/x`, verify it resolves to `src/features/adherence/services/x` (NOT `src/services/api/`). Always check `vite.config.js` for alias definitions before assuming a path.

## Why
Production bugs caused by modifying the wrong file because alias resolution was assumed, not verified. `@feature/x` aliases can be misleading.

## How to Verify
```bash
grep -A 20 "alias" vite.config.js
```

## Related anti-pattern
Linked to R-001 (duplicate file risk).

## Consolidated anti-patterns
- `AP-002` — assume import location based on file name

Use this rule as the canonical guidance for alias and import-path verification. Keep the AP as historical shorthand, not as the primary place to learn the guardrail.
