---
title: Aggressive Test Cleanup
summary: >-
  afterEach must call: clearCache(), vi.clearAllMocks(), vi.clearAllTimers(), optionally
  global.gc().
layer: warm
status: active
applies_to:
  paths:
    - scripts/**
    - docs/**
  diff_triggers:
    - afterEach
    - global.gc
    - clearCache
    - clearState
    - vi.clearAllMocks
    - vi.clearAllTimers
    - vi.resetAllMocks
  keywords:
    - aggressive
    - test
    - cleanup
legacy_tags:
  - all
bootstrap_default: false
id: R-078
incident_count: 1
last_referenced: None
legacy_pack: test-hygiene
review_due: "2026-06-30"
tags:
  - testing
---

# R-078 — Aggressive Test Cleanup [HIGH]

## Rule
Every test's `afterEach` must call all four cleanup operations:

```javascript
afterEach(() => {
  clearCache()          // or clearState() — project-specific
  vi.clearAllMocks()
  vi.resetAllMocks()
  vi.clearAllTimers()
  if (global.gc) global.gc()  // optional: hint GC on low-RAM machines
})
```

## Why
2026-02-23 test isolation. Shared state between tests caused intermittent failures that were hard to reproduce. Aggressive cleanup ensures each test starts from a clean slate.
