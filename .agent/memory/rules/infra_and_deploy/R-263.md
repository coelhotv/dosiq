---
id: R-263
title: Fixar versão Node no projeto e na Vercel
status: active
layer: warm
category: infra_and_deploy
tags: [node, vercel, runtime, ci, environment]
first_seen: "2026-06-04"
sprint: "2026-W23"
---

## Regra

Declarar `"engines": { "node": "20.x" }` no `package.json` raiz **e** `NODE_VERSION=20` nas env vars da Vercel. Sem essa âncora, bumps silenciosos de runtime da Vercel mudam o ambiente de execução sem sinal visível no código ou no CI.

## Por que

Vercel atualiza o runtime padrão periodicamente. Uma função que funcionava em Node 18 pode quebrar silenciosamente ao mover para Node 20 (ou vice-versa) — especialmente em checagens de globals nativos (WebSocket, fetch). A âncora garante que o ambiente local, o CI e a Vercel usem exatamente a mesma versão.

## Como aplicar

```json
// package.json raiz
"engines": { "node": "20.x" }
```

```
// Vercel Dashboard → Settings → General → Node.js Version
NODE_VERSION=20
```

Ao decidir subir para Node 22 (que tem WebSocket nativo): atualizar ambos juntos e testar o smoke ([[R-262]]) antes.

## Referências

- [[AP-212]] — incidente de origem
- [[R-262]] — smoke test de cold start
