---
title: Service Role Authentication
summary: >-
  Any endpoint using service_role key (bypasses RLS) MUST have authentication via Supabase Auth +
  ADMIN_CHAT_ID.
layer: warm
status: active
applies_to:
  paths:
    - api/**
  diff_triggers:
    - service_role
    - ADMIN_CHAT_ID
    - ADMIN_USER_ID
    - res.status
    - supabase.auth.getUser
    - user.id
  keywords:
    - service
    - role
    - authentication
legacy_tags:
  - supabase
  - vercel
bootstrap_default: false
id: R-042
incident_count: 21
last_referenced: "2026-07-25"
legacy_pack: infra-api
review_due: "2026-06-30"
tags:
  - supabase
  - infra
  - safety
---

# R-042 — Service Role Authentication [HIGH]

## Rule
Any endpoint using the `service_role` Supabase key (which bypasses RLS) MUST have authentication. Verify admin identity via Supabase Auth + `ADMIN_CHAT_ID`.

## Why
PR #73 security fix. A `service_role` endpoint was exposed without authentication, allowing any caller to bypass row-level security.

## Verification pattern
```javascript
// Always verify before using service_role
const { data: { user } } = await supabase.auth.getUser(token)
if (!user || user.id !== ADMIN_USER_ID) {
  return res.status(403).json({ error: 'Forbidden' })
}
// Only now use service_role operations
```

## Related rule
R-040 (Vercel API Rewrites — ensure the route is properly protected)
