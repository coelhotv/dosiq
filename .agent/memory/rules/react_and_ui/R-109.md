---
title: key={controllingProp} para Reinicializar Estado Interno
summary: >-
  Quando estado interno é inicializado com base em prop e deve reinicializar quando a prop muda,
  usar key={controllingProp} no JSX para forçar remount.
layer: warm
status: active
applies_to:
  paths:
    - apps/web/**
  keywords:
    - key
    - controllingprop
    - reinicializar
    - estado
    - interno
legacy_tags:
  - react
bootstrap_default: false
id: R-109
incident_count: 1
last_referenced: "2026-03-05"
legacy_pack: react-hooks
review_due: "2026-06-30"
tags:
  - react
---

# R-109: key={controllingProp} para Reinicializar Estado Interno

**Summary:** Quando estado interno é inicializado com base em prop e deve reinicializar quando a prop muda, usar key={controllingProp} no JSX para forçar remount.

**Applies to:** react

**Tags:** react

## Rationale

Some internal state should reset when a controlling prop changes. Using `key` is the explicit remount mechanism when sync logic would be more brittle.

## Implementation Notes

Apply `key={controllingProp}` to the component boundary when full internal reset is the intended behavior.

---

*Last updated: 2026-04-10*
*Rule ID: R-109*
*Status: Active*
