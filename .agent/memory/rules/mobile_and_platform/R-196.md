---
title: isInQuietHours deve tratar janela cross-midnight (start > end = spans midnight)
summary: >-
  isInQuietHours deve tratar janela cross-midnight: se start > end, a janela passa pela meia-noite
  (cur >= s || cur < e)
layer: warm
status: active
applies_to:
  paths:
    - apps/mobile/**
  keywords:
    - isinquiethours
    - deve
    - tratar
    - janela
    - cross
    - midnight
    - start
    - end
legacy_tags:
  - notifications
  - quiet-hours
  - server
category: mobile_and_platform
id: R-196
tags:
  - notifications
  - quiet-hours
  - wave-n2
---

## Regra

Ao implementar quiet hours, verificar se a janela cruza a meia-noite:

```js
if (s <= e) {
  // Janela normal: ex. 13:00 → 15:00
  return cur >= s && cur < e
} else {
  // Cross-midnight: ex. 22:00 → 07:00
  return cur >= s || cur < e
}
```

- `start = end` → considerar como janela vazia (não suprime)
- `start = null || end = null` → quiet hours desativados (não suprime)
- Limiar de `start` é **inclusive**; limiar de `end` é **exclusive**

## Origem

Sprint 2.2 / Wave N2 — `server/bot/utils/notificationGate.js`. Testado com 18 casos incluindo cross-midnight.
