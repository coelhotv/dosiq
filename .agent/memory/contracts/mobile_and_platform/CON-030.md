---
id: CON-030
title: Dose Activity Surface (Android ongoing notification)
category: mobile_and_platform
lifecycle: warm
status: accepted
spec: 039
phase: F2
pr: 691
trigger_context: ["doses", "ongoing-notification", "notifee", "live-activity", "mobile", "android"]
---

# CON-030 — Dose Activity Surface (Android)

Superfície de estado contínuo da dose no Android (Spec 039 / F2). Promove o spike
`devDoseActivitySpike` a produção. Renderiza UMA dose como ongoing notification "companheira"
consumindo [[CON-029]] (`selectActiveDoseActivity`). A máquina de estados vive no core; esta
camada só APRESENTA. F3 (iOS Live Activity) espelha o mesmo contrato.

## Forma

`apps/mobile/src/platform/doseActivity/`
```js
// doseActivitySurfaceService.js
showDoseActivity(activity, { now?, discreet?, doseItem? }) -> Promise<void>  // exibe/atualiza/encerra
showDoseDone({ instanceId, medicineLabel?, takenAt?, doseItem? }) -> Promise<void> // card 'done' (3min)
readSurfaceLabel(instanceId) -> Promise<string|null>  // nome da superfície ATIVA (auto-gate do done)
endDoseActivity(instanceId) -> Promise<void>                                 // cancel-on-resolve
ensureSurfaceChannel() -> Promise<void>                                      // canal DEFAULT idempotente
DOSE_ACTIVITY_CHANNEL_ID = 'dose-activity-v1'   // canal próprio, separado dos de alarme
LATE_CHRONO_CAP_MINUTES = 120                    // CL-3 / T021a
DONE_CARD_TIMEOUT_MS = 3 * 60 * 1000             // auto-dismiss do card 'done'
SURFACE_ACTION = { REGISTER, SNOOZE }            // ids estáveis (R-222)

// DoseActivityBridge.jsx — monta no AppRoot; deriva a dose ativa e exibe/encerra.
// SEM toggle próprio: gate = userId + dose critical_alarm pendente (extensão do alarme crítico).
```

## Invariantes (atualizado pós-smoke F2 — 2026-06-28; ver spec §9.7)

- **Canal DEFAULT silencioso e separado** (`dose-activity-v1`) — NUNCA tocar nos canais de alarme
  (`dose-alarm-v3`/`-critical-v2`, [[R-261]]). Sem som / sem loop / sem full-screen.
- **id da notificação = `${doseInstanceId}:surface`** (id PRÓPRIO via `surfaceId()`): coexiste com o
  alarme (id=`doseInstanceId`) no T0 sem sobrescrever o trigger dele — Notifee só admite **1 trigger
  pendente por id**; ids iguais fariam a superfície matar o alarme fullscreen no T0 (bug smoke). `endDoseActivity`/
  cancel usam `surfaceId(instanceId)` (cancel-on-resolve [[AP-235]] / PO-2.2).
- **"Registrar" ABRE a modal bulk** (decisão PO smoke): `handleAlarmAction` detecta `SURFACE_ACTION.REGISTER`
  e navega via `navigationRef` p/ deeplink (`bulk-plan` se `treatmentId`, senão `dose-individual`) — o
  botão usa `launchActivity:'default'`. Motivo: sítio de aplicação do injetável só é selecionável na
  modal. **"Adiar"** segue soneca canônica (`SURFACE_TO_ALARM_ACTION` = só SNOOZE). Handler de
  background lazy-require ([[AP-205]]); press do CORPO (sem launch) não navega ([[AP-207]]).
- **SEM toggle** (CL-4, decisão PO): superfície é extensão do alarme crítico. Gate = `userId` + dose
  `critical_alarm` pendente. (A invariante antiga "toggle opt-in default OFF / CL-2" foi REVOGADA.)
- **Sem filtro de lead-time no bridge** (revisto): quem decide a elegibilidade é a state factory do core
  ([[CON-029]]) — `d ≥ 240min` → `null` (sem superfície). Bridge busca janela `[now−3d, now+3d]`
  (lookback p/ `late`/`now` vencidas; `missed` é podada por `selectActiveDoseActivity`).
- **TRIGGER-DRIVEN** (re-trabalho pós-smoke — US2.1 exige transição com app FECHADA): as transições
  de estado são agendadas como **triggers nativos** (`Notifee TimestampTrigger` + `alarmManager.allowWhileIdle`
  → fura Doze) e ENCADEADAS — `armDoseActivity` exibe o estado atual + agenda o PRÓXIMO boundary;
  na entrega (`onBackgroundEvent`/`onForegroundEvent` DELIVERED) `advanceDoseActivity` agenda o seguinte.
  Boundaries vêm de `doseActivityBoundaryTimes` ([[CON-029]]) + cap do `late`. O modelo flip-timer
  (`setTimeout` foreground) foi APOSENTADO (só transicionava em foreground). Validado no spike DEV.
- **Countdown**: crono regressivo (`direction:'down'`) em `upcoming`/`now`. Como o Chronometer nativo
  não para no zero, o boundary **T0 re-exibe "agora" ESTÁTICO** (trigger no horário exato) → para o
  countdown antes do negativo, com a app fechada.
- **Marca-passo do alarme** (fix race do T0): o `fullScreenAction` do alarme traz a app a foreground no
  T0 e a race pode dropar o DELIVERED do boundary `:surface` → cadeia trava. `reconcileDoseActivityFromAlarm`
  re-arma a superfície a cada entrega/ação do alarme (bg+fg) — alarme é o marca-passo confiável que
  mantém a máquina viva atravessando T0 e a soneca (PR #691). Coexiste por id próprio (`:surface`).
- **CL-3:** `late` mostra count-up só ≤ `LATE_CHRONO_CAP_MINUTES`; acima → `showChronometer:false`
  + corpo estático via `daysAgoLabel` (semanal/GLP-1).
- **Estado `done`** (spec.md:366): card verde "Tomada às HH:mm ✓", `ongoing:false`, auto-dismiss
  `DONE_CARD_TIMEOUT_MS` (3min — spec pedia 3s, ilegível). Disparo em **fonte única**: o choke point
  `doseService._cancelAlarmBestEffort` via `readSurfaceLabel` (auto-gate: só doses que TINHAM superfície,
  via `__surface:'true'`), capturado ANTES do `cancelAlarm` (que apaga a notif pelo mesmo id). Cobre
  todos os caminhos de registro (modal, in-app, alarme). `registerTaken` NÃO chama `showDoseDone`
  direto — duplicava IPC + flicker (PR #691 / Gemini #901). `formatClock` extrai HH:mm do `Date` +
  zero-pad (NÃO `Intl.DateTimeFormat` — ICU limitado no Hermes, [[AP-218]]; PR #691 #898).
- **`missed`** é terminal → encerra a superfície (não ocupa ongoing).
- **Modo discreto** (PO-SEC-1): `discreet` → `android.visibility=PRIVATE` (SO redige no bloqueio);
  nome real SEMPRE no conteúdo. Título=`nome · dose` via `formatDoseItem` (R-272); corpo=`estado · HH:mm`.

## iOS — Live Activity + Dynamic Island (F3, PR pendente)

Espelha a MESMA máquina de estados ([[CON-029]]); muda só a camada de render. ADR-075 (emenda):
target via **`@bacons/apple-targets`** (`targets/dose-activity/expo-target.config.js` + Swift), não
mais wiring manual no Xcode (reproduzível no prebuild/EAS).

- **Forma:** `liveActivityService.js` (casca do módulo nativo `DoseActivityBridge`: `start`/`update`/
  `end`/`drainPendingActions`) + `DoseLiveActivityBridge.jsx` (deriva a dose ativa via CON-029 e dirige
  a LA; iOS-only, mount platform-split no AppRoot). Bridge nativo do app injetado por
  `withDoseActivityBridge.js` (fontes versionados em `ios-native/`; `ios/` é efêmero).
- **Server-free + transição em background:** iOS NÃO roda JS em background (sem equivalente ao
  trigger Notifee do Android — o "pacemaker via alarme" da F2 NÃO existe aqui). Mecanismos:
  (a) timer VIVO `Text(timerInterval:)` (countdown/up sozinho); (b) re-derive via `update()` no
  mount/foreground/resync (o alarme T0 fullscreen traz o app ao foreground → cobre later→upcoming→now);
  (c) **`staleDate`** = próximo boundary (`doseActivityBoundaryTimes`) passado ao nativo — quando passa,
  o sistema re-renderiza a LA em background e o **widget RECALCULA o estado de scheduledAt+now**
  (`displayState`, espelha SURFACE_WINDOWS) → cobre **now→late server-free**. `done`/`missed` vêm do
  campo (terminais). **Fora de escopo 039 (FR-009 — server-free):** multi-hop em background profundo
  exigiria ActivityKit push/APNs (greenfield — só web push existe hoje; FCM não entrega Live Activity
  push) → seria uma SPEC FUTURA separada, NÃO fase da 039 (F4=validação, F5=housekeeping). É constraint
  aceita do MVP, não dívida.
- **Paridade de apresentação com a F2 (AP-249):** título = **nome + dose** (`formatDoseItem`/R-272);
  **HH:mm agendado** junto do countdown (`captionLabel` = "frase · HH:mm"); frases espelham STATE_BODY
  ("Dose crítica chegando/agora/pendente"); count-up com **cap CL-3** (120min) → `daysAgoLabel`.
- **Marca dosiq (AP-249):** ícone = `DosiqMark` (SwiftUI Shape, path canônico de `dosiq-logo-mono.svg`),
  NUNCA SF Symbol genérico (`drop.fill`).
- **Privacidade lock screen (decisão PO 2026-06-29):** default iOS = **mostra o nome** na lock screen
  (`discreet=false`) — diferente do Android, que manda `AndroidVisibility.PRIVATE` e DEFERE ao SO. iOS
  Live Activity NÃO é redigida pelo sistema (a config "Mostrar Pré-visualizações" não a cobre; sem API
  de lock-state no widget), então ocultar o nome só via toggle PRÓPRIO do dosiq. O caminho `discreet`
  (genérico + horário, sem nome) está implementado no widget e pronto p/ esse toggle.
  **BACKLOG "adequação LGPD":** Ajuste em Configurações → "ocultar nome do remédio na tela bloqueada"
  alimentando `discreet`. Reverte o requisito original (state.json "default discreto") por decisão PO —
  utilidade do DI/card pesou mais; toggle dá a escolha depois. Fora da F3.
- **Cores = tokens:** Swift hardcoda hex espelhando `tokens.js` (later #8e9199 · upcoming #99f6e4 ·
  now #14b8a6 · late #904d00 · done #166534 · missed #ba1a1a).
- **"Adiar" abre o app (smoke F3, decisão técnica):** `DoseSnoozeIntent.openAppWhenRun=true`. iOS não
  roda JS em background (sem observer Darwin) → com `false` o `scheduleSnooze` (JS) só rodava no próximo
  foreground = soneca não acontecia com app minimizado/morto. Diferente do Android (notifee reagenda
  nativo em bg). Trazer o app à frente é o único caminho server-free; reimplementar a soneca nativa em
  Swift (notifee tem canal/som/fullscreen próprios) seria duplicação/drift. Limitação aceita do MVP.
- **App Intents (iOS 17+):** `DoseRegisterIntent`/`DoseSnoozeIntent` enfileiram no **App Group**
  (`group.com.coelhotv.dosiq`) + Darwin notification; o RN drena (`drainPendingActions`) e age com
  **sessão VIVA** (PO-SEC-2 — `supabase.auth.getUser()`, nunca userId cacheado). "Registrar" abre a
  modal bulk (sítio do injetável); "Adiar" → `scheduleSnooze`. iOS<17 cai no fallback (notif interativa).
- **Card `done` (smoke F3, 2026-06-29):** ao registrar, `doseService._cancelAlarmBestEffort` emite
  `triggerDoseActivityRefresh` (bus dep-zero) → `DoseLiveActivityBridge` re-deriva e mostra o `done`
  (registrar in-app é foreground, sem mudança de AppState que acione o derive sozinho). `takenAt` =
  `registeredAt` do doseItem (horário REAL da tomada), nunca `now` do re-derive. As funções de
  superfície Android (`showDoseDone`/`endDoseActivity`/`readSurfaceLabel`, notifee) ficam atrás de
  `Platform.OS==='android'` — no iOS postavam notif local "Dose registrada" (bug F3).
  **DI não mostra `done` (decisão PO — ACEITO):** `Activity.end(dismissalPolicy:.after(+180))` remove
  a atividade da Dynamic Island na hora (a ilha só mostra ativas); o card `done` persiste só na **lock
  screen** por ~3min. Forçar `done` na DI exigiria não-encerrar + `end` agendado (setTimeout foreground
  frágil / derive tardio → trava na ilha) — pior server-free. Item resolvido sair da ilha é aceitável.
- **Encerramento:** `endLiveActivity` (cancel-on-resolve, AP-235 / PO-3.2).
- **Smoke MANUAL pendente (PO):** PO-3.1 (transições+App Intent na ilha), PO-3.2 (encerra), PO-SEC-2.

## Consumidores

- AppRoot monta `DoseActivityBridge` (Android) **ou** `DoseLiveActivityBridge` (iOS), platform-split.
- Relacionado: [[CON-029]], [[CON-026]], [[ADR-075]], [[R-255]], [[R-256]], [[R-257]], [[AP-235]], [[PO-SEC-2]].
