# DEVFLOW Anti-Patterns Index

> **Nota T038c (auditoria pós-040, 2026-07-08):** APs de shape/null/campo-errado em JS estão
> PARCIALMENTE mitigados por TypeScript nos módulos nível A (strict islands). Continuam válidos
> em código nível B (`strict:false`) — só aposentar quando `strict:true` global. [ANNOTATE-TS]

## 📦 Data & Schema (`data_and_schema`)
- **[AP-327]** 🔴 **O RESUMO do auto-grupo do Android herda o canal e passa no predicado de tipo — mas não tem `data`.** `list.find(isAlarmNotification)` no `AppState 'active'` devolvia o `GROUP_SUMMARY` que o SO cria sozinho (id `2147483647`, `tag=ranker_group`, mesmo `channelId` das agrupadas) em vez do alarme real; resumo não tem `doseInstanceId` ⇒ `openAlarmScreen` barrava no guard seguinte e **o takeover de dose crítica NUNCA abria, com o alarme tocando ao lado** (iOS ok — lá o takeover depende de PRESS, que o usuário sempre dá). Medido no device: `displayed {total:3, canais:[critical,critical,activity], achouAlarme:true}` → `saiu: sem-doseInstanceId`. 🔴 **Quebrou sem NENHUM commit no caminho do alarme:** o resumo só nasce com notificações simultâneas suficientes — quando a superfície de dose (039) passou a coexistir com alarme+push, o SO começou a agrupar e o `find` passou a pegar o resumo; qualquer feature que poste +1 notificação reintroduz a condição. **Farol:** `find`/`[0]` sobre `getDisplayedNotifications()` (a lista tem itens que o SO criou); predicado que decide por canal/categoria (metadado HERDÁVEL) em vez de conteúdo; código que acha o item e o descarta num guard adiante (o par 'achou/não serve' = filtro incompleto, não guard errado). Fix: `pickPromotableAlarm` seleciona por conteúdo (`doseInstanceId` + não-stale). 🔴 **2º furo, mesma família:** o cold start não tinha caminho NENHUM — app morto lançado pelo intent não emite `AppState 'active'` (nasce ativo) e `getInitialNotification` volta vazio (não houve press); medido `result code=0`+`WARM`+pid novo, `Running "main"` 1,4s após o headless. Fix: `promoteActiveAlarm()` também na MONTAGEM → [[R-309]]. Antes deste, 3 hipóteses minhas caíram por medição — inclusive 'R8 ofuscou o notifee', que quase virou fix a partir de um `total:0` contaminado por notificações órfãs de reinstalação. Ver [[AP-207]] · [[AP-323]] · [[R-308]] -> [`anti-patterns/mobile_and_platform/AP-327.md`](./anti-patterns/mobile_and_platform/AP-327.md)
- **[AP-326]** 🔴 **Otimização de build que decide por análise ESTÁTICA apaga o que só existe como string do lado JS — e o opt-out documentado pode ser ignorado calado.** `enableShrinkResourcesInReleaseBuilds` (ligado de brinde junto do R8, PR #782) removia do APK o `drawable/ic_dosiq_mark`, smallIcon da notificação de dose: a única referência é a string JS `smallIcon: 'ic_dosiq_mark'`, resolvida do outro lado da ponte RN ⇒ órfão para o shrinker ⇒ `IllegalArgumentException: no valid small icon` e [[AP-251]] ressuscitado por uma flag. ⚠️ Regra de ProGuard NÃO cobre: `-keep class` protege CÓDIGO; recurso exige `tools:keep` em `res/raw/keep.xml` — dois mecanismos no mesmo build, fácil supor que um cobre o outro. 🔴 **E o keep NÃO funcionou:** o APK seguinte trazia `raw/keep` empacotado (prova de que chegou ao build) e seguia sem o drawable — removido, não "nunca compilado": a tabela é alfabética e o buraco era exatamente o ID que ele ocuparia (`0x7f0800af`, entre `ic_clock_black_24dp` e `ic_keyboard_black_24dp`). Sem erro, sem aviso; causa não identificada. Fix: flag **desligada** (R8/minify de código permanece — era o que o Play pedia); custo 35,8→37,7 MB (+1,9 MB), ainda −36% vs v0.30.0. **Farol:** otimização cuja decisão é "isto parece não usado" + recurso endereçado por string (`smallIcon`, `getIdentifier`, som, fonte, `require` dinâmico); allowlist cujo efeito ninguém mediu no artefato — a lista existir não prova que foi honrada. **Conduta:** opt-out de terceiro que falha sem explicação ⇒ desligar a otimização opcional, não caçar o bug, quando o que está em jogo é caminho clínico (1,9 MB não compra o alarme). Verificação: `aapt2 dump resources <apk> | grep drawable/ic_dosiq_mark`. Ver [[R-308]] · [[AP-324]] -> [`anti-patterns/mobile_and_platform/AP-326.md`](./anti-patterns/mobile_and_platform/AP-326.md)
- **[AP-325]** 🔴 **Flag de "tolerar falha" esconde que o recurso NUNCA funcionou — não que ele falha às vezes.** `SENTRY_ALLOW_FAILURE: "true"` no perfil de produção do `eas.json` lia-se como *"o upload de símbolos às vezes falha, não derruba build de 20min"*. A medição (API do Sentry, 2026-07-30) disse outra coisa: `files/dsyms/` → `[]`, `artifact-bundles` → vazio, release `0.30.0+3000` existe e recebeu evento **sem nenhum arquivo de simbolização**. Nunca funcionou. O token era válido (autenticou pela CLI) — só não chegava ao build: o `.easignore` corta todo dotfile (`.env` junto) e o env do `expo prebuild` morre antes do gradle. **A flag é o que torna isso caro:** sem ela o 1º build teria falhado e o fix custaria minutos, meses atrás; com ela o sistema REPORTA SUCESSO de uma operação que não ocorreu, e a descoberta fica reservada pro dia em que alguém precisa ler stack trace de produção (Constituição §IX numa superfície interna). Com R8 ligado o custo sobe de inconveniente p/ crash ilegível. **Farol:** `ALLOW_FAILURE`/`CONTINUE_ON_ERROR`/`|| true`/`failOnError:false` sobre etapa cujo PRODUTO ninguém confere depois — a pergunta que desarma é *"se isto nunca tivesse funcionado, como eu saberia?"*; pior ainda em observabilidade, onde a ferramenta que deveria contar a verdade sobre falhas é a que falha calada. Fix: flag removida + `lib-sentry-token.sh` (carrega o token dos .env e ABORTA antes do build de produção se faltar). ✅ **RESOLVIDO no 1º build de produção com o fix (v0.30.1+3001, 2026-08-02)** — bundle `aca088bc…` no servidor, 2 arquivos. 🔴 **E a verificação que este AP prescrevia estava ERRADA:** `files/dsyms/` NÃO detecta sourcemap de JS — consultar ali deu `0` com o upload funcionando e quase produziu a conclusão oposta ao fato. Endpoint certo p/ JS = `files/artifact-bundles/`; `dsyms` é só símbolo NATIVO; `releases/<rel>/files/` é legado e volta vazio mesmo funcionando. Quando existir, o LOG DO BUILD é a evidência direta — pedir o log custa menos que inferir do estado final. Lacuna aberta: mapping do R8 não sobe (exigiria `io.sentry.android.gradle`) ⇒ crash JS legível, crash Java/Kotlin ofuscado -> [`anti-patterns/process_and_testing/AP-325.md`](./anti-patterns/process_and_testing/AP-325.md)
- **[AP-324]** 🔴 **Dependência de DESENVOLVIMENTO que só sabe se excluir numa plataforma vaza para o binário de produção da outra.** `expo-dev-client` declara `"debugOnly": true` **só no bloco `apple`** do seu `expo-module.config.json`; no `android` o campo não existe ⇒ autolinkado em toda variante. O gradle plugin do dev-launcher reforça a ilusão (checa a variante e **não instrumenta** fora de debug) — mas instrumentar ≠ linkar: o AAR foi inteiro pro AAB. O Play Console cobrou isso em DOIS avisos que pareciam ser do nosso código e não eram: `DevLauncherExpoActivityConfigurator` (edge-to-edge depreciado) e `GmsBarcodeScanningDelegateActivity` (orientação) — **o dosiq não tem leitor de código de barras**; o ML Kit veio de `play-services-code-scanner` + `mlkit:barcode-scanning`, deps que só o dev-launcher usa pro QR do dev menu. Junto: Compose, Koin, Apollo, okhttp, gson e o deep link `exp+dosiq-app` no manifest de produção. **Nenhum gate pega** — não é bug de código, é composição de dependências ([[R-308]]). **Farol:** pacote de dev em `dependencies`; `debugOnly` em um bloco de plataforma só; aviso da loja citando classe de pacote que você não chama; Activity/permissão/scheme no manifest que ninguém reconhece. 🔴 **1ª tentativa de fix FALHOU e é a parte mais útil deste AP:** `expoAutolinking.exclude` no `settings.gradle` NÃO funciona — `AutolinkigCommandBuilder.kt:44-46` achata a lista com `joinToString(" ")` e a flag do CLI é variádica (`--exclude <exclude...>`), então o autolinking procura um módulo literalmente chamado `"expo-dev-client expo-dev-launcher ..."` e exclui ZERO, sem erro. `settings.gradle` certo + CLI certo + produto errado; só a inspeção do APK pegou (260 refs a `expo/modules/devlauncher` no dex). Fix real: `expo.autolinking.android.exclude` no **package.json** (lido direto pelo CLI, sem o builder quebrado) — gradle 23→19 projetos, zero `expo-dev-*`; + `withoutDevSchemeOnRelease.js` p/ o scheme `exp+<slug>`, que vem do config plugin e sobrevive à exclusão dos módulos. Custo aceito pelo PO: exclude é estático (não há env var p/ condicionar por perfil), então o dev build Android perde o dev menu do Expo — o do RN permanece. Origem: avisos do Play sobre a v0.30.0 -> [`anti-patterns/mobile_and_platform/AP-324.md`](./anti-patterns/mobile_and_platform/AP-324.md)
- **[AP-319]** 🔴 **`REVOKE SELECT (coluna)` é INERTE quando existe `GRANT SELECT` de tabela.** O Postgres não subtrai coluna de um privilégio concedido no nível da tabela — o `REVOKE` é aceito, responde sucesso e não faz nada; `has_column_privilege('anon',…,'updated_by','SELECT')` seguiu `true` e `select=*` devolveria o uuid interno do admin. Passa despercebido porque a linha do `REVOKE` **existe na migração** (quem revisa marca como resolvido), a leitura das colunas legítimas funciona, e o teste óbvio é o positivo ("anon lê as 5 do contrato?" ✅) em vez do negativo ("anon lê o que NÃO deveria?"). Fix: nunca conceder no nível da tabela — `GRANT SELECT (col1,…,colN)` enumerado, e coluna nova passa a ser privada por omissão (default correto). Gate: no BEGIN..ROLLBACK, `has_any_column_privilege(role,tab,'SELECT')=true` **E** `has_table_privilege(role,tab,'SELECT')=FALSE` (se for `true`, o revoke é inerte); depois do apply, curl com `select=*` ⇒ `42501` (R-295). Documentar no cliente que `42501` em `select=*` é DESENHO — senão alguém "conserta" para `*` e a leitura morre parecendo falha de rede. Origem: 051-A PR 1.5a (`app_version_gate`, S-10), pego no rollback ANTES do apply. Ver [[AP-278]] · [[R-305]] -> [`anti-patterns/data_and_schema/AP-319.md`](./anti-patterns/data_and_schema/AP-319.md)
- **[AP-317]** CSV de export LGPD: BOM não respeitado pelo viewer (WPS Android — mojibake em texto acentuado, bytes escritos corretos verificado no source nativo) + header `'=== METADADOS ==='` sem escape líder → spreadsheet interpreta `=` como fórmula (`#ERROR!`). Achado no smoke real de device da spec 055 PR 1.2 (migração `expo-file-system`) — NÃO é bug da migração (bytes corretos), é bug de conteúdo pré-existente em `@dosiq/core` (spec 008). Correção separada, PO decidiu não bloquear PR 1.2. Ver [[R-270]] -> [`anti-patterns/data_and_schema/AP-317.md`](./anti-patterns/data_and_schema/AP-317.md)
- **[AP-315]** **Inventário herdado de arquivo tratado como CENSO: o que ele NEGA custa mais caro que o que ele erra.** Slice grande deixa no `tasks.md`/prompt um inventário medido ("são 22 arquivos", "o único `select()` explícito é X"); a sessão seguinte trabalha a partir dele. O que o doc **erra** é barato (mede de novo). O que ele **nega** é caro: você nunca procura, porque o documento já afirmou que não existe. Medido no 029 F6, contra um brief de 2 dias antes: (a) "**o** select explícito com `titration_status`" — havia **DOIS** (`dashboardService:31` **e** `treatmentsService:113`), e o segundo é `42703` em prod; (b) "70 protocolos com as colunas **não-nulas**" — as 68 linhas eram **byte-idênticas**, `titration_schedule=[]` em 100%: nunca existiu escada N1 em prod (coluna zumbi, não dado zumbi); (c) um arquivo já repontado 2 slices antes seguia na lista; (d) o export **LGPD** nem foi olhado — o DROP tiraria a titulação inteira da portabilidade do titular, em silêncio. Irmão do [[AP-308]], onde "a ÚNICA escrita em `protocols`" eram QUATRO. Regra: inventário é **piso, nunca teto**; refazer a medição SEMPRE (custa um grep, o alternativo é outage) — sobretudo quando o doc é recente e detalhado, porque precisão compra confiança e confiança impede a re-medição; desconfiar de todo singular/superlativo ("o único", "apenas", "só resta") sem o comando que o produziu; perguntar **o que o inventário não olhou** (o buraco não estava numa linha errada, estava fora do enquadramento); corrigir o doc no MESMO PR quando a medição o desmentir, senão a próxima sessão herda a mesma negação. Origem: 029 F6 (#765 · `acd50be2`). Ver [[R-295]] · [[R-296]] · [[R-302]] -> [`anti-patterns/data_and_schema/AP-315.md`](./anti-patterns/data_and_schema/AP-315.md)
- **[AP-293]** Tabela-filha denormaliza `user_id` (dono) p/ RLS direta MAS a FK p/ o pai referencia só `parent(id)` → a RLS de INSERT valida só `user_id=auth.uid()`, não que o `parent_id` é do mesmo dono. Cliente insere filha com o PRÓPRIO user_id (RLS passa) + `parent_id` ALHEIO → injeta filha na entidade de outro usuário. Invisível ao alvo (RLS de SELECT filtra), mas explode quando um consumidor bypassa RLS (cron/motor `service_role`, RPC DEFINER) e faz JOIN filha→pai por `parent_id` sem re-checar dono. Caso real 029 F1: `titration_steps` (motor F3 = service_role); teste de RLS cobria spoof de user_id na própria linha, não o vetor parent_id alheio — pego pelo Gemini #746. Fix: FK COMPOSTA `(parent_id, user_id) → parent(id, user_id)` (+ UNIQUE(id,user_id) no pai) — impossível no banco apontar p/ pai de outro dono. Guard: filha que denormaliza dono usa FK composta, nunca só parent_id; teste inclui o vetor parent_id-alheio+user_id-próprio. Irmão de AP-272/AP-278 -> [`anti-patterns/data_and_schema/AP-293.md`](./anti-patterns/data_and_schema/AP-293.md)
- **[AP-280]** `getNow()` (relógio DESLOCADO p/ o fuso do usuário) usado como "agora" em cálculo de DURAÇÃO contra um timestamptz absoluto → o gap perde o offset do fuso (GMT-3: 29d0h − 3h = 28d21h → floor 28). Verde em máquina GMT-3, VERMELHO no CI (UTC). Caso real 044 F2 (T004, gap de reativação do estoque). Regra: duração física (gap/TTL/idade) usa instante absoluto (`Date.now()`, `.getTime()`) dos dois lados; diferença de CALENDÁRIO ("que dia é hoje p/ o usuário") usa `getNow()`/`parseLocalDate` (R-020/ADR-049). Misturar as famílias no mesmo subtraendo é sempre bug. Farol: teste de tempo que passa local e falha no CI → rodar `TZ=UTC` antes do push. (`new Date()` é barrado pelo lint R-020 — usar `Date.now()`.) Primo de AP-270 -> [`anti-patterns/data_and_schema/AP-280.md`](./anti-patterns/data_and_schema/AP-280.md)
- **[AP-281]** Preferência de servidor cacheada em contexto (lida 1× por sessão) e NÃO reconciliada após a escrita do próprio app → o usuário vê o app contradizer a escolha que acabou de fazer (044 F3: escolheu dose-only no onboarding, caiu num app com a aba Estoque; reload consertava = dado certo, cache errado). Todo provider que cacheia valor de servidor expõe `refresh()`; TODA escrita desse valor pelo app chama o refresh antes de navegar/concluir. Farol: "dá reload e funciona" -> [`anti-patterns/react_and_ui/AP-281.md`](./anti-patterns/react_and_ui/AP-281.md)
- **[AP-282]** Erro de validação em campo que a tela NÃO renderiza → toast "verifique os campos destacados" sem nenhum campo destacado; fluxo morto (044 smoke: auto-fill ANVISA gravava `regulatory_category` cru ('Biologico') contra enum acentuado fechado; `normalizeRegulatoryCategory` existia e era usada no cadastro e na web — só o onboarding ficou de fora). Entrada externa que alimenta enum fechado passa pelo normalizador em TODOS os consumidores; toast genérico só é honesto se houver destaque — senão, mostrar a mensagem real -> [`anti-patterns/react_and_ui/AP-282.md`](./anti-patterns/react_and_ui/AP-282.md)
- **[AP-283]** Wizard que persiste em passo intermediário: ao ACRESCENTAR um passo, o passo que gravava deixa de ser o último e "voltar" passa a duplicar dado (044 F3: 4º passo no onboarding → voltar recriaria medicamento+tratamento; disfarçado com `navigation.reset`). Wizard grava no FIM, uma vez; passos acumulam em contexto. `reset` para impedir retorno = cheiro de escrita no lugar errado. Bônus: abandono no passo final deixava dado gravado + permissão de push queimada antes da gravação -> [`anti-patterns/react_and_ui/AP-283.md`](./anti-patterns/react_and_ui/AP-283.md)
- **[AP-284]** Preferência de conta cacheada em provider e reconciliada SÓ quando a escrita é local (o `refresh()` do [[AP-281]]) → a escrita vinda de OUTRA superfície nunca chega: desligar o estoque na web deixava o mobile (minimizado) mostrando aba/saldos até ser morto e reaberto; pull-to-refresh não cobre (refaz DADOS, não a preferência). Valor de servidor cacheado que pode ser escrito de +1 superfície DEVE revalidar no ponto de retomada: `AppState 'active'` (mobile) + `focus`/`visibilitychange` (web). Sem polling e sem realtime vivo p/ valor que muda 1×/mês; fail-safe [[AP-277]] vale NA revalidação (rede caiu → permanece ATIVO; nunca esconder superfície de quem usa). Farol: "mudei num device e o outro só obedeceu depois de reabrir". Caso real 044 F4a (#739), pego só no smoke multi-device. **Emenda 055-W1.7:** falha de rede na revalidação com CACHE local existente preserva o cache (não reativa) — só falha SEM cache aciona o fail-safe puro; ver distinção (a)/(b) no arquivo -> [`anti-patterns/react_and_ui/AP-284.md`](./anti-patterns/react_and_ui/AP-284.md)
- **[AP-285]** Opção destrutiva renderizada como BOTÃO DE AÇÃO → selecionar É executar (sem confirmação, sem chance de reler a alternativa). 044 F4a (#739): no sheet de reconciliação a web chamava `resumeAndZero()` — que ZERA o saldo de todos os medicamentos — no clique da própria opção "Começar do zero", enquanto o mobile pedia rádio + "Ativar estoque": a MESMA decisão com semânticas diferentes por superfície. Ação irreversível exige DOIS atos: escolher (`role=radiogroup`/`radio` + `aria-checked` + roving tabindex, foco acompanhando a seleção) e confirmar (botão separado); paridade web↔mobile no nº de atos. Escolha reseta a cada abertura via MONTAGEM CONDICIONAL (desmontar reseta) — nunca via effect (`set-state-in-effect`), e jamais deixar a opção destrutiva pré-marcada de uma tentativa anterior. Pego pelo revisor externo -> [`anti-patterns/react_and_ui/AP-285.md`](./anti-patterns/react_and_ui/AP-285.md)
- **[AP-279]** Mock de client de query que IGNORA o operador de filtro (`.in()`/`.eq()` como `mockReturnThis()` no-op, `then` devolvendo o array completo) faz o teste medir o MOCK, não o código. Caso real 044 F2 (T005): teste do filtro dose-only nos alertas de estoque falhou com o código CORRETO (mock devolvia a linha do usuário que o `.in()` tinha excluído). O modo GRAVE é o simétrico — mock devolvendo dados já filtrados à mão faz o assert passar mesmo sem filtro nenhum no código (falso-verde em filtro de segurança/escopo/opt-out). Regra: o mock DEVE honrar o operador que o teste afirma; o que ele não aplica, o teste não pode afirmar. Padrão forte = assertar o ARGUMENTO do filtro + a saída já filtrada. Irmão de AP-276/R-288 (não mockar o primitivo sob teste) -> [`anti-patterns/test_hygiene/AP-279.md`](./anti-patterns/test_hygiene/AP-279.md)
- **[AP-278]** GRANT de `anon` em função `SECURITY DEFINER` que MUTA dado, tolerado porque "na prática ela falha mesmo" → proteção POR ACIDENTE, não por controle de acesso. `restore_stock_for_log` (muta estoque) tinha EXECUTE p/ anon; só não era explorável porque resolvia o dono por `auth.uid()`, NULL p/ anon → abortava. O acidente EVAPORA no primeiro refactor legítimo: a task que ia fazê-la aceitar `p_user_id` (p/ rodar server-side) transformaria o grant num bypass real (anon restaura estoque de terceiro passando o user_id). Fix: `REVOKE EXECUTE ... FROM PUBLIC, anon` + GRANT explícito p/ authenticated/service_role (CLAUDE.md §Migrações), ANTES do refactor. Regra: auditar `has_function_privilege('anon', oid,'EXECUTE')` de TODA SECURITY DEFINER que escreve — o guard interno e o grant são camadas INDEPENDENTES; nenhuma pode ser a única. Detectado no review #735 (Gemini apontou o guard que não cobria anon nas 3 RPCs — não explorável, mas a auditoria de grants que ele provocou achou o furo real ao lado). Irmão de AP-275 -> [`anti-patterns/data_and_schema/AP-278.md`](./anti-patterns/data_and_schema/AP-278.md)
- **[AP-277]** PL/pgSQL: `SELECT ... INTO v` que retorna ZERO linhas **atribui NULL** a `v` — NÃO preserva o valor de inicialização. `COALESCE` DENTRO do SELECT nem é avaliado sem linha. Flag booleana de política inicializada com o default seguro (`v_stock_tracking BOOLEAN := true`) vira NULL → `IF v THEN` falso → guard cai para o lado errado (**fail-OPEN**) em silêncio. Caso real 044 F1: usuário sem linha em `user_settings` teria estoque DESLIGADO por omissão (contra FR-009); dose registrava com saldo 0 sem "Estoque insuficiente". Fix: `v := COALESCE(v, true)` DEPOIS do SELECT (cobre "sem linha" E "coluna NULL"). Detectado pelo teste BEGIN..ROLLBACK do failure mode (R-270) — o preflight escrito pelo agente afirmava a premissa ERRADA; só a execução contra o banco derrubou. Lição: failure mode do R-270 só vale EXECUTADO. Família AP-276/R-288. **Emenda 055-W1.7:** o fail-safe cobre só o caso "nunca soube da preferência" (a) — "sei que o usuário desligou" (b) é coberto por um degrau de cache ANTES deste, nunca pelo fail-safe em si -> [`anti-patterns/data_and_schema/AP-277.md`](./anti-patterns/data_and_schema/AP-277.md)
- **[AP-273]** TTL/threshold temporal de higiene (`last_seen_at < now()-ttl`) fixado no spec sem calibrar contra a distribuição REAL de prod → efeito imediato zero. 043 Slice B: TTL 60d specado desativaria 0 (farm do reviewer todo <32d). Fix: (1) medir distribuição por faixa (`count() FILTER`) antes de fixar o valor + antes de aplicar; valor = decisão do operador quando é tradeoff de segurança; (2) separar proof do outcome — PO fecha pelo GUARD verificável na hora (`recent_wrongly_deactivated=0` + rows preservados), outcome agregado gradual vira SC de observação, nunca `expect` imediato; (3) baseline + prévia de impacto antes do write. Relaciona AP-208/AP-187 -> [`anti-patterns/data_and_schema/AP-273.md`](./anti-patterns/data_and_schema/AP-273.md)
- **[AP-272]** Tabela com `user_id NOT NULL` **sem FK p/ `auth.users`** E fora da RPC `delete_user_account()` → excluir conta deixa dado de saúde do titular **órfão** (latente/silencioso; 0 órfãos até a 1ª exclusão real). Caso: `dose_instances`/`dose_adherence_monthly`/`dose_critical_events` (a última sem FK alguma → nunca limpa; as outras só por acaso via `protocol_id` CASCADE, órfãs se `protocol_id` NULL). Anexar na RPC sozinho repete o erro na próxima tabela. Fix: FK `user_id→auth.users ON DELETE CASCADE` (cobre RPC + prune Admin API); trilha que sobrevive usa SET NULL deliberado. CASCADE bypassa RLS/sem BEFORE DELETE trigger → ok com append-only. Testar BEGIN..ROLLBACK (contar=0). Detectado 046 §T000 -> [`anti-patterns/data_and_schema/AP-272.md`](./anti-patterns/data_and_schema/AP-272.md)
- **[AP-240]** Janela de vigência/dias-restantes por diff de ms em tempo real (`(end - getNow())/86400000`) → prescrição que vence HOJE vira falso 'vencida' já ao meio-dia + drift de DST. Regra é sobre DATAS de calendário, não instantes. Fix: normalizar `now` p/ meia-noite local (`parseLocalDate(formatLocalDate(now))`, sem `new Date()` por R-020) + `Math.round` + `MS_PER_DAY` nomeado; `end===hoje`→0→'vencendo'. Alertas diários/cron/janelas-N-dias comparam datas, nunca `Date.now()` em janela 24h. Latente no `Stock.jsx` original; carve-out+teste expôs (HIGH Gemini #680) -> [`anti-patterns/data_and_schema/AP-240.md`](./anti-patterns/data_and_schema/AP-240.md)
- **[AP-237]** Endpoint de LLM (`api/chatbot.js`) **sem auth** + `systemPrompt` montado no **cliente** → abuso anônimo de quota Groq + bypass das "REGRAS ABSOLUTAS" (cliente removia guardrails SaMD). Fix: auth Supabase JWT obrigatória (401), system prompt composto NO SERVIDOR (cliente só envia `patientContext`), safety guard + rate limit server-side. Validação só client-side é UX, não segurança -> [`anti-patterns/infra_and_deploy/AP-237.md`](./anti-patterns/infra_and_deploy/AP-237.md)
- **[AP-238]** Path alias vive em 2 configs (`vite.config.js` + `eslint.config.js`); mover destino de alias atualizando só o vite deixa `import-x/no-unresolved` no lint (resolver eslint é separado). Build/test resolvem pelo vite e mascaram. Fix: ao mover pasta com alias, grep+atualizar AMBOS; `rtk lint` é o gate que pega. Detectado 038 Slice A (`@settings`) -> [`anti-patterns/infra_and_deploy/AP-238.md`](./anti-patterns/infra_and_deploy/AP-238.md)
- **[AP-239]** Flatten de pasta `redesign/` → `components/` colide com arquivo legacy de mesmo nome no destino (não só a base homônima do componente principal). Premissa "base morta deletada no B1 = collision-free" é insuficiente. Fix: antes do `git mv`, `ls` destino + cruzar TODOS os nomes-alvo; colisão com legacy dead (0 importers) → `git rm` antes; build checkpoint por pasta. Detectado 038 Slice B2 (`ConsultationSections` legacy) -> [`anti-patterns/react_and_ui/AP-239.md`](./anti-patterns/react_and_ui/AP-239.md)
- **[AP-236]** Ler metadado de ancoragem (`instance_id`) do objeto **pós-Zod** → `undefined` (não é coluna, `logSchema` o estrita no `safeParse`) → ancoragem em lote quebrada (toda dose vira avulsa). Bateu web E mobile no mesmo PR #673. Fix: mapear sobre `validatedLogs` mas ler o metadado do **input cru por índice** (`logsData[index].instance_id`). Irmão de AP-214 (sintoma igual, fix oposto: lá adiciona ao schema, aqui nunca espera do schema). Detectado review Gemini #673 -> [`anti-patterns/data_and_schema/AP-236.md`](./anti-patterns/data_and_schema/AP-236.md)
- **[AP-231]** Mutar `medicine_logs` direto (UPDATE/DELETE) sem replay dos efeitos colaterais do registro (estoque FIFO + âncora da instância) → corrupção. Dois casos no PR #668: (a) **ghost taken** web — `logService.delete` deletava o log e a FK `ON DELETE SET NULL` limpava `medicine_log_id` mas deixava `dose_instance.status='taken'` → dose fantasma contando na adesão; (b) **furo de estoque** mobile — `useHistoryMutation` mutava log via cliente bypassando o FIFO. Fix: toda mutação no service (`logService`/`doseService.updateOrphanLog`/`deleteOrphanLog`) restaura+reconsome estoque e reverte instância; paridade web↔mobile↔bot. FK SET NULL nunca reverte campos de estado dependentes. Irmão de AP-193/AP-185. Detectado smoke+review #668 -> [`anti-patterns/data_and_schema/AP-231.md`](./anti-patterns/data_and_schema/AP-231.md)
- **[AP-228]** Prefill com `''` em campo `z.enum().nullable()` (ex.: `intake_unit ?? ''`) → `''` não é membro do enum NEM `null` → update falha mudo. Pior quando o input do campo é condicional (renderiza só p/ líquidos): erro "campo obrigatório" sem campo visível. Caso real: editar tratamento não-líquido (smoke C). Fix: fallback `?? null`. Smoke deve cobrir edit de TODA variante (líquido E sólido). Irmão de AP-225/AP-214. Detectado smoke #665 -> [`anti-patterns/data_and_schema/AP-228.md`](./anti-patterns/data_and_schema/AP-228.md)
- **[AP-227]** `CREATE OR REPLACE FUNCTION` com parâmetro **adicionado** NÃO substitui — cria um **OVERLOAD** (assinatura nova ≠ antiga), deixando a antiga viva → `PGRST203` (ambiguidade) ou resolve p/ a versão velha sem a lógica nova. Ex.: `create_purchase_with_stock` +`p_injection_container` (B4) deixou 8-arg + 9-arg coexistindo. Fix: `DROP FUNCTION IF EXISTS ...(assinatura antiga)` ANTES do CREATE; conferir `pg_get_function_identity_arguments` pós-migração. Primo de AP-221. Detectado smoke B4 -> [`anti-patterns/data_and_schema/AP-227.md`](./anti-patterns/data_and_schema/AP-227.md)
- **[AP-226]** `formatNumberPtBR` (e qualquer `String(Number(x))`) NÃO arredonda — exibe o float cru. Multiplicar/dividir concentração sem limpar (`ratio*vol`, `amount/denom`) faz a dízima JS vazar pro chip/card E ser persistida. Fix: `cleanFloat` antes de exibir/persistir (R-277). Detectado review #661 -> [`anti-patterns/data_and_schema/AP-226.md`](./anti-patterns/data_and_schema/AP-226.md)
- **[AP-225]** Zod v4: `.partial()` NÃO remove `.default()` — update parcial materializa o default e grava por cima do valor real. `medicineUpdateSchema.partial()` flipava `presentation: injetavel→comprimido` (líquido→sólido, corrupção clínica) em qualquer update que omitisse o campo. Fix: override dos campos com default p/ `.optional()` sem default no update; defaults só no create. Irmão de AP-223/AP-214. -> [`anti-patterns/data_and_schema/AP-225.md`](./anti-patterns/data_and_schema/AP-225.md)
- **[AP-223]** Zod v4: `.partial()` lança `Error` em runtime em schema com `.superRefine()` — invisível em build/lint/testes. Fix: extrair `BaseSchema = z.object({...})` sem refinements; `Schema = BaseSchema.superRefine(...)`; update usa `BaseSchema.partial()`, create usa `Schema`. -> [`anti-patterns/data_and_schema/AP-223.md`](./anti-patterns/data_and_schema/AP-223.md)
- **[AP-221]** Drift de assinatura de RPC: mudar args (add/rename/reorder) numa migração sem atualizar TODOS os callers JS → `PGRST202` só em runtime (PostgREST resolve por nome+args, sem erro de compilação). Ex.: `consume_stock_fifo` +`p_user_id` (Fase A) quebrou callers web+mobile na tomada de dose. Grep callers + atualizar na mesma fase + smoke write-path. Relaciona R-270 -> [`anti-patterns/data_and_schema/AP-221.md`](./anti-patterns/data_and_schema/AP-221.md)
- **[AP-214]** Campo ausente no `protocolSchema` (Zod) → `partial()` stripa silenciosamente em cada update → campo nunca persiste no DB. Adicionar ao schema base antes de qualquer migration -> [`anti-patterns/data_and_schema/AP-214.md`](./anti-patterns/data_and_schema/AP-214.md)
- **[AP-190]** Modelo materializado sem writer de regime p/ uma transição de status: `dose_instances.missed` só era setado pelo backfill one-shot; nenhum cron fazia `pending→missed` → adesão inflada + streak não quebra (latente, degrada silencioso). Toda transição precisa writer recorrente + invariante de completude; backfill não conta; consumidor audita o produtor. Fix F2.5: sweep `markMissedDueInstances` + missed re-ancorável -> [`anti-patterns/data_and_schema/AP-190.md`](./anti-patterns/data_and_schema/AP-190.md)
- **[AP-181]** `FREQUENCY_MATCHERS` (isProtocolActiveOnDate) desatualizado vs DB: falta `dias_alternados` (→ vira diário) e `personalizado` → false (→ nunca ativo); latente (0 protocolos afetados em prod), fix na Fase 3 -> [`anti-patterns/data_and_schema/AP-181.md`](./anti-patterns/data_and_schema/AP-181.md)
- **[AP-164]** Função com mesmo nome em arquivos diferentes do mesmo package — barrel `export *` colide silenciosamente; consumer importa a errada (isProtocolActiveOnDate strict vs period-only) -> [`anti-patterns/data_and_schema/AP-164.md`](./anti-patterns/data_and_schema/AP-164.md)
- **[AP-168]** Cache invalidation incompleto em mutation — esquece snapshots adjacentes (toggleActive invalidou só protocols, esqueceu treatments-snapshot) -> [`anti-patterns/data_and_schema/AP-168.md`](./anti-patterns/data_and_schema/AP-168.md)
- **[AP-058]** z.record() argumento único quebra parse em Zod v4 — TypeError '_zod' com objetos não-vazios -> [`anti-patterns/data_and_schema/AP-058.md`](./anti-patterns/data_and_schema/AP-058.md)
- **[AP-005]** UTC midnight = wrong day in GMT-3 -> [`anti-patterns/data_and_schema/AP-005.md`](./anti-patterns/data_and_schema/AP-005.md)
- **[AP-B02]** HTTP 400 Bad Request + `[QueryCache] Fetch falhou` em toda abertura da view afet... -> [`anti-patterns/data_and_schema/AP-B02.md`](./anti-patterns/data_and_schema/AP-B02.md)
- **[AP-D01]** Protocol A with ["08:00", "20:00"] = 2 doses expected. `COUNT(DISTINCT)` returns... -> [`anti-patterns/data_and_schema/AP-D01.md`](./anti-patterns/data_and_schema/AP-D01.md)
- **[AP-D03]** After CROSS JOIN LATERAL jsonb_array_elements_text (expands doses), `COUNT(*)` c... -> [`anti-patterns/data_and_schema/AP-D03.md`](./anti-patterns/data_and_schema/AP-D03.md)
- **[AP-D07]** Core-Mobile Adherence Parity Gap -> [`anti-patterns/data_and_schema/AP-D07.md`](./anti-patterns/data_and_schema/AP-D07.md)
- **[AP-D08]** Fragile date parsing via toLocaleString default -> [`anti-patterns/data_and_schema/AP-D08.md`](./anti-patterns/data_and_schema/AP-D08.md)
- **[AP-P15]** `calculateStreaks()` criava ~2700 Date objects (90 dias × N protocolos × 3 calls... -> [`anti-patterns/data_and_schema/AP-P15.md`](./anti-patterns/data_and_schema/AP-P15.md)
- **[AP-P16]** Ignora fuso horário local. Em GMT-3 (Brasil), `2026-03-01T00:00:00.000Z` = 21:00... -> [`anti-patterns/data_and_schema/AP-P16.md`](./anti-patterns/data_and_schema/AP-P16.md)
- **[AP-P17]** HTTP 400 Bad Request silencioso. UI mostra "Erro ao carregar dados" sem mensagem... -> [`anti-patterns/data_and_schema/AP-P17.md`](./anti-patterns/data_and_schema/AP-P17.md)
- **[AP-S01]** 500 error on INSERT, data rejected by database -> [`anti-patterns/data_and_schema/AP-S01.md`](./anti-patterns/data_and_schema/AP-S01.md)
- **[AP-S08]** Database error, failed writes -> [`anti-patterns/data_and_schema/AP-S08.md`](./anti-patterns/data_and_schema/AP-S08.md)
- **[AP-182]** `node-cron` em `server/` só roda em DEV — cron de prod é endpoint serverless + cron-job.org gated por hora; job agendado novo precisa de caminho `api/` (ver ADR-051) -> [`anti-patterns/infra_and_deploy/AP-182.md`](./anti-patterns/infra_and_deploy/AP-182.md)
- **[AP-183]** Auth de cron `authHeader !== \`Bearer ${process.env.SECRET}\`` sem guarda de presença: se a env var não está setada, vira `"Bearer undefined"` e qualquer requisição com esse header burla a auth. Sempre `if (!secret || authHeader !== ...)` (falha-fechado). Aplica a qualquer validador comparando contra env var (Gemini #604) -> [`anti-patterns/infra_and_deploy/AP-183.md`](./anti-patterns/infra_and_deploy/AP-183.md)
- **[AP-184]** Import ESM de subpath de pacote **sem extensão** (`import x from 'zod/v4/locales/pt'`): Vite/vitest anexam `.js` e mascaram, mas o Node ESM estrito (runtime serverless Vercel, `packages/*`+`server/*` deployados raw) falha com `ERR_MODULE_NOT_FOUND`. Em código que entra no grafo de `api/*`, sempre usar extensão `.js` explícita -> [`anti-patterns/infra_and_deploy/AP-184.md`](./anti-patterns/infra_and_deploy/AP-184.md)
- **[AP-185]** Conditional UPDATE (`.eq('status','pending')`) sem checar linhas afetadas: em corrida, o 2º writer vira no-op silencioso (0 linhas), mas o caller assume sucesso e grava o elo inverso → vínculo unidirecional/órfão. Usar `.select()` no update e retornar boolean; caller só prossegue se afetou linha (Gemini #609) -> [`anti-patterns/data_and_schema/AP-185.md`](./anti-patterns/data_and_schema/AP-185.md)
- **[AP-186]** SELECT Supabase/PostgREST sem paginação trunca em ~1000 linhas SEM erro — varredura/migração/export opera sobre subconjunto silenciosamente. Paginar com `.range()` em loop até página incompleta; se o processamento muta o filtro, ler tudo antes de escrever. Pego por canário (1000 vs 1778 real) no backfill (PR-F2.4) -> [`anti-patterns/data_and_schema/AP-186.md`](./anti-patterns/data_and_schema/AP-186.md)
- **[AP-187]** Auto-report de sub-agente em operação de prod ≠ verdade (Haiku reportou b0c9 `0/0/0`, DB mostrou 1657 taken). Delegação de migração/bulk-write DEVE ser idempotente+reversível e o orquestrador verifica no DB com invariantes (ex: `count(taken)===count(logs com elo)`, 0 dupes, cobertura de todos os alvos) — nunca aceita o auto-report como prova -> [`anti-patterns/process_and_testing/AP-187.md`](./anti-patterns/process_and_testing/AP-187.md)
- **[AP-SL01]** Logs não aparecem em Vercel. Função Node.js server context é invisível para Verc... -> [`anti-patterns/data_and_schema/AP-SL01.md`](./anti-patterns/data_and_schema/AP-SL01.md)
- **[AP-SL02]** Handler chama `bot.sendChatAction()` que não existe no mock → `"is not a functio... -> [`anti-patterns/data_and_schema/AP-SL02.md`](./anti-patterns/data_and_schema/AP-SL02.md)
- **[AP-T10]** Tests pass in GMT but fail in GMT-3 (local); date off by 1 day -> [`anti-patterns/data_and_schema/AP-T10.md`](./anti-patterns/data_and_schema/AP-T10.md)
- **[AP-W07]** Test fails: `2026-03-06T02:00:00Z` = 23:00 BRT = still "today" -> [`anti-patterns/data_and_schema/AP-W07.md`](./anti-patterns/data_and_schema/AP-W07.md)
- **[AP-W12]** `dosage_per_intake = 0` becomes `1`; incorrect dose recorded -> [`anti-patterns/data_and_schema/AP-W12.md`](./anti-patterns/data_and_schema/AP-W12.md)
- **[AP-W14]** Test passes in BRT but fails in CI (UTC): same UTC timestamp = different local h... -> [`anti-patterns/data_and_schema/AP-W14.md`](./anti-patterns/data_and_schema/AP-W14.md)
- **[AP-W16]** CI reporta apenas o PRIMEIRO teste que falha; outros testes timezone-dependentes... -> [`anti-patterns/data_and_schema/AP-W16.md`](./anti-patterns/data_and_schema/AP-W16.md)
- **[AP-115]** Enum Zod do dispatcher não atualizado ao adicionar novos kinds — novos notification types são rejeitados silenciosamente antes de chegar aos canais -> [`anti-patterns/data_and_schema/AP-115.md`](./anti-patterns/data_and_schema/AP-115.md)
- **[AP-N20]** useEffect de hidratação sem sincronizar todos os estados locais derivados de settings — campo ausente mantém valor inicial do useState; nunca usar `?? 'valor'` quando o schema Zod já garante `.default()` -> [`anti-patterns/react_and_ui/AP-N20.md`](./anti-patterns/react_and_ui/AP-N20.md)
- **[AP-142]** consume_stock_fifo com `entry_type='purchase'` bloqueia consumo de entradas adjustment legítimas — estoque visível na UI mas FIFO lança "Estoque insuficiente" (phantom stock) -> [`anti-patterns/data_and_schema/AP-142.md`](./anti-patterns/data_and_schema/AP-142.md)
- **[AP-189]** Fallback de arrays vazios `[]` do banco de dados usando o operador lógico `||` em JavaScript -> [`anti-patterns/data_and_schema/AP-189.md`](./anti-patterns/data_and_schema/AP-189.md)

## 🔔 Notifications (`notifications`)
- **[AP-255]** Gate de canal de notificação **crítica/timeSensitive** que só *adiciona* o canal de alarme (mobile_push) sem *remover* os demais → dose crítica dispara em paralelo no Telegram/web push, duplicando o aviso (e o snooze re-dispara tudo). `resolveChannelsForUser` fazia `if(isCritical) channels.push('mobile_push')` — aditivo. Fix: crítica = **só** `mobile_push` se houver device ativo; **fallback** telegram se não houver (nunca muda); não-crítica segue preferências. Bônus: `critical_alarm` precisa estar no `data` do dispatch senão `isCritical` é sempre false. Guard: teste crítica+device→`['mobile_push']`, crítica+sem-device→`['telegram']`. Bug prod (alarme + telegram juntos no snooze) -> [`anti-patterns/notifications/AP-255.md`](./anti-patterns/notifications/AP-255.md)

## 🚀 Infra & Deploy (`infra_and_deploy`)
- **[AP-320]** Env de controle de segurança lida em ESCOPO DE MÓDULO depende da ordem de import: `server/utils/auth.ts` viu `ADMIN_USER_ID` como `undefined` enquanto `api/admin.ts` a viu preenchida, **no mesmo processo** — um `dotenv/config` transitivo (via `deadLetterQueue.js`) injeta o `.env` DURANTE o grafo de import, e a ordem ESM avalia o módulo importado antes do corpo do importador (probe: `C depois de auth.ts: undefined` → `D depois de deadLetterQueue: b0c9…`). Em produção não havia impacto (Vercel injeta antes do processo) e a divergência negou o acesso — mas "seguro porque o import certo veio primeiro" é [[AP-278]] puro: evapora num reorder de imports, e o modo de falha oposto (liberar) é indistinguível no código. Fix: ler a env NA CHAMADA (`readConfig()` dentro da função), não no topo do arquivo. Gate: o fail-closed NÃO é demonstrável em harness local (`dotenv@17` sobe a árvore e reinjeta o `.env` do repo) — encodar com `vi.stubEnv('ADMIN_USER_ID','')` + assere que nega ANTES de chamar `getUser()`. Origem: 051-A PR 1.5a, achado ao executar o PO-SEC-4. Ver [[AP-278]] · ADR-091 D10 -> [`anti-patterns/infra_and_deploy/AP-320.md`](./anti-patterns/infra_and_deploy/AP-320.md)
- **[AP-276]** 🔴 FURO DE ARQUITETURA (concorrência). Claim de fila com `FOR UPDATE SKIP LOCKED` que **não muda o status** da linha reivindicada → a exclusão dura só a transação do RPC; após o commit o lock solta e a linha volta a ser reivindicável **durante o processamento assíncrono** → outro drenador/tick concorrente pega a MESMA linha → **envio/efeito duplicado** (UNIQUE de insert NÃO protege — é a mesma linha). SKIP LOCKED resolve contenção de claim DENTRO da txn, não posse exclusiva ATRAVÉS da janela de processamento. Caso: `claim_notification_outbox` (043 Slice A) deixava `status='pending'` — passou o gate de failure-modes (só cobria inputs degenerados, não interleaving) e o teste com **mock do claim** (o próprio primitivo sob prova → propriedade assumida, não exercida — família AP-259). Pego só por revisor externo (Gemini #734 re-review). Fix: claim seta `status='processing'` + `claimed_at=now()` na MESMA txn (posse exclusiva) + **recuperação de stuck**: reivindicar `processing` preso > N min (worker que crasha entre claim e finalize), senão 'processing' vira buraco negro (efeito perdido); `revertClaim`/`markFailed` voltam a 'pending' limpando `claimed_at`. Ver [[R-288]]. -> [`anti-patterns/infra_and_deploy/AP-276.md`](./anti-patterns/infra_and_deploy/AP-276.md)
- **[AP-275]** Confiar na nota da CLAUDE.md "tabelas novas pós-30/10 não recebem grants automáticos" e **omitir REVOKE** numa tabela server-internal → o DB (dosiq prod) TEM default privileges legados que auto-concedem `INSERT/SELECT/UPDATE/DELETE/...` a **anon E authenticated** em toda tabela nova de `public`. RLS habilitada sem policy nega as LINHAS (dado seguro), mas o GRANT existe (fura defense-in-depth + reprova PO-SEC-2 literal). Detectado 043 Slice A: `notification_outbox` nasceu com grants anon/authenticated apesar do "nenhum grant emitido". Fix: SEMPRE `REVOKE ALL ON <tabela> FROM anon; REVOKE ALL ... FROM authenticated;` em tabela server-internal + **verificar live** `information_schema.role_table_grants` (não confiar na premissa). Relaciona AP-183 (least-privilege) -> [`anti-patterns/infra_and_deploy/AP-275.md`](./anti-patterns/infra_and_deploy/AP-275.md)
- **[AP-274]** Arquivo do **island strict-A** (ex.: `server/notifications/*`) que **importa código nível-B** não-tipado (ex.: `server/bot/_adherenceHelpers.ts`) arrasta a subárvore inteira do B pro compile `tsconfig.strict.json` → catraca `B server/*` estoura (+27 erros TS7006 que antes nem eram contados) mesmo com a fonte A limpa. Sintoma: strict-island.sh vira vermelho num commit que só adicionou arquivos A "limpos". Fix: **inverter a dependência** — o consumidor nível-B (ex.: `api/notify.ts`) monta o registry e **injeta** os builders no arquivo A por parâmetro (DI); o island A nunca importa B. Guard: arquivo em `server/notifications/` só importa outros A ou tipos; dep A→B vira injeção. Relaciona R-283/R-284. Detectado 043 Slice A -> [`anti-patterns/infra_and_deploy/AP-274.md`](./anti-patterns/infra_and_deploy/AP-274.md)
- **[AP-259]** Cron por-minuto que seleciona trabalho por **fatia de 1 minuto exata** (`scheduled_for ∈ [now+lead, +1min)`) perde qualquer alvo cujo gatilho `T−lead` já passou (dose criada/editada p/ `<lead`min) ou se o tick pular aquele minuto. Sintoma: LA push-to-start nunca disparava p/ doses de curto prazo (`la_push_started_at`=0 no histórico); app fechado sem LA (alarme T0 ainda tocava — degradação). Mock que ignora o filtro de tempo esconde (bug vive no predicado SQL). Fix: janela = **range** `[now, now+lead]` + trava de idempotência (`la_push_started_at IS NULL`). Guard: cron temporal usa range até o horizonte, nunca fatia de minuto; teste captura `gte`/`lte` e filtra por eles; emitir sinal de falha (não só sucesso). Debug trail 042/spec 041 -> [`anti-patterns/infra_and_deploy/AP-259.md`](./anti-patterns/infra_and_deploy/AP-259.md)
- **[AP-256]** Cliente HTTP/2 de saída (APNs) sem timeout em serverless pendura a função até o teto da plataforma → `http2.connect`+`request` sem `setTimeout`; se a Apple demora/conexão falha silenciosa, a `Promise` nunca resolve, travando o slot (e atrasando o cron de lembretes onde o disparo roda). Fix: `setTimeout(5s)` que fecha o cliente e resolve fail-open `{ok:false,reason:'timeout'}` + `safeResolve` (clearTimeout+resolve) em TODOS os terminadores. Guard: todo I/O de saída em `server/`/`api/` precisa de timeout explícito. Review Gemini PR #696 (spec 041) -> [`anti-patterns/infra_and_deploy/AP-256.md`](./anti-patterns/infra_and_deploy/AP-256.md)
- **[AP-242]** Subdomínio/URL do Supabase (`*.supabase.co`) hardcoded em código de app/scripts num repo **opensource público** → vaza o id do projeto + acopla o código a um ambiente. Detectado no mobile `useMedicineDatabase` (base URL ANVISA) e em `scripts/analyze-notifications.js`. Fix: ler de env (`EXPO_PUBLIC_SUPABASE_URL` via `nativePublicAppConfig` no mobile; `VITE_SUPABASE_URL` na web; `SUPABASE_URL` em scripts), nunca literal. Guard: `git grep '\.supabase\.co'` em arquivos tracked deve achar só docs/audit, nunca código. Detectado smoke 037 Slice 2 -> [`anti-patterns/infra_and_deploy/AP-242.md`](./anti-patterns/infra_and_deploy/AP-242.md)
- **[AP-243]** `Fragment` como filho direto de `AnimatePresence` quebra a animação de `exit` (e mascara a raiz do warning de key). O AnimatePresence precisa de filhos keyed diretos (motion/component) p/ adiar a desmontagem. Fix: blocos condicionais keyed separados; não-animados ficam fora do presence. Detectado spec 015 Onda 3 (review #687) -> [`anti-patterns/react_and_ui/AP-243.md`](./anti-patterns/react_and_ui/AP-243.md)
- **[AP-244]** Adapter cross-superfície passa shape local em vez do contrato do core: web alimenta `buildPatientContext` com `stats` do Dashboard (`rates.adherence`) em vez de `stats.adherence` (CON-028) → adesão sumia do payload do chatbot só na web (mobile/Telegram usam o fetcher canônico). Fix: conformar o adapter ao contrato (`{ ...stats, adherence: stats?.rates?.adherence ?? stats?.adherenceRate ?? null }`). Detectado spec 015 Onda 3 smoke -> [`anti-patterns/data_and_schema/AP-244.md`](./anti-patterns/data_and_schema/AP-244.md)
- **[AP-217]** Reescrever função SQL existente a partir do spec (não do `pg_get_functiondef` ao vivo) descarta invariantes: overloads, filtros (`entry_type`), guards de posse, ordenação FIFO — regressões silenciosas que lint/testes web não pegam. Diff real vs proposto + grep callers por overload + teste BEGIN…ROLLBACK antes de aplicar -> [`anti-patterns/infra_and_deploy/AP-217.md`](./anti-patterns/infra_and_deploy/AP-217.md)
- **[AP-H27]** Import relativo cross-boundary quebra após git mv em monorepo — tests em src/ com `../../../../server/` falham -> [`anti-patterns/infra_and_deploy/AP-H27.md`](./anti-patterns/infra_and_deploy/AP-H27.md)
- **[AP-096]** CSS Deletion Without Verifying className Usage in JSX -> [`anti-patterns/infra_and_deploy/AP-096.md`](./anti-patterns/infra_and_deploy/AP-096.md)
- **[AP-H04]** npm ERESOLVE conflict when adding new workspace package with incompatible peer d... -> [`anti-patterns/infra_and_deploy/AP-H04.md`](./anti-patterns/infra_and_deploy/AP-H04.md)
- **[AP-H05]** GitHub Actions npm ci fails when new workspace added but lock file not committed -> [`anti-patterns/infra_and_deploy/AP-H05.md`](./anti-patterns/infra_and_deploy/AP-H05.md)
- **[AP-130]** SyntaxError: The requested module does not provide an export named 'X' (ESM) -> [`anti-patterns/infra_and_deploy/AP-130.md`](./anti-patterns/infra_and_deploy/AP-130.md)
- **[AP-SL03]** Listeners específicos (com patterns/sessão) capturam algumas mensagens, outras c... -> [`anti-patterns/infra_and_deploy/AP-SL03.md`](./anti-patterns/infra_and_deploy/AP-SL03.md)
- **[AP-140]** Duplicate Imports in ESM Node.js -> [`anti-patterns/infra_and_deploy/AP-140.md`](./anti-patterns/infra_and_deploy/AP-140.md)
- **[AP-179]** Entitlement novo no `app.config.js` (ex.: `associatedDomains`) → `expo run:ios` local falha (erro 65) porque o provisioning profile cacheado é anterior à capability. Habilitar no App ID não retroage ao profile. Fix: `rm -rf ~/Library/MobileDevice/Provisioning\ Profiles/` + regerar (Xcode toggle auto-signing). EAS registra via API Apple automaticamente -> [`anti-patterns/infra_and_deploy/AP-179.md`](./anti-patterns/infra_and_deploy/AP-179.md)

## 📱 Mobile & Platform (`mobile_and_platform`)
- **[AP-323]** `navigationRef.addListener('state', ...)` anexado ANTES do `NavigationContainer` montar nunca dispara depois — mesmo quando a navegação de fato acontece. `useAlarmScreenActive` (VersionGateOverlay cede pro alarme full-screen) montava fora da árvore de navegação (sibling de `NavigationTree`, que só cria o container depois que `session` resolve) e o listener ficava pendurado num alvo que ainda não existia. Fix: polling (`setInterval` 250ms lendo `isReady()`/`getCurrentRoute()`) em vez de evento — independe de ordem de montagem entre componentes irmãos. Introduzido pelo próprio fix anterior da mesma sessão (retry de `navigationRef.isReady()` no `AlarmSchedulerBridge`), só exposto na 2ª rodada de smoke. Smoke 055/1.5b (kill switch) -> [`anti-patterns/mobile_and_platform/AP-323.md`](./anti-patterns/mobile_and_platform/AP-323.md)
- **[AP-322]** `?? []` na fronteira hook→tela apaga a distinção `null` (erro, sem dado) vs `[]` (sucesso, vazio real). `useTreatments.ts:59` fazia `groups: transformed.groups ?? []`; o transformer devolvia `null` de propósito em erro pra `TreatmentsScreen.tsx:192` (`state.error && !state.groups`) escolher `ErrorState`, mas o `??` apagava isso antes de chegar na tela — offline com dados reais mostrava "você não tem tratamentos" em vez do erro de conexão. `useTodayData.ts` não tinha o mesmo coalescing e funcionava certo. Fix: nunca normalizar um valor que alimenta checagem null-vs-empty rio abaixo; grep por `?? []`/`|| []` logo antes de expor pro componente quando o transformer usa `null` de propósito. Dívida: falta teste de HOOK (só telas mockavam `useTreatments()` inteiro). Smoke 055 (kill switch) -> [`anti-patterns/mobile_and_platform/AP-322.md`](./anti-patterns/mobile_and_platform/AP-322.md)
- **[AP-321]** Erro cru (rede/JS, em inglês) vaza pra UI quando o fallback offline "sem cache pra tentar" relança o próprio erro original (`throw originalErr`) — em avião isso é `TypeError: Network request failed`, nunca traduzido. `describeLoadFailure` (core) não distinguia fallback com motivo próprio (`new Error('Cache expirado...')`, PT) de rethrow puro do mesmo objeto. Fix: só confiar na mensagem do fallback quando `fallbackErr !== originalErr` (identidade, não heurística de string); senão cai sempre no `defaultMessage` PT. Achados no mesmo smoke: `StockScreen.tsx` ignorava o `error` real do hook com uma string hardcoded; "Cache expirado" é jargão técnico que a persona não entende (trocado por texto que descreve o efeito, não o mecanismo). App 100% brasileiro — zero mensagem em inglês é política, não sugestão. Smoke 055 (kill switch) -> [`anti-patterns/mobile_and_platform/AP-321.md`](./anti-patterns/mobile_and_platform/AP-321.md)
- **[AP-258]** Read-modify-write concorrente em AsyncStorage (fila JSON) sem serialização → clobber/perda. `flush()` lê `[A]`, aguarda `insertOne(A)`; `enqueue(B)` lê `[A]` e grava `[A,B]`; flush grava os remaining sobre `[A]` (`[]`) → **B perdido**. Testes seriais não pegam (só sob concorrência headless×foreground). `flushInFlight` (só bloqueia flush reentrante) NÃO cobre enqueue↔flush. Fix: mutex simples (lock encadeado por promise) em enqueue/flush(read→process→write)/peek/clear; enqueue concorrente espera e anexa ao estado drenado. Guard: store sem transação (AsyncStorage/arquivo/localStorage) com RMW + >1 produtor DEVE serializar; teste enfileirando DURANTE flush com insert deferred. Review #700 (042 Slice B) -> [`anti-patterns/mobile_and_platform/AP-258.md`](./anti-patterns/mobile_and_platform/AP-258.md)
- **[AP-257]** Supor que a iOS Live Activity transiciona/encerra sozinha sem app rodando JS → `displayState` recomputa só em re-render; `Text(timerInterval:)` anima dígitos mas NÃO re-renderiza; re-render em bg depende de UM único `staleDate`; iOS não roda JS em bg → sem update/end. Sintoma (bug prod v0.23.3): countdown congela no zero, `late` preso até swipe, registro não vira `done`. Premissa "degrada bem como na 039" era falsa (039 nunca entregou transição sem app vivo). Android ok (Notifee TimestampTrigger encadeado). Fix: servidor dirige ciclo via push APNs (update por boundary + end na resolução), token per-Activity (`Activity.pushTokenUpdates`) em `dose_instances.la_push_token`, idempotência `la_push_state`. Guard: superfície contínua iOS que transita/encerra com app fechado EXIGE driver server-side (push) ou nativo agendado — nunca só staleDate+foreground. Spec 041 fix-up, ADR-076 emendada -> [`anti-patterns/mobile_and_platform/AP-257.md`](./anti-patterns/mobile_and_platform/AP-257.md)
- **[AP-251]** Notificação Android com `smallIcon: 'ic_X'` cujo drawable não existe no build lança `IllegalArgumentException: no valid small icon` → `displayNotification` rejeita. Duplo-silencioso em managed Expo: (1) `android/` é efêmero → asset some a cada prebuild se nenhum config plugin o regenerar; (2) callsite em try/catch com `if(__DEV__)` engole o erro em prod. Feature inteira não aparece sem sinal. Small icon = máscara alpha → drawable BRANCO, SO tinge com `color` da notif (por-estado). Fix: regenerar via `withDangerousMod` android (padrão dos plugins iOS). Bug prod 039/F2 (superfície sumia no device, só simulador via asset manual). Família AP-250 -> [`anti-patterns/mobile_and_platform/AP-251.md`](./anti-patterns/mobile_and_platform/AP-251.md)
- **[AP-250]** `ActivityAttributes` em dupla membership (widget = synchronized folder `targets/`, sempre fresco; app = cópia em `ios/DoseActivityRN/` via `withDangerousMod`, só atualiza no prebuild) → `expo run:ios` incremental deixa a cópia app-side STALE. App cria a Activity com o shape antigo, widget decodifica esperando o novo → Codable falha → **Live Activity some** (compila sem erro, mascara). Fix: clean prebuild (`rm -rf ios` / `expo prebuild --clean`) ao editar arquivo de dupla membership; `build-ios.sh` já limpa. Detectado smoke 039/F3 -> [`anti-patterns/mobile_and_platform/AP-250.md`](./anti-patterns/mobile_and_platform/AP-250.md)
- **[AP-249]** Ícone de branding em superfície nativa default p/ SF Symbol/genérico "semântico" (`drop.fill`=gota=medicação) em vez da MARCA dosiq do mock — recorrente no épico 039 (Android F2 + iOS F3 widget, mesma falha). Causa raiz: não abrir o mock canvas ANTES de escolher ícone; só descobre no smoke. O mock é fonte de verdade de branding. Fix: marca portada do SVG canônico (`apps/web/public/dosiq-logo-mono.svg`, viewBox 130 103 600 600) — RN via `DosiqLogo.jsx`; widget Swift via `DosiqMark` Shape (parser M/L/C/Z do mesmo path, sem react-native-svg). Regra: superfície com branding → conferir mock + reusar o path canônico, nunca SF Symbol genérico. Detectado smoke F3 -> [`anti-patterns/mobile_and_platform/AP-249.md`](./anti-patterns/mobile_and_platform/AP-249.md)
- **[AP-248]** iOS: `DateTimePicker` dentro de `<Modal>` aninhado tem gestos engolidos pelo Modal externo — `onChange` não dispara, `taken_at` salvo como `now` em vez do horário selecionado. Fix: overlay absoluto (`StyleSheet.absoluteFillObject`) no mesmo Modal, sem segundo `<Modal>`. Detectado smoke 039 prod (TestFlight) -> [`anti-patterns/mobile_and_platform/AP-248.md`](./anti-patterns/mobile_and_platform/AP-248.md)
- **[AP-246]** ActivityKit: encerrar Activities anteriores em `Task` separada e chamar `Activity.request` fora dela (síncrono) → request concorre com o `.end()`; quando a Task de cleanup roda, lista `Activity.activities` (já inclui a nova) e encerra a recém-criada → LA pisca e some. Fix: serializar — `await` dos ends + request na MESMA Task. Smoke não pega (1 disparo sem activity prévia = loop vazio). Aplicar no MVP F3 (T030-T034). Detectado spike 039 F0, review Gemini #689 -> [`anti-patterns/mobile_and_platform/AP-246.md`](./anti-patterns/mobile_and_platform/AP-246.md)
- **[AP-232]** Caret-fantasma iOS: `TextInput` perde caret/teclado ao (a) trocar a **subárvore** por estado (ternário `isPa ? rowA : rowB`) — desmonta o input focado, iOS fecha teclado/perde foco; e (b) `placeholder` divergente entre estados (`120`/`80` vs `0`) re-medir o campo vazio, deixando "focado sem cursor". Fix: campo principal = **mesmo nó React** em todos os tipos (PA só adiciona a diastólica como irmão), placeholder `0` uniforme, sem foco programático em campo vazio. Smoke 032 (PA 0.18.1) -> [`anti-patterns/mobile_and_platform/AP-232.md`](./anti-patterns/mobile_and_platform/AP-232.md)
- **[AP-213]** AsyncStorage é app-scoped (não per-user) — `signOut` não limpa; dados do usuário anterior (caches + flags de feature) vazam para o próximo login. `logoutUser` deve chamar `multiRemove` com todas as chaves user-scoped -> [`anti-patterns/mobile_and_platform/AP-213.md`](./anti-patterns/mobile_and_platform/AP-213.md)
- **[AP-215]** Hook com side effect destrutivo (`cancelAll`) executa no mount com estado inicial `[]` → `hasCriticalProtocol=false` → cancela alarmes antes de `load()` completar. Gate via sub-componente condicional + `loaded` state (R-266) -> [`anti-patterns/mobile_and_platform/AP-215.md`](./anti-patterns/mobile_and_platform/AP-215.md)
- **[AP-205]** Import pesado (supabase client / firebase) avaliado no top-level do `index.js` (pré-AppRegistry) crasha o cold start no Android (API 30). Entrypoint deve ter grafo de import mínimo; lazy-`require` a cadeia pesada dentro do callback do handler (notifee.onBackgroundEvent) ou mover p/ a árvore React -> [`anti-patterns/mobile_and_platform/AP-205.md`](./anti-patterns/mobile_and_platform/AP-205.md)
- **[AP-206]** Gate/supressão de push server-side NÃO é smoke-testável on-device pré-deploy — o push nasce da prod (`api/notify` em `main`); a dedup só vale pós-merge+deploy. Flag no DB + sync mobile podem estar certos e ainda dobrar. Validar por unit test; não confundir com bug -> [`anti-patterns/infra_and_deploy/AP-206.md`](./anti-patterns/infra_and_deploy/AP-206.md)
- **[AP-207]** `addNotificationResponseReceivedListener` (expo) captura o press da notif do Notifee no Android (sistema compartilhado) → `navigateFromPush(undefined)` força `navigate('Hoje')` no root (rota aninhada em TABS) → crash "not handled by any navigator". Guard: alarme tem `doseInstanceId` → não navegar; passar o `data` completo -> [`anti-patterns/mobile_and_platform/AP-207.md`](./anti-patterns/mobile_and_platform/AP-207.md)
- **[AP-208]** Stale device rows acumulam no version bump (`device_fingerprint` inclui `appVersion` → novo row por versão); rows antigos ativos confundem diagnóstico de duplicata até Expo desativar via DeviceNotRegistered. Filtrar por `last_seen_at`/`app_version`, não contar linhas -> [`anti-patterns/notifications/AP-208.md`](./anti-patterns/notifications/AP-208.md)
- **[AP-209]** Adicionar param a RPC = OVERLOAD (não substitui); 7-arg + 8-arg-DEFAULT = "function is not unique" → DROP a assinatura antiga antes de recriar. Schema cache PostgREST: app novo + função antiga = "Could not find function" → aplicar migration ANTES de soltar o app -> [`anti-patterns/data_and_schema/AP-209.md`](./anti-patterns/data_and_schema/AP-209.md)
- **[AP-202]** View day-scoped (timeline do Hoje) reusa fetch de janela larga (`getDoseInstancesForPeriod(14)`, p/ tendência de adesão) sem filtrar ao dia local → renderiza ocorrências de dias anteriores (5x as doses). Filtrar `scheduled_for ∈ [startOfDay, endOfDay]` (tz) ANTES de montar a lista do dia -> [`anti-patterns/mobile_and_platform/AP-202.md`](./anti-patterns/mobile_and_platform/AP-202.md)
- **[AP-180]** FlatList `flexGrow:0` em bottom-sheet `maxHeight` → lista longa transborda sem scroll (último item inalcançável iOS); fix `flexShrink:1` na lista + container -> [`anti-patterns/mobile_and_platform/AP-180.md`](./anti-patterns/mobile_and_platform/AP-180.md)
- **[AP-173]** `signOut()` escopo global (default) trava o logout (iOS sim / Workbox PWA) — usar `signOut({ scope: 'local' })` + cleanup local -> [`anti-patterns/mobile_and_platform/AP-173.md`](./anti-patterns/mobile_and_platform/AP-173.md)
- **[AP-174]** Bottom sheet/Modal RN + teclado + tab bar (Android API 24) corta campos/botões; KAV não mede em Modal. Fluxo destrutivo com input → tela cheia -> [`anti-patterns/mobile_and_platform/AP-174.md`](./anti-patterns/mobile_and_platform/AP-174.md)
- **[AP-175]** Autocomplete RN: `setFocused(false)` sem blur nativo (keyboardShouldPersistTaps) → 2ª busca morta (effect aborta em `!focused`). `inputRef.blur()` no select -> [`anti-patterns/mobile_and_platform/AP-175.md`](./anti-patterns/mobile_and_platform/AP-175.md)
- **[AP-176]** Dois `NavigationContainer` com a mesma ref → "navigation hasn't been initialized". Container único + filho condicional -> [`anti-patterns/mobile_and_platform/AP-176.md`](./anti-patterns/mobile_and_platform/AP-176.md)
- **[AP-177]** VirtualizedList (FlatList) aninhada em ScrollView mesma orientação → warning/perf. Overlays curtos: ScrollView plano + `.map()` -> [`anti-patterns/mobile_and_platform/AP-177.md`](./anti-patterns/mobile_and_platform/AP-177.md)
- **[AP-178]** `useFocusEffect` NÃO dispara ao voltar do background (foreground do app, ex: retorno das Configs do SO) — só em foco de navegação. Re-checagem de permissão exige TAMBÉM `AppState.addEventListener('change')` filtrando `'active'` -> [`anti-patterns/mobile_and_platform/AP-178.md`](./anti-patterns/mobile_and_platform/AP-178.md)
- **[AP-171]** `Intl.NumberFormat` (moeda) não confiável no Hermes — renderiza errado em device sem erro; usar formatação manual (sinal antes do símbolo) -> [`anti-patterns/mobile_and_platform/AP-171.md`](./anti-patterns/mobile_and_platform/AP-171.md)
- **[AP-A03]** When 2+ protocols exist for same medicine, logs bleed between them. Protocol A's... -> [`anti-patterns/mobile_and_platform/AP-A03.md`](./anti-patterns/mobile_and_platform/AP-A03.md)
- **[AP-B01]** Vite 7 base64-encoda o conteúdo raw do JSX e emite `data:text/jsx;base64,...` no... -> [`anti-patterns/mobile_and_platform/AP-B01.md`](./anti-patterns/mobile_and_platform/AP-B01.md)
- **[AP-B03]** Cadeia transitiva puxa chunks inteiros para o main bundle. Ex: `import ReportGen... -> [`anti-patterns/mobile_and_platform/AP-B03.md`](./anti-patterns/mobile_and_platform/AP-B03.md)
- **[AP-B04]** `@shared/services/index.js` exporta `stockService`, `adherenceService`, etc. Qua... -> [`anti-patterns/mobile_and_platform/AP-B04.md`](./anti-patterns/mobile_and_platform/AP-B04.md)
- **[AP-D05]** Slice no prop de doses do PriorityDoseCard -> [`anti-patterns/mobile_and_platform/AP-D05.md`](./anti-patterns/mobile_and_platform/AP-D05.md)
- **[AP-H01]** Usar quantity_taken como valor do dosage pill -> [`anti-patterns/mobile_and_platform/AP-H01.md`](./anti-patterns/mobile_and_platform/AP-H01.md)
- **[AP-H07]** ~~Watchman 'Operation not permitted' em repo iCloud~~ **[RESOLVIDO 2026-05-24 — projeto fora do iCloud]** -> [`anti-patterns/mobile_and_platform/AP-H07.md`](./anti-patterns/mobile_and_platform/AP-H07.md)
- **[AP-H08]** Todas as versões (v2, v3) causam 'Cannot read property get of undefined' no Herm... -> [`anti-patterns/mobile_and_platform/AP-H08.md`](./anti-patterns/mobile_and_platform/AP-H08.md)
- **[AP-H09]** registerRootComponent detecta transform.routerRoot=app no URL do bundle (Expo Go... -> [`anti-patterns/mobile_and_platform/AP-H09.md`](./anti-patterns/mobile_and_platform/AP-H09.md)
- **[AP-H10]** SecureStore chunked é condição necessária mas não suficiente. Navigation.jsx DEV... -> [`anti-patterns/mobile_and_platform/AP-H10.md`](./anti-patterns/mobile_and_platform/AP-H10.md)
- **[AP-H11]** Hermes não lança 'not implemented' em testes sintéticos — só lança em uso real p... -> [`anti-patterns/mobile_and_platform/AP-H11.md`](./anti-patterns/mobile_and_platform/AP-H11.md)
- **[AP-H12]** url.searchParams cria um HermesURLSearchParams desvinculado a cada acesso. Mutaç... -> [`anti-patterns/mobile_and_platform/AP-H12.md`](./anti-patterns/mobile_and_platform/AP-H12.md)
- **[AP-H13]** No Hermes, o getter nativo href chama toString() internamente. Substituir toStri... -> [`anti-patterns/mobile_and_platform/AP-H13.md`](./anti-patterns/mobile_and_platform/AP-H13.md)
- **[AP-H14]** getTodayLogs com boundaries '2026-04-13T00:00:00' (sem timezone) é tratado como ... -> [`anti-patterns/mobile_and_platform/AP-H14.md`](./anti-patterns/mobile_and_platform/AP-H14.md)
- **[AP-H16]** Mobile code had pt-EU Portuguese (registar/registados) instead of pt-BR (tomar/t... -> [`anti-patterns/mobile_and_platform/AP-H16.md`](./anti-patterns/mobile_and_platform/AP-H16.md)
- **[AP-H17]** whatwg-url-without-unicode uses Symbol.for('[webidl2js] constructor registry') w... -> [`anti-patterns/mobile_and_platform/AP-H17.md`](./anti-patterns/mobile_and_platform/AP-H17.md)
- **[AP-P01]** `rootMargin: '200px'` + sentinel mid-JSX = observer fires immediately on view op... -> [`anti-patterns/mobile_and_platform/AP-P01.md`](./anti-patterns/mobile_and_platform/AP-P01.md)
- **[AP-P03]** `analyzeAdherencePatterns` + Zod validation on 500 objects in useMemo = Main Thr... -> [`anti-patterns/mobile_and_platform/AP-P03.md`](./anti-patterns/mobile_and_platform/AP-P03.md)
- **[AP-P12]** `getAdherenceSummary` chamava 3 sub-funções que cada uma buscava `protocols` ind... -> [`anti-patterns/mobile_and_platform/AP-P12.md`](./anti-patterns/mobile_and_platform/AP-P12.md)
- **[AP-P13]** `setIsLoading(false)` permite ao React agendar um render, mas queries disparadas... -> [`anti-patterns/mobile_and_platform/AP-P13.md`](./anti-patterns/mobile_and_platform/AP-P13.md)
- **[AP-P14]** 13 HTTP roundtrips no primeiro load do Dashboard (~8s em 4G). Cada service que c... -> [`anti-patterns/mobile_and_platform/AP-P14.md`](./anti-patterns/mobile_and_platform/AP-P14.md)
- **[AP-P18]** Title/patient overlap, clipped headers, and layout churn every time content leng... -> [`anti-patterns/mobile_and_platform/AP-P18.md`](./anti-patterns/mobile_and_platform/AP-P18.md)
- **[AP-P20]** The consultation PDF loses clinical usefulness and makes it harder to distinguis... -> [`anti-patterns/mobile_and_platform/AP-P20.md`](./anti-patterns/mobile_and_platform/AP-P20.md)
- **[AP-P21]** A protocol with multiple time slots per day counts the same day several times, i... -> [`anti-patterns/mobile_and_platform/AP-P21.md`](./anti-patterns/mobile_and_platform/AP-P21.md)
- **[AP-H26]** Falha de Regex do plugin Firebase v21+ no AppDelegate.swift do Expo 53 -> [`anti-patterns/mobile_and_platform/AP-H26.md`](./anti-patterns/mobile_and_platform/AP-H26.md)
- **[AP-W28]** Cálculo de meia-noite frágil usando .setHours(24) em vez de .setDate(+1). -> [`anti-patterns/mobile_and_platform/AP-W28.md`](./anti-patterns/mobile_and_platform/AP-W28.md)
- **[AP-110]** The Stale Cache Ghost (Obsolete offline data display) -> [`anti-patterns/mobile_and_platform/AP-110.md`](./anti-patterns/mobile_and_platform/AP-110.md)
- **[AP-112]** 1 push por protocolo — envia N notificações simultâneas para o mesmo usuário em vez de 1 bloco semântico agrupado -> [`anti-patterns/mobile_and_platform/AP-112.md`](./anti-patterns/mobile_and_platform/AP-112.md)
- **[AP-113]** `new Date().getHours()` como fallback em código de notificações — usa relógio do servidor (UTC) em vez do timezone do usuário, gerando emojis/saudações incorretos -> [`anti-patterns/mobile_and_platform/AP-113.md`](./anti-patterns/mobile_and_platform/AP-113.md)
- **[AP-155]** `Intl.DateTimeFormat` unsafe in module scope on legacy Hermes (Android ≤ 7) -> [`anti-patterns/mobile_and_platform/AP-155.md`](./anti-patterns/mobile_and_platform/AP-155.md)
- **[AP-156]** `schema.pick({[field]:true})` em validação isolada descarta refinements de objeto (.refine/.superRefine) -> [`anti-patterns/mobile_and_platform/AP-156.md`](./anti-patterns/mobile_and_platform/AP-156.md)
- **[AP-157]** Normalizar dataset grande (NFD) em cada keystroke vs pré-normalizar no boot via useMemo -> [`anti-patterns/mobile_and_platform/AP-157.md`](./anti-patterns/mobile_and_platform/AP-157.md)
- **[AP-158]** Callbacks em `route.params` quebram React Navigation + criam stale closures -> [`anti-patterns/mobile_and_platform/AP-158.md`](./anti-patterns/mobile_and_platform/AP-158.md)
- **[AP-162]** Import de `@expo/vector-icons` carrega ~4 MB de fonts cumulativos — preferir `lucide-react-native` ou import específico por família -> [`anti-patterns/mobile_and_platform/AP-162.md`](./anti-patterns/mobile_and_platform/AP-162.md)
- **[AP-163]** `borderStyle: 'dashed'/'dotted'` em RN dispara `WARN Unsupported dashed / dotted border style` (RN só aceita `'solid'`) — multiplica por frame em containers animados via `LayoutAnimation` -> [`anti-patterns/mobile_and_platform/AP-163.md`](./anti-patterns/mobile_and_platform/AP-163.md)
- **[AP-165]** Modal RN sem `statusBarTranslucent` no Android < 8: overlay não cobre topo + inputs do form parent vazam por cima + bottom tab bar corta botões do sheet -> [`anti-patterns/mobile_and_platform/AP-165.md`](./anti-patterns/mobile_and_platform/AP-165.md)
- **[AP-166]** `useFormState.validate()` lê `values` da frame anterior — race com `handleChange` assíncrono quando coerce + validate na mesma frame. Fix: aceitar `overrides` opcional -> [`anti-patterns/react_and_ui/AP-166.md`](./anti-patterns/react_and_ui/AP-166.md)
- **[AP-167]** Eager parse string→number em input decimal apaga vírgula (estado intermediário `"0,"` vira `0`); user não completa decimal -> [`anti-patterns/react_and_ui/AP-167.md`](./anti-patterns/react_and_ui/AP-167.md)
- **[AP-159]** Delete de entidade pai sem pre-check de dependências FK deixa órfãos no banco (hard block via XDeleteBlockedSheet) -> [`anti-patterns/data_and_schema/AP-159.md`](./anti-patterns/data_and_schema/AP-159.md)

## 🛡️ Process & Testing (`process_and_testing`)
- **[AP-297]** 🔴 Slice Tier 1+ vai a PR SEM rodar RC5 (self-review pré-PR) → o revisor externo (Gemini) vira o **1º** revisor e devolve volume alto/recorrente de findings **mecânicos** (8-11/PR): `afterEach` ausente num teste Vitest **apesar de o CLAUDE.md exigir**, JSDoc que promete "retorna null em X" mas o código segue, guard SQL faltando, ergonomia de query. Custo some porque a ORDEM de revisão está invertida — o RC5 Pass 1 + fix-first pega essa classe **de graça, com contexto quente**, antes do cold-start do revisor externo. Assinatura recorrente "pego só por revisor externo" em [[AP-266]]/[[AP-276]]/[[AP-285]]. Caso: 029 F2 #747 (10 comentários, maioria AUTO-FIX). Guard: rodar `/devflow code-review` antes de `gh pr create` em Tier 1+; resolver AUTO-FIX antes do PR; checklist da casa (teste novo→`afterEach` clearAllMocks/Timers; JSDoc↔código; migração→tabela failure-modes R-270). Farol: PR volta com >2 findings que uma regra existente já cobre = RC5 pulado, não revisor zeloso -> [`anti-patterns/process_and_testing/AP-297.md`](./anti-patterns/process_and_testing/AP-297.md)
- **[AP-298]** 🔴 Filtro por coluna **INEXISTENTE** + `error` descartado (`const { data } = await`) = varredura **morta em silêncio**. O cron de titulação filtrava `.eq('status','ativo')` — `protocols` tem `active` (boolean), não `status` → PostgREST devolve `42703`, o erro morre na destruturação, `data` vem `undefined`, `if (!data) continue` → **o auto-avanço nunca rodou em prod desde o PR #526 (2026-05-06)**. Invisível porque o `expected_dose` vem do gerador PURO (caminha a escada em memória): a DOSE saía certa, só a linha não avançava e o push não saía. A suíte testava a função pura com o client mockado (`{}`) — verde sobre caminho morto (família [[AP-279]]). Silêncio ≠ vazio. Ver [[R-270]], [[AP-215]] -> [`anti-patterns/data_and_schema/AP-298.md`](./anti-patterns/data_and_schema/AP-298.md)
- **[AP-301]** 🔴🔴 **Status diz "ativo", mas o RELÓGIO que faz a feature andar só é setado num dos caminhos de escrita → zumbi silencioso.** A titulação N1 do dosiq **nunca funcionou em produção, em 6 meses**: `create()` com escada setava `stage_started_at`; **`update()` adicionando escada depois — o fluxo REAL do usuário — gravava `titration_status='titulando'` e deixava o relógio NULL**; e `advanceTitrationStage()` (que setava) era **código morto, zero chamadores**. O motor ignora etapa sem t₀ (corretamente) → status diz titulando, nada anda, **sem erro/log/alerta**. Invisível porque a dose EXIBIDA vinha do gerador puro (não depende do relógio na etapa 0) e porque **nenhum CHECK** proibia `status='titulando' AND stage_started_at IS NULL`. Empilhado com [[AP-298]]: dois bugs independentes, cada um suficiente pra explicar o silêncio. Regra: status-que-declara-ativo + campo-que-faz-andar são **UM fato** — CHECK que torne a metade irrepresentável (`CHECK (status<>'ativo' OR relogio IS NOT NULL)`); TODO caminho de escrita que liga a feature seta o relógio (inventariar create/update/RPC/migração/seed); testar a ATIVAÇÃO pelo caminho do usuário real ("adicionei depois"), não pelo conveniente ("criei já com"); função de domínio sem chamador é dívida ativa. Farol: `WHERE status='ativo' AND relogio IS NULL` > 0 em prod (aqui: **100% das linhas**). Custo de 2ª ordem: a 029 nasceu como MIGRAÇÃO e o F2 provou paridade N1↔N2 (52 testes) **com um cadáver** — antes de espelhar comportamento, provar que ele ACONTECE em prod, não que o código existe. Ver [[R-271]] · [[R-295]] -> [`anti-patterns/data_and_schema/AP-301.md`](./anti-patterns/data_and_schema/AP-301.md)
- **[AP-300]** 🔴🔴 **Tipo escrito à mão declara campo que a tabela NÃO tem → o `select()` sai do tipo e quebra em runtime.** `TitrationStepLike` (à mão, F2) declarava `description?: string|null`; a coluna nunca existiu (`description` é do N1/jsonb, e a spec decidiu que "nota por etapa não migra" — o F1 corretamente não criou). O F2 declarou no tipo pra compilar `stageNote: currentStep.description`: em runtime lia `undefined`, **inofensivo e invisível por um slice inteiro**. O F3 escreveu 5 selects **a partir do tipo** → `42703` em todo o read-path de `protocols` (web+mobile+cron; sem protocols = **sem dose, sem estoque**). **O F2 plantou, o F3 pisou.** O que assusta: **NENHUM gate pegou e a lista inteira passou** — tsc (o select é *string*), 2068 testes + jest 434 (o client está **mockado** — [[AP-279]]), lint, SQL live 21/21 (testava a *RPC*, não os selects), RC5 e review externo (revisaram lógica, não confrontaram a string com o schema). Um select do PostgREST é a única coisa do projeto simultaneamente **crítica + invisível ao compilador + invisível aos testes**: não existia gate. Antídoto barato: fixture de contrato que dispara cada select de produção contra o banco real (~10 linhas/select — pegaria este E o [[AP-298]]). Farol: campo `?:` em tipo à mão que nunca aparece preenchido em teste nem em dado real = campo **inexistente** esperando um select. Ver [[R-295]] -> [`anti-patterns/data_and_schema/AP-300.md`](./anti-patterns/data_and_schema/AP-300.md)
- **[AP-304]** 🔴 **`schema.partial()` torna campos opcionais mas NÃO remove os `.default()` → update parcial injeta defaults e o repo os grava, zerando dados que o usuário não tocou.** `protocolUpdateSchema = protocolSchema.partial()`; parsear `{ active: false }` (pausar tratamento) devolve TAMBÉM `weekdays: []`, `titration_status: 'estável'`, `titration_schedule: []`, `current_stage_index: 0`, `critical_alarm: false` (todo campo com `.default()`). O `update()` gravava `validation.data` cru → **`weekdays` do tratamento semanal virava `[]`** (dia da semana some e não volta ao reativar) + reescrevia colunas N1 deprecadas. Invisível porque o teste do repo usava `toMatchObject` (subset — não vê chaves EXTRA no payload) e o toggle manda só `{active}` (ninguém suspeita que zera outro campo). Confirmado no banco (Supabase MCP): 1 linha com `weekdays: []`. Fix: o `update` persiste **só as chaves que o chamador enviou** (interseção `Object.keys(updates)` × `validation.data`) — validação de tipos permanece, defaults injetados não vazam pro banco. Farol: `Zod .partial()` + `.default()` no mesmo schema usado para UPDATE parcial; teste de payload com `toMatchObject` em vez de `toEqual` (não pega chave a mais); update que manda 1 campo e o banco perde outro. Bug anterior à 029, write-path compartilhado web+mobile. Ver [[R-295]] -> [`anti-patterns/data_and_schema/AP-304.md`](./anti-patterns/data_and_schema/AP-304.md)
- **[AP-308]** 🔴 **RPC escreve na tabela que o repositório protege → o hook de sincronização não roda e a feature nasce pela metade.** `confirm_titration_switch` (SQL) é a ÚNICA escrita em `protocols` fora do `createProtocolRepository`, logo a única que escapa do `syncInstancesOnWrite` (wipe + regen de 30d de `dose_instances`). `medicine_switch`: o protocolo CRIADO nasce com **zero** instâncias → calendário vazio até o cron das ~03:00 (numa dose SEMANAL, quase 24h). `dose_change`: a RPC atualiza `dosage_per_intake` mas as futuras já materializadas mantêm a `expected_dose` do cronograma ANTERIOR → o tratamento diz uma dose e o lembrete entrega outra (**risco clínico** — pior que a ausência). Invisível porque (1) o cron faz convergir sozinho, então a janela é de horas; (2) o teste da RPC valida SQL ✅ e o do gerador valida JS ✅ — o defeito está no VÃO, nenhum lado errado isoladamente; (3) a rede lazy do app só cobre `LOOK_AHEAD_DAYS=3` (72h, cota de alarme Android), então **só frequência semanal expõe** o que a diária esconde. Regra: tabela cujo repositório mantém DADO DERIVADO tem invariante que vive no hook, não no schema — antes de escrever nela por RPC/trigger/migração/job, inventariar o que o repo faz ALÉM do INSERT e replicar (ou anotar a divergência). Farol: dado derivado que "aparece depois" (no próximo cron/refresh) — o "depois" é o sintoma, não o design. Fix: `resyncProtocolWindow` no core chamado atrás da RPC (3 chamadores herdam), best-effort (R-245). Espelho de [[AP-300]] (leitura) e [[AP-305]] (saída). Origem: 029 F5.5, smoke do PO. 🔁 **REINCIDÊNCIA (052, PR #761): eram QUATRO caminhos, não um** — o texto "a ÚNICA escrita" era impressão, não inventário. Vivos também: `_applyTitrationPlan` (**cron**, SQL cru mutando `dosage_per_intake` — o avanço NORMAL da escada, o pior deles), `saveLadderEdit` e `createFullLadder` (este achado só pelo RC6). Lições novas: (a) tabela que alimenta o dado derivado **por embed** (`titration_steps`) é caminho de escrita da tabela derivada — foi por aí que escapou; (b) `ON CONFLICT DO NOTHING` converge estado AUSENTE, **nunca conserta linha ERRADA** já materializada — quando ouvir "o cron conserta depois", perguntar *conserta ausência ou erro?*; (c) **congelar dado derivado transforma bug transitório em permanente**, então a spec que congela fecha TODOS os caminhos de invalidação no MESMO PR -> [`anti-patterns/data_and_schema/AP-308.md`](./anti-patterns/data_and_schema/AP-308.md)
- **[AP-309]** Finding do RC6 sobre linha **FORA do diff** não vira thread inline → o achado mais raro é o que mais some. PR #761: RC6 devolveu **5 findings**, script publicou **3 inline** ("3 inline, 0 orphan"); os 2 descartados incluíam o MELHOR da revisão — `createFullLadder`, 4º caminho do [[AP-308]] em código **não tocado**, que a própria PR agravava. Perverso porque o `AI_REVIEW.md` manda anexar arquivo COMPLETO justamente para o revisor ver além do hunk ("diff-only é o blind spot #1") — e o canal de saída descarta em silêncio o que só ele podia ver. `0 orphan` **soa** como cobertura total e não é contagem de descartados. Regra: (1) nunca julgar o RC6 pelas threads — conferir `len(findings)` do JSON contra os comentários publicados; (2) finding sem âncora entra na resposta geral do PR com veredicto e evidência; (3) `introduced:false` ≠ "não é deste PR" — perguntar *este PR muda a SEVERIDADE do achado?* (congelar dado, ampliar alcance, remover acidente protetor ⇒ virou escopo). Farol: revisão que só produz findings mecânicos em PR grande = suspeita de truncamento/descarte, mesma desconfiança de "zero findings em diff gordo" -> [`anti-patterns/process_and_testing/AP-309.md`](./anti-patterns/process_and_testing/AP-309.md)
- **[AP-316]** **Gate mecânico (tsc/lint/jest/expo-doctor) não cobre `xcodebuild` real — bump de SDK nativo passa "verde" com build iOS quebrado.** PR 1.1 da spec 055 (Expo 53→54) fechou todos os gates prescritos (expo-doctor 18/18, tsc 0, lint 0, jest 61/61) e RC5/RC6 sem findings — nenhum deles compila código nativo. Só `npx expo run:ios` real (rodado pelo PO, fora do gate) revelou `@react-native-firebase` v21 quebrando sob Xcode 26/RN 0.81 em 2 classes: include não-modular (mitigável via `CLANG_ALLOW_NON_MODULAR_INCLUDES_IN_FRAMEWORK_MODULES` nos Pods, config plugin novo `withFirebasePodfileFix.js`) e macro `RCT_EXPORT_METHOD` do `analytics` como `-Wimplicit-int` fatal (não mitigável sem suprimir warning real — pacote precisa de bump). Android não afetado, o que escondeu o gap. Regra: bump de SDK nativo exige `npx expo run:ios` **e** `run:android` reais como gate antes do RC5 — as duas plataformas podem divergir, nunca extrapolar de uma pra outra. Ver [[AP-251]] (mesma família, Android) -> [`anti-patterns/tooling_and_build/AP-316.md`](./anti-patterns/tooling_and_build/AP-316.md)
- **[AP-312]** **Cast que PARECE desleixo mas é contorno de resolução de tipos — "limpar" quebra o gate cross-program.** `import * as X` + `X as any` grita dívida, e trocar por import nomeado falha com `TS2305: has no exported member` em símbolos que **obviamente existem** (`ChevronRight`). Causa não é o símbolo: é **peer não hoisted** — `lucide-react-native` na raiz, `react-native-svg` só em `apps/mobile/node_modules`; o `.d.ts` do lucide faz `import { SvgProps } from 'react-native-svg'`, resolve a partir da raiz, não acha, e o módulo INTEIRO degrada p/ "sem exports". Engana porque (1) a mensagem aponta o símbolo, não o peer — nome válido e nome inventado dão erro idêntico; (2) `lint`/`jest`/`tsc` do programa passam, só o **cross-program** (`strict-island.sh`) acusa; (3) é indistinguível de dívida real olhando a linha. Regra: cast sem justificativa = dívida, **cast com causa raiz documentada = decisão**; antes de remover cast em import de lib, rodar o gate cross-program; revisor que manda trocar por import nomeado precisa provar que o módulo RESOLVE. Origem: RC6 do #763 marcou `CircleQuestionMark` como inexistente (é o nome ATUAL — `CircleHelp`/`HelpCircle` é que não existem; o fix proposto causaria o crash descrito). Ver [[AP-254]] · [[AP-300]] · R-295 -> [`anti-patterns/tooling_and_build/AP-312.md`](./anti-patterns/tooling_and_build/AP-312.md)
- **[AP-311]** 🔴 **Vínculo entre entidades guardado numa linha FILHA opcional → apagar a linha 'sorteada' orfana o conjunto inteiro, e a leitura mostra VAZIO em vez de erro.** `titration_steps.protocol_id` é o único fio escada↔tratamento e fica NULL na maioria das etapas (medido: 6/15, e 1 das 4 vigentes); `getLadderForProtocol` busca QUALQUER etapa com aquele `protocol_id` p/ descobrir a `titration_id` e, não achando, devolve `[]` — **indistinguível de 'não há escada'**: sem erro, sem log, nenhum gate acusa. Smoke do PO 2026-07-20: apagou etapas futuras, entre elas a ÚNICA com vínculo → escada íntegra no banco, invisível no app; para o usuário, 'apaguei 2 etapas' virou 'perdi o tratamento'. Regra: (1) o vínculo mora no nível da RELAÇÃO (escada ou tratamento), nunca em linha filha apagável; (2) vínculo derivado de regra de negócio (run same-med) é instável por natureza; (3) leitura DEVE distinguir 'não existe' de 'não achei'; (4) antes do fix estrutural, **medir blast radius** — classe que morre por construção em entrega já decidida vira **Proof Obligation** dela, não vínculo de transição a deletar depois. Decidido: sem fix próprio (1 usuário atingido = o PO) → **PO-9 + T029b** na 052 Slice C, com repro literal; ⚠️ reavaliar se o Slice C escorregar OU a titulação chegar a usuário real antes dele. Conserto operacional + query de diagnóstico no detail. Ver [[AP-254]] · [[R-299]] -> [`anti-patterns/data_and_schema/AP-311.md`](./anti-patterns/data_and_schema/AP-311.md)
- **[AP-313]** 🔴 **Navegação que TAMBÉM invalidava cache: remover o desvio leva junto um efeito que ninguém escreveu.** `navigation.replace` trocava o `protocolId` → remontava a timeline → recarregava a escada. Ninguém tinha escrito "recarregue depois de confirmar" — acontecia de carona. O Slice C removeu a navegação (corretamente: a armadilha sumiu) e o efeito não-escrito foi junto: banco certo, banner vivo na tela. Gate nenhum pega (compila, lint limpo, RPC provada). Antes de remover navegação/remount/troca de `key`, perguntar **"o que MAIS isto fazia?"**, não só "ainda é necessário?". Invalidação é de quem detém o estado, nunca da rota. Farol: bug que **"se conserta quando saio e volto"** = refresh-on-focus mascarando ausência de invalidação na ação -> [`anti-patterns/react_and_ui/AP-313.md`](./anti-patterns/react_and_ui/AP-313.md)
- **[AP-310]** Enriquecedor/mapper compartilhado que **passa a emitir um campo já escrito explicitamente no objeto** inverte a precedência pelo spread — em silêncio. `{medicineId: log?.x ?? null, ...enrich(inst)}` funciona enquanto `enrich` não devolve `medicineId`; no dia em que devolve (é o que migrar fonte de verdade FAZ), o spread vira o dono da regra. Nada quebra: mesmo tipo, campo presente e populado — só o VALOR errado. `tsc` não vê (shape idêntico), lint não vê (sintaxe legítima), teste só vê com fixture em que as fontes **divergem** (a típica usa as duas concordando e passa dos dois jeitos). Regra: ao adicionar campo à saída de um mapper, auditar todo call site que faz spread dele; ordem defensiva = **spread primeiro, campo com precedência depois**, com comentário do POR QUÊ (senão o próximo 'organiza' o objeto e reintroduz). Origem: 052 Slice B (#762) — `makeInstanceEnricher` ganhou `medicineId` e anulou a precedência do log ancorado (o que foi REALMENTE tomado) sobre o congelado; achado no **RC5 do próprio autor**. Família do [[AP-300]] (defeito atravessa a bateria porque nenhuma camada verifica VALOR, só forma), em JS puro -> [`anti-patterns/data_and_schema/AP-310.md`](./anti-patterns/data_and_schema/AP-310.md)
- **[AP-305]** Campo de contrato que representa CAPACIDADE (ação/botão/deeplink/anexo) existe, valida e é preenchido — mas **um canal não o consome** → feature "pronta" que não faz nada no aparelho. `payload.actions` vive no schema desde o "Gate 4", só o **Telegram** o lê; o `expoPushChannel` nunca emitiu `categoryId` nem ações. Seguir o escopo herdado ao pé da letra ("estender o actionSchema + o builder") daria schema válido + builder correto + testes verdes e **push SEM botão**, visível só no smoke em device. Espelho do [[AP-300]] no lado da SAÍDA: tsc valida a forma, o teste do builder afirma o preenchimento, e nada pergunta *quem consome*. Regra: `grep` o campo nos `channels/`+handlers (zero hits fora do schema = decorativo); push exige SEMPRE o par `categoryId` (servidor) + registro da categoria (app); o teste do CANAL asserta o campo na mensagem final; capacidade parcial de propósito anota no schema QUAL canal ainda não consome — senão o próximo slice a lê como pronta (foi F3→F5) -> [`anti-patterns/mobile_and_platform/AP-305.md`](./anti-patterns/mobile_and_platform/AP-305.md)
- **[AP-299]** Entidade NOVA materializa explicitamente o valor que a tabela ANTIGA representa por **NULL** → escrever de um lado no outro viola CHECK. `titration_steps.intake_unit` aceita `'cp'`; `protocols.intake_unit` NÃO (comprimido = NULL lá, 62/70 linhas em prod) → a RPC de transição abortaria com `23514` em **toda titulação de comprimido** (o caso canônico do N1). Só não explodiu porque a única escada de prod era 'mg'. Todo par (coluna nova → coluna antiga) é FRONTEIRA DE DOMÍNIO, não cópia: comparar os CHECKs dos dois lados e mapear (`NULLIF(unit,'cp')`). Sinal: o modelo novo nasce com enum que substitui convenção implícita do velho. Pego no preflight [[R-270]], antes de prod. Ver [[R-271]] -> [`anti-patterns/data_and_schema/AP-299.md`](./anti-patterns/data_and_schema/AP-299.md)
- **[AP-302]** Caminho de produção REAL não tem nenhum reprocessamento automático; um módulo com o nome certo (`getPendingForRetry`/`markForRetry` em `deadLetterQueue.ts`) é código morto (zero chamadores) e só o retry MANUAL via admin existe de fato. Achado por evidência de banco (Supabase MCP em `failed_notification_queue`) após 503 real do Expo, não por suposição de onde o mecanismo "deveria" estar. Fix: job `dlq_auto_retry` no cron já existente (zero função serverless nova, R-090). Farol: função de retry sem caller no caminho real de produção = nome que promete e não cumpre; grep por quem chama antes de assumir que "existe retry". Irmão de [[AP-290]] (gate que não cobre a árvore que promete proteger). PR #748 -> [`anti-patterns/notifications/AP-302.md`](./anti-patterns/notifications/AP-302.md)
- **[AP-224]** NUNCA `git stash`/checkout/reset com sub-agente ativo escrevendo no mesmo working tree — stash captura trabalho parcial do agente ("some" sob ele), agente re-faz divergindo, pop conflita. Baseline-check → worktree isolado, `git diff main` (read-only) ou antes do spawn. Recuperação: aguardar agente → restore seletivo do stash mantendo a versão final do agente nos sobrepostos (incidente 012 Fase A) -> [`anti-patterns/process_and_testing/AP-224.md`](./anti-patterns/process_and_testing/AP-224.md)
- **[AP-234]** Commit de docs direto na main (specs/`.agent`) **enquanto uma feature-branch está aberta** → squash-merge do GitHub aplica a árvore da branch sobre a main e **apaga** o doc (a branch nasceu antes dele). Falha silenciosa, sem conflito; pior em troca de contexto/multi-agente. Incidente real: draft 035 sumiu no squash do #671. Fix: **rebase a branch na main logo após o commit de docs** (`git checkout <branch> && git rebase main`), OU adiar o doc até o merge, OU `git ls-tree -r <merge-commit> | grep <path>` no C5 (rede de segurança). Recuperação: `git checkout <commit> -- <path>` + re-commit -> [`anti-patterns/process_and_testing/AP-234.md`](./anti-patterns/process_and_testing/AP-234.md)
- **[AP-235]** Alarme local (Notifee) não cancelado ao resolver a dose por outra superfície: `registerDose`/`registerDoseMany` não chamavam `cancelAlarm` → trigger de soneca/nag órfão reabria a tela cheia numa dose já `taken` (dupla contagem / `taken→skipped_user`); `AlarmFullScreen` também não revalidava status. Fix: cancel-on-resolve best-effort por instância em registerDose/Many + auto-dismiss da tela cheia (revalida `dose_instances.status`, fecha quando nenhuma `pending`; grupo só quando todas) -> [`anti-patterns/mobile_and_platform/AP-235.md`](./anti-patterns/mobile_and_platform/AP-235.md)
- **[AP-204]** Mock de hook que retorna função nova a cada chamada (`setOverride: vi.fn()` no factory) desestabiliza dep de `useCallback`/`useEffect` do consumidor → re-render em loop → `waitFor(loading=false)` estoura timeout em todos os testes. Sintoma: stderr repete o mesmo teste N vezes. FIX: referência estável (criar `const fn = vi.fn()` fora do factory e retornar sempre a mesma) -> [`anti-patterns/process_and_testing/AP-204.md`](./anti-patterns/process_and_testing/AP-204.md)
- **[AP-203]** Time-travel de dev (offset em `getNow`/`getRawNow`) vaza p/ ESCRITAS: registrar dose com offset ativo grava `taken_at` no futuro real + ancora instância futura como `taken` → dados sujos no DB compartilhado. Smoke cross-dia: usar conta descartável + limpar `taken_at > now()` ao fim; NUNCA mexer no relógio do SO (quebra JWT Supabase → loop refresh/429) -> [`anti-patterns/process_and_testing/AP-203.md`](./anti-patterns/process_and_testing/AP-203.md)
- **[AP-169]** Branch local outdated antes de spawn → sub-agent duplica arquivos (port manual de PR-A já mergeado) -> [`anti-patterns/process_and_testing/AP-169.md`](./anti-patterns/process_and_testing/AP-169.md)
- **[AP-170]** Alias adicionado em `vitest.config.js` não propaga para `vitest.critical.config.js` (CI falha silencioso com "is not a function") -> [`anti-patterns/process_and_testing/AP-170.md`](./anti-patterns/process_and_testing/AP-170.md)
- **[AP-001]** Production bug — fix goes to unused file -> [`anti-patterns/process_and_testing/AP-001.md`](./anti-patterns/process_and_testing/AP-001.md)
- **[AP-160]** Opus violando R-010 (ordem de hooks) em fixes incrementais pós-validação (cavecrew acerta zero-shot via R-230) -> [`anti-patterns/process_and_testing/AP-160.md`](./anti-patterns/process_and_testing/AP-160.md)
- **[AP-161]** DEVFLOW state.json counter drift entre distillations — sessions modificam INDEX.md sem bumpar counters; D5 self-clean profundo é a correção obrigatória -> [`anti-patterns/process_and_testing/AP-161.md`](./anti-patterns/process_and_testing/AP-161.md)
- **[AP-012]** Broken build, lint errors, failing tests -> [`anti-patterns/process_and_testing/AP-012.md`](./anti-patterns/process_and_testing/AP-012.md)
- **[AP-095]** Preview build fails, PR unmergeeable, wasted review cycles -> [`anti-patterns/process_and_testing/AP-095.md`](./anti-patterns/process_and_testing/AP-095.md)
- **[AP-A01]** Code ends up on `main` without review, history/audit trail lost, violates delive... -> [`anti-patterns/process_and_testing/AP-A01.md`](./anti-patterns/process_and_testing/AP-A01.md)
- **[AP-D02]** 12 logs × 10 protocols = 120 rows in intermediate result. `SUM(expected_count)` ... -> [`anti-patterns/process_and_testing/AP-D02.md`](./anti-patterns/process_and_testing/AP-D02.md)
- **[AP-H18]**  -> [`anti-patterns/process_and_testing/AP-H18.md`](./anti-patterns/process_and_testing/AP-H18.md)
- **[AP-H19]** Accidentally removing View, Text, StyleSheet from imports when replacing them wi... -> [`anti-patterns/process_and_testing/AP-H19.md`](./anti-patterns/process_and_testing/AP-H19.md)
- **[AP-H20]** ~~iCloud sync delay em git commands antes do gsync-native~~ **[OBSOLETO 2026-05-24 — gsync-native aposentado]** -> [`anti-patterns/process_and_testing/AP-H20.md`](./anti-patterns/process_and_testing/AP-H20.md)
- **[AP-H21]** Showing '1 mg' when it should be '1 un. of 50mg'. Mixing protocol quantity (how ... -> [`anti-patterns/process_and_testing/AP-H21.md`](./anti-patterns/process_and_testing/AP-H21.md)
- **[AP-T01]** Tests pass locally, fail in CI; unpredictable hangs -> [`anti-patterns/process_and_testing/AP-T01.md`](./anti-patterns/process_and_testing/AP-T01.md)
- **[AP-T02]** Memory accumulation, OOM on 8GB machines, state leaks between tests -> [`anti-patterns/process_and_testing/AP-T02.md`](./anti-patterns/process_and_testing/AP-T02.md)
- **[AP-T06]** Timing-dependent, flaky in CI; can timeout unexpectedly -> [`anti-patterns/process_and_testing/AP-T06.md`](./anti-patterns/process_and_testing/AP-T06.md)
- **[AP-071]** Timezone-dependent logic in notifications without UTC conversion first. -> [`anti-patterns/notifications/AP-071.md`](./anti-patterns/notifications/AP-071.md)
- **[AP-075]** Presentation Leakage in Business Logic (L1): Construção de strings formatadas em schedulers -> [`anti-patterns/notifications/AP-075.md`](./anti-patterns/notifications/AP-075.md)
- **[AP-076]** Silent Mode Gate em L1 antes do dispatcher — viola 3-layer arch, impede log/inbox para usuários silent -> [`anti-patterns/notifications/AP-076.md`](./anti-patterns/notifications/AP-076.md)
- **[AP-077]** Callback Data HH:MM split destructuring — `split(':')` parte o horário; usar `parts.slice(N).join(':')` -> [`anti-patterns/notifications/AP-077.md`](./anti-patterns/notifications/AP-077.md)
- **[AP-T07]** If assertion fails, Promise stays pending → Vitest hangs indefinitely -> [`anti-patterns/process_and_testing/AP-T07.md`](./anti-patterns/process_and_testing/AP-T07.md)
- **[AP-T08]** Blocks development, 6.5 min wait time discourages testing -> [`anti-patterns/process_and_testing/AP-T08.md`](./anti-patterns/process_and_testing/AP-T08.md)
- **[AP-T09]** Tests >15s can trigger 10-min kill switch in agents, fail CI -> [`anti-patterns/process_and_testing/AP-T09.md`](./anti-patterns/process_and_testing/AP-T09.md)
- **[AP-W01]** Edit goes to wrong file; bug remains; spec can have stale paths -> [`anti-patterns/process_and_testing/AP-W01.md`](./anti-patterns/process_and_testing/AP-W01.md)
- **[AP-W03]** `"Found multiple elements with text…"` test failure -> [`anti-patterns/process_and_testing/AP-W03.md`](./anti-patterns/process_and_testing/AP-W03.md)
- **[AP-H28]** Modificar dados reais de produção para fins de teste de UI. -> [`anti-patterns/process_and_testing/AP-H28.md`](./anti-patterns/process_and_testing/AP-H28.md)
- **[AP-055]** App Store Connect Manual Binary Upload Myth. No "Upload" button for .ipa files... -> [`anti-patterns/process_and_testing/AP-055.md`](./anti-patterns/process_and_testing/AP-055.md)
- **[AP-114]** Validação de Entrada Silenciosa (Missing Zod in Services) -> [`anti-patterns/process_and_testing/AP-114.md`](./anti-patterns/process_and_testing/AP-114.md)
- **[AP-131]** Stale Mock Method Signature after Service Refactor -> [`anti-patterns/process_and_testing/AP-131.md`](./anti-patterns/process_and_testing/AP-131.md)
- **[AP-120]** Refatoração por Purgue (Over-cleaning) — remoção de código orquestrador sem validar dependências de infra -> [`anti-patterns/process_and_testing/AP-120.md`](./anti-patterns/process_and_testing/AP-120.md)
- **[AP-133]** Sub-agente commitando direto na main sem branch/PR/review → regressões de produção silenciosas (layout, auth, admin) -> [`anti-patterns/process_and_testing/AP-133.md`](./anti-patterns/process_and_testing/AP-133.md)
- **[AP-139]** Redundant Null Check in initialized metrics -> [`anti-patterns/process_and_testing/AP-139.md`](./anti-patterns/process_and_testing/AP-139.md)



## ⚛️ React & Ui (`react_and_ui`)
- **[AP-247]** Filtrar tratamentos por `active===true` (ou `status==='ativo'`) sem checar período de vigência vaza prescrições encerradas (`end_date < hoje`) e futuras (`start_date > hoje`) em superfícies de seleção de dose. `resolveTreatmentStatus` não checa `start_date`. Fix: usar `isTreatmentSchedulableOn(p)` de `@dosiq/core` (ativo + hoje ∈ [start, end]). Afeta modal bulk mobile + GlobalDoseModal web. Bug prod 039/TestFlight -> [`anti-patterns/react_and_ui/AP-247.md`](./anti-patterns/react_and_ui/AP-247.md)
- **[AP-245]** Roteamento editar-vs-registrar dose por `isOrphan` (`source==='log'`) em vez de presença de `logId` → dose agendada **já tomada** (não-órfã) caía em `registerRetro` → `register_dose_atomic` (insert-only) → `P0001 "Ocorrência já registrada"`. Toda dose tomada (órfã OU agendada) carrega `logId` (`medicine_log_id`). Fix: rotear por `logId` (tem log → `update_dose_log_atomic`; sem log → registro retroativo). Regra-mãe: editar≠registrar; decisão UPDATE-vs-INSERT checa log de apoio, nunca origem. Detectado smoke pós-688 -> [`anti-patterns/react_and_ui/AP-245.md`](./anti-patterns/react_and_ui/AP-245.md)
- **[AP-241]** Mapeamento parcial à mão de um objeto de origem (ANVISA → wizard) em vez de reusar o mapper canônico → drift de campo silencioso: `AnvisaSearchBar` montava o payload do TreatmentWizard só com `name`/`active_ingredient`/`therapeutic_class`, **omitindo `laboratory` e `regulatory_category`** que o `formatSelectedMedicine` (path do form de medicines) já preenchia. Lab vinha vazio no wizard para não-genéricos; passava no teste isolado do form (path correto) mas falhava no path do wizard. Fix: reusar `formatSelectedMedicine` nos dois pontos (paridade por construção). Família AP-214 (campo dropado) + R-235/R-279 (canônico antes de inline). Detectado smoke 037 -> [`anti-patterns/react_and_ui/AP-241.md`](./anti-patterns/react_and_ui/AP-241.md)
- **[AP-229]** Invalidação por evento (`mr:*-saved`) wired em só UM de N hooks que leem a mesma fonte → consumidor esquecido fica stale (ex.: `ScatterTrend` não atualizava após editar medida; listener só no `useTodayMeasures`, faltou no `useMeasures`). Fix: inventariar TODOS os hooks que leem a tabela e wirar o mesmo listener em cada. Smoke: escrita em 1 superfície + verificar atualização nas demais. Detectado review #666 -> [`anti-patterns/react_and_ui/AP-229.md`](./anti-patterns/react_and_ui/AP-229.md)
- **[AP-222]** `useState(prop)` não rastreia prop mutável — modal/form reutilizado no DOM fica com dados do 1º mount (form vazio ou com registro anterior), sem erro no console. Fix: `key={entity?.id || 'create'}` no caller força remount. NÃO usar `useEffect+setState` (lint error `set-state-in-effect`). -> [`anti-patterns/react_and_ui/AP-222.md`](./anti-patterns/react_and_ui/AP-222.md)
- **[AP-172]** Recalcular status de estoque manualmente (thresholds por dias) em vez de `resolveStockStatus` — saldo 0 sem consumo virava HIGH (deveria CRITICAL); diverge da listagem -> [`anti-patterns/react_and_ui/AP-172.md`](./anti-patterns/react_and_ui/AP-172.md)
- **[AP-004]** `ReferenceError` — TDZ crash -> [`anti-patterns/react_and_ui/AP-004.md`](./anti-patterns/react_and_ui/AP-004.md)
- **[AP-094]** ReferenceError: Cannot access handler before initialization — TDZ crash -> [`anti-patterns/react_and_ui/AP-094.md`](./anti-patterns/react_and_ui/AP-094.md)
- **[AP-97]** Última definição sobrescreve design system canônico, causando valores legados/op... -> [`anti-patterns/react_and_ui/AP-97.md`](./anti-patterns/react_and_ui/AP-97.md)
- **[AP-D04]** Criar sistema de badge de estoque novo em vez de reutilizar StockPill -> [`anti-patterns/react_and_ui/AP-D04.md`](./anti-patterns/react_and_ui/AP-D04.md)
- **[AP-D06]** Exibir dados de Carlos (bar-pct, quantidade, histórico global) no modo Dona Mari... -> [`anti-patterns/react_and_ui/AP-D06.md`](./anti-patterns/react_and_ui/AP-D06.md)
- **[AP-H02]** Passar IDs ao invés de objetos para treatmentPlanService no LogForm -> [`anti-patterns/react_and_ui/AP-H02.md`](./anti-patterns/react_and_ui/AP-H02.md)
- **[AP-H03]** Estado isDoseModalOpen fora do DashboardProvider sem lazy load -> [`anti-patterns/react_and_ui/AP-H03.md`](./anti-patterns/react_and_ui/AP-H03.md)
- **[AP-H06]** Runtime error when React/React-DOM pair doesn't have exact same version (19.2.5 ... -> [`anti-patterns/react_and_ui/AP-H06.md`](./anti-patterns/react_and_ui/AP-H06.md)
- **[AP-H15]** useCallback com state no deps array + useEffect([callback]) cria loop infinito: ... -> [`anti-patterns/react_and_ui/AP-H15.md`](./anti-patterns/react_and_ui/AP-H15.md)
- **[AP-LOG-001]** 50+ debug logs on Dashboard load polluteDevTools, confuse users, create noise th... -> [`anti-patterns/react_and_ui/AP-LOG-001.md`](./anti-patterns/react_and_ui/AP-LOG-001.md)
- **[AP-H22]** Global Brand Leak: Applying brand-specific aesthetic tokens (Comfortaa, mint backgrounds) to global UI keys causes UX fatigue and readability issues in the main app product. -> [`anti-patterns/react_and_ui/AP-H22.md`](./anti-patterns/react_and_ui/AP-H22.md)
- **[AP-P02]** Safari blocks Main Thread 200-400ms for parse/compile before first render (e.g.,... -> [`anti-patterns/react_and_ui/AP-P02.md`](./anti-patterns/react_and_ui/AP-P02.md)
- **[AP-P09]** N queries Supabase simultaneous. With 10 items → 10 round-trips HTTP, each block... -> [`anti-patterns/react_and_ui/AP-P09.md`](./anti-patterns/react_and_ui/AP-P09.md)
- **[AP-P10]** All columns transferred unnecessarily. 90 days logs × 10 protocols = ~2700 rows ... -> [`anti-patterns/react_and_ui/AP-P10.md`](./anti-patterns/react_and_ui/AP-P10.md)
- **[AP-P11]** Ref callbacks recreated on state change. React calls `old(null)` without cleanup... -> [`anti-patterns/react_and_ui/AP-P11.md`](./anti-patterns/react_and_ui/AP-P11.md)
- **[AP-P19]** Daily rows show inflated totals like `360/360` or mismatch the clinical meaning ... -> [`anti-patterns/react_and_ui/AP-P19.md`](./anti-patterns/react_and_ui/AP-P19.md)
- **[AP-W02]** size="sm" and size="md" props stop working, API contract broken, layout regressi... -> [`anti-patterns/react_and_ui/AP-W02.md`](./anti-patterns/react_and_ui/AP-W02.md)
- **[AP-W04]** Violates Wave 1 purity guardrail; couples component to context, breaking reuse -> [`anti-patterns/react_and_ui/AP-W04.md`](./anti-patterns/react_and_ui/AP-W04.md)
- **[AP-W05]** Flash of full/empty ring before animation starts (browser renders default value) -> [`anti-patterns/react_and_ui/AP-W05.md`](./anti-patterns/react_and_ui/AP-W05.md)
- **[AP-W06]** Silent failure on Safari < 16.2; no background color applied -> [`anti-patterns/react_and_ui/AP-W06.md`](./anti-patterns/react_and_ui/AP-W06.md)
- **[AP-W08]** Wrong ID passed to logService.create(); log references wrong protocol -> [`anti-patterns/react_and_ui/AP-W08.md`](./anti-patterns/react_and_ui/AP-W08.md)
- **[AP-W09]** High risk of breaking SmartAlerts, LogForm integrations in 932-line file -> [`anti-patterns/react_and_ui/AP-W09.md`](./anti-patterns/react_and_ui/AP-W09.md)
- **[AP-W10]** Increases API surface; creates unintended dependencies -> [`anti-patterns/react_and_ui/AP-W10.md`](./anti-patterns/react_and_ui/AP-W10.md)
- **[AP-W11]** Prop silently ignored; feature broken with no error or warning in runtime or tes... -> [`anti-patterns/react_and_ui/AP-W11.md`](./anti-patterns/react_and_ui/AP-W11.md)
- **[AP-W13]** CI lint failure; confuses future agents about what is active -> [`anti-patterns/react_and_ui/AP-W13.md`](./anti-patterns/react_and_ui/AP-W13.md)
- **[AP-W15]** State is stale if derived value changes after mount (e.g., `defaultViewMode` aft... -> [`anti-patterns/react_and_ui/AP-W15.md`](./anti-patterns/react_and_ui/AP-W15.md)
- **[AP-W17]** Defaults de expansão de seções ficam presos no valor do primeiro render; UX inco... -> [`anti-patterns/react_and_ui/AP-W17.md`](./anti-patterns/react_and_ui/AP-W17.md)
- **[AP-W18]** TypeError at runtime: "Cannot read properties of undefined" when component tries... -> [`anti-patterns/react_and_ui/AP-W18.md`](./anti-patterns/react_and_ui/AP-W18.md)
- **[AP-W19]** Component displays wrong values (e.g., ring gauge shows 0% adherence). useDashbo... -> [`anti-patterns/react_and_ui/AP-W19.md`](./anti-patterns/react_and_ui/AP-W19.md)
- **[AP-W20]** Worse UX: 4 clicks (button → modal open → form fill → confirm) instead of 1-clic... -> [`anti-patterns/react_and_ui/AP-W20.md`](./anti-patterns/react_and_ui/AP-W20.md)
- **[AP-W21]** Batch UI Promises Single-Item Implementation -> [`anti-patterns/react_and_ui/AP-W21.md`](./anti-patterns/react_and_ui/AP-W21.md)
- **[AP-W22]** Responsive Layout Using Inline Styles Instead of CSS Classes -> [`anti-patterns/react_and_ui/AP-W22.md`](./anti-patterns/react_and_ui/AP-W22.md)
- **[AP-W23]** Destructuring Wrong Property Name from Hook (Wave 7 Treatments — 2026-03-25) -> [`anti-patterns/react_and_ui/AP-W23.md`](./anti-patterns/react_and_ui/AP-W23.md)
- **[AP-W24]** FABs e chatbot trigger aparecem sobre Modal mesmo com z-index corrigido -> [`anti-patterns/react_and_ui/AP-W24.md`](./anti-patterns/react_and_ui/AP-W24.md)
- **[AP-W25]** Usar componente Button com className custom causa conflito de estilos no mobile -> [`anti-patterns/react_and_ui/AP-W25.md`](./anti-patterns/react_and_ui/AP-W25.md)
- **[AP-W26]** Flicker de interface no mount causado por animações concorrentes com LoadingState. -> [`anti-patterns/react_and_ui/AP-W26.md`](./anti-patterns/react_and_ui/AP-W26.md)
- **[AP-W27]** Typography e FontWeights fixos (hardcoded) em vez de usar tokens compartilhados. -> [`anti-patterns/react_and_ui/AP-W27.md`](./anti-patterns/react_and_ui/AP-W27.md)
- **[AP-W29]** Heurística de expansão de UI não re-avaliada na virada de dia local. -> [`anti-patterns/react_and_ui/AP-W29.md`](./anti-patterns/react_and_ui/AP-W29.md)
- **[AP-056]** Unstable Fetcher Loop -> [`anti-patterns/react_and_ui/AP-056.md`](./anti-patterns/react_and_ui/AP-056.md)
- **[AP-057]** Closure Reference Leak (Snapshot ReferenceError) -> [`anti-patterns/react_and_ui/AP-057.md`](./anti-patterns/react_and_ui/AP-057.md)
- **[AP-111]** Blind Toggle Fallacy (Double-tap bug with implicit state) -> [`anti-patterns/react_and_ui/AP-111.md`](./anti-patterns/react_and_ui/AP-111.md)
- **[AP-132]** Unprotected String Prototypes on Form Data: Prevents crash when calling .trim() or similar on undefined/null form fields. -> [`anti-patterns/react_and_ui/AP-132.md`](./anti-patterns/react_and_ui/AP-132.md)
- **[AP-134]** CSS breakpoint sem descontar sidebar 256px → overflow em tablet (1024px útil ≈ 464px). Layouts 2-col com cards largos requerem 1280px+ -> [`anti-patterns/react_and_ui/AP-134.md`](./anti-patterns/react_and_ui/AP-134.md)
- **[AP-135]** user_metadata.role inexistente neste projeto → isAdmin sempre false → AdminSection nunca renderiza. Admin = VITE_ADMIN_CHAT_ID vs telegram_chat_id -> [`anti-patterns/react_and_ui/AP-135.md`](./anti-patterns/react_and_ui/AP-135.md)
- **[AP-125]** Stale Dispatcher Mock Contracts — Manter mocks de testes que esperam estruturas de payload prontas (L2) quando a arquitetura foi refatorada para enviar dados brutos de domínio (L1). -> [`anti-patterns/process_and_testing/AP-125.md`](./anti-patterns/process_and_testing/AP-125.md)
- **[AP-198]** "TypeError: Cannot read property 'useState' of null" crash caused by Metro bundling multiple versions of React. -> [`anti-patterns/react_and_ui/AP-198.md`](./anti-patterns/react_and_ui/AP-198.md)
- **[AP-199]** C4 DoD check não verificou todos os arquivos listados na spec — item de spec silenciosamente pulado (renumerado de uma colisão AP-117 + entrada de índice recriada no D5) -> [`anti-patterns/process_and_testing/AP-199.md`](./anti-patterns/process_and_testing/AP-199.md)
- **[AP-136]** Wide useMemo Dependencies: Passar objetos inteiros (dashboardData) em vez de propriedades específicas (protocols) causa re-renders espúrios e bails do compilador -> [`anti-patterns/react_and_ui/AP-136.md`](./anti-patterns/react_and_ui/AP-136.md)
- **[AP-137]** React Compiler vs Manual Memoization: Mismatch entre manual deps e inferência do compilador força skip de otimização (Compilation Skipped) -> [`anti-patterns/react_and_ui/AP-137.md`](./anti-patterns/react_and_ui/AP-137.md)
- **[AP-197]** ReferenceError: 'colors' is not defined. Falta de import de tokens ao padronizar UI nativa. (renumerado de AP-H26 no D5 — colisão de ID) -> [`anti-patterns/react_and_ui/AP-197.md`](./anti-patterns/react_and_ui/AP-197.md)
- **[AP-H23]** @react-native-firebase v19 incompatível com Swift AppDelegate (Expo 53+) — Firebase não inicializa; usar v21+ -> [`anti-patterns/mobile_and_platform/AP-H23.md`](./anti-patterns/mobile_and_platform/AP-H23.md)
- **[AP-H24]** FirebaseCoreInternal/GoogleUtilities quebra pod install sem useModularHeaders — instalar expo-build-properties com `ios.useModularHeaders: true` -> [`anti-patterns/mobile_and_platform/AP-H24.md`](./anti-patterns/mobile_and_platform/AP-H24.md)
- **[AP-H25]** googleServicesFile iOS ausente no app.config.js — sempre configurar `ios.googleServicesFile` além do Android -> [`anti-patterns/mobile_and_platform/AP-H25.md`](./anti-patterns/mobile_and_platform/AP-H25.md)
- **[AP-059]** CSS var(--token) em pacotes compartilhados web+mobile — RN não resolve custom properties; usar hex direto ou tokens JS -> [`anti-patterns/mobile_and_platform/AP-059.md`](./anti-patterns/mobile_and_platform/AP-059.md)
- **[AP-116]** getLastNotificationResponseAsync dentro de useEffect com session como dep — re-executa em logout+login causando navegação de cold start espúria -> [`anti-patterns/mobile_and_platform/AP-116.md`](./anti-patterns/mobile_and_platform/AP-116.md)
- **[AP-117]** Params de navegação sem contrato tipado entre emissor e receptor — mismatch silencioso (ex: `bulkMode` vs `screen`) faz a modal nunca abrir -> [`anti-patterns/mobile_and_platform/AP-117.md`](./anti-patterns/mobile_and_platform/AP-117.md)
- **[AP-118]** Repository INSERT não espelhando campos do dispatcher/schema após adição tardia — dados descartados silenciosamente no banco mesmo com schema e dispatcher corretos -> [`anti-patterns/mobile_and_platform/AP-118.md`](./anti-patterns/mobile_and_platform/AP-118.md)
- **[AP-119]** Redundant Derived State Synchronization -> [`anti-patterns/react_and_ui/AP-119.md`](./anti-patterns/react_and_ui/AP-119.md)
- **[AP-121]** Inline Styles for Design System Tokens: Use utility classes instead of inline style attributes for consistent design. -> [`anti-patterns/react_and_ui/AP-121.md`](./anti-patterns/react_and_ui/AP-121.md)
- **[AP-122]** Hardcoded Identifiers for Authorization: Hardcoding user/admin IDs in logic bypasses dynamic authorization systems. -> [`anti-patterns/react_and_ui/AP-122.md`](./anti-patterns/react_and_ui/AP-122.md)
- **[AP-123]** Disconnected "Back" Navigation Flow: Navigation that assumes a fixed parent (like Dashboard) instead of actual entry context. -> [`anti-patterns/react_and_ui/AP-123.md`](./anti-patterns/react_and_ui/AP-123.md)
- **[AP-124]** MarkdownV2 escapes in pushBody: results in visible backslashes in mobile notifications. -> [`anti-patterns/mobile_and_platform/AP-124.md`](./anti-patterns/mobile_and_platform/AP-124.md)
- **[AP-126]** Config Linting Regression — Rodar regras ESM estritas em arquivos de configuração causa ERR_PACKAGE_PATH_NOT_EXPORTED. -> [`anti-patterns/process_and_testing/AP-126.md`](./anti-patterns/process_and_testing/AP-126.md)
- **[AP-127]** Cross-Boundary Import Leak — Imports relativos entre domínios isolados (web/server) quebram a arquitetura de monorepo. -> [`anti-patterns/infra_and_deploy/AP-127.md`](./anti-patterns/infra_and_deploy/AP-127.md)
- **[AP-128]** Re-export de sub-path de workspace sem wildcard — "Missing specifier" em Vite 7/Vitest. Fix: adicionar `"./schemas/*": "./src/schemas/*.js"` no exports field. -> [`anti-patterns/infra_and_deploy/AP-128.md`](./anti-patterns/infra_and_deploy/AP-128.md)
- **[AP-129]** Imports sem extensão `.js` em ESM Node.js — "Cannot find module" no Vercel. `import { x } from './schema'` falha em ESM puro; usar `'./schema.js'`. -> [`anti-patterns/infra_and_deploy/AP-129.md`](./anti-patterns/infra_and_deploy/AP-129.md)
- **[AP-196]** Strict Metadata Validation causing Logging Regressions — Validar estritamente objetos de transporte de metadados remove campos de domínio vitais para observabilidade. (renumerado de AP-140 no D5 — colisão de ID) -> [`anti-patterns/process_and_testing/AP-196.md`](./anti-patterns/process_and_testing/AP-196.md)



- **[AP-138]** Promise não-awaited em Linking event handler → crash via TurboModule (EXC_CRASH SIGABRT). Toda operação async em Linking callbacks exige await + try/catch. -> [`anti-patterns/mobile_and_platform/AP-138.md`](./anti-patterns/mobile_and_platform/AP-138.md)
- **[AP-195]** `Object.fromEntries(new URLSearchParams(str))` lança "iterator method is not callable" no Hermes (React Native/Expo). URLSearchParams não implementa o protocolo iterator no Hermes. Usar `.get(key)` diretamente. (renumerado de AP-139 no D5 — colisão de ID) -> [`anti-patterns/mobile_and_platform/AP-195.md`](./anti-patterns/mobile_and_platform/AP-195.md)
- **[AP-141]** Fragmentação de Resolução de IDs em Notificações Cross-Platform (protocol_id vs protocolId) -> [`anti-patterns/mobile_and_platform/AP-141.md`](./anti-patterns/mobile_and_platform/AP-141.md)
- **[AP-191]** Adesão SQL assimétrica numerador/denominador — `v_daily_adherence` calcula `expected` só de `protocols WHERE active=true` mas conta `taken` de TODOS os logs; protocolo finalizado (one-shot, ex: antibiótico 7d) sai do denominador e fica no numerador → `taken>expected` → `adherence>100%` (sparkline "118%↓"). Sem clamp. Numerador e denominador DEVEM derivar da MESMA fonte/escopo (idealmente `dose_instances`); sempre clamp 0-100 em % de adesão. -> [`anti-patterns/data_and_schema/AP-191.md`](./anti-patterns/data_and_schema/AP-191.md)
- **[AP-192]** ✅RESOLVED(F3.2d) SPA reseta a view no refresh de token (refocus da aba) — effect de roteamento URL→view depende de `session`; ao refocar, Supabase renova o token → novo `session` → effect re-dispara → como a URL não acompanha navegação interna (SPA state-based), força `setCurrentView('dashboard')` → usuário é jogado do HealthHistory pro "hoje". Fix: gatear effect a `session?.id` (identidade do usuário), não ao objeto `session`. -> [`anti-patterns/react_and_ui/AP-192.md`](./anti-patterns/react_and_ui/AP-192.md)
- **[AP-193]** ✅RESOLVED(S3.6.2/mobile 0.7.0 + reconcile one-shot PR-F4.1/S4.0.5: DB varrido em todos usuários, 0 colisões slot missed×avulso restantes; adapter de timeline dedupe por (protocol_id, slot-tolerância)) Writer cross-plataforma sem âncora log↔instância → falsos `missed`. A âncora da F2.3 (liga `medicine_log`→`dose_instance` via markTaken/snap) ficou só no web + bot; o **mobile `doseService` (FAB + BulkModal) insere log e PARA**, sem ancorar → log avulso → instância `pending` → sweep F2.5 vira `missed` → falso missed corrompe streak/adesão (view legada de logs diverge mostrando 100%). Toda superfície que escreve logs é writer da máquina de estados e DEVE ancorar (reusar core repo, R-231). Ao migrar LEITURA pra instâncias, auditar os pontos de ESCRITA da mesma plataforma. Fix: S3.6.2/F3.3 + reconcile one-shot. -> [`anti-patterns/data_and_schema/AP-193.md`](./anti-patterns/data_and_schema/AP-193.md)
- **[AP-233]** ✅RESOLVED(spec 033/PR #671/mobile 0.19.0) Event ID prefixado do core (`inst:<uuid>`/`log:<uuid>`) usado como UUID bruto. `createTimelineService` prefixa `event.id` p/ unicidade na timeline mesclada; o UUID bruto vive em `payload.instanceId`/`payload.logId` (exposto via `mapToMobileShape`). Passar `instance.id` (prefixado) em `registerDose`/`undoDose` → `_anchorLogToInstance` best-effort não acha a instância e **falha em silêncio**: log inserido + estoque consumido, mas `dose_instances.status` fica `missed` (desync, parente do AP-193). Fix: usar `instanceId`/`logId` (UUID bruto) p/ FK/RPC/mutação; `event.id` só p/ React key/dedup. Âncora best-effort deve LOGAR falha de match. -> [`anti-patterns/data_and_schema/AP-233.md`](./anti-patterns/data_and_schema/AP-233.md)
- **[AP-201]** 🔒SEGURANÇA: `CREATE OR REPLACE VIEW` **reseta `security_invoker` para false** (Postgres não preserva o reloption). View sobre tabela com RLS (`dose_instances`, policy `user_id=auth.uid()`) passa a rodar como OWNER → **bypassa RLS**. Como o client consome `v_daily_adherence`/`v_adherence_heatmap` SEM filtro `user_id` (depende de RLS p/ escopar), a view retornou TODOS os 15 usuários → consumidor faz `grid[dow][period]=row` e a última linha (de outro usuário) sobrescreve → células 0%/worstCell errados + **vazamento cross-usuário**. Sintoma enganoso: parecia cache stale (sobreviveu a hard reload/server restart) — só o Network cru (linhas duplicadas por chave) revelou. FIX: `ALTER VIEW ... SET (security_invoker = true)` (ou declarar no CREATE). Regra: toda view sobre tabela RLS declara `WITH (security_invoker = true)` -> [`anti-patterns/infra_and_deploy/AP-201.md`](./anti-patterns/infra_and_deploy/AP-201.md)
- **[AP-200]** Mudança de FONTE/shape de dado cacheado server-side (ex: rewrite de view `v_daily_adherence`/`v_adherence_heatmap` medicine_logs→dose_instances, PR-F4.1) NÃO invalida o cache de query persistido em `localStorage` (`dosiq_query_cache`, SWR staleTime 30s + evict 60s). Hard reload (Cmd+Shift+R) limpa HTTP/memória mas NÃO localStorage → SWR serve stale e o componente guarda no state → usuário vê adesão pré-migração "presa" mesmo após refresh. Invalidação programática só dispara em mutação (log create/edit/delete), não em DDL. FIX: bumpar `persistKey` (`dosiq_query_cache`→`_v2`) no deploy que muda a fonte → todo cliente abandona o cache velho e refetcha. Regra: bumpar persistKey a cada mudança de fonte/shape de query cacheada server-side -> [`anti-patterns/react_and_ui/AP-200.md`](./anti-patterns/react_and_ui/AP-200.md)
- **[AP-194]** `getNow()`/`getSaoPauloTime().toISOString()` p/ limite de query em coluna UTC desloca a janela 3h (omite doses recentes). `getSaoPauloTime` retorna `new Date(spWallClock)` → em servidor UTC, `.toISOString()` rotula o horário de SP como UTC (3h a menos) vs `scheduled_for`/`taken_at` (UTC real). Usar `getServerTimestamp()` (UTC real) + `addDays(nowIso,-N)`, 1 chamada de timestamp/janela. `getNow()` só p/ wall-clock/fronteira-de-dia local, nunca `.toISOString()` vs coluna UTC. Janelas adjacentes com gte/lte inclusivo precisam de teto exclusivo (ms-1) p/ não double-contar boundary. -> [`anti-patterns/data_and_schema/AP-194.md`](./anti-patterns/data_and_schema/AP-194.md)
- **[AP-210]** `@supabase/supabase-js` ≥ 2.90 verifica WebSocket global no construtor — crash em Node < 22 mesmo sem uso de Realtime. Fix: `realtime: { transport: ws }` + dep direta `ws ^8.21` -> [`anti-patterns/infra_and_deploy/AP-210.md`](./anti-patterns/infra_and_deploy/AP-210.md)
- **[AP-211]** Dep transitiva crítica de runtime (`ws`, fetch polyfill) sem entrada direta em package.json — pode ser removida em `npm install` futuro sem aviso. Fix: dep direta no workspace correto -> [`anti-patterns/infra_and_deploy/AP-211.md`](./anti-patterns/infra_and_deploy/AP-211.md)
- **[AP-212]** Sem smoke test de cold start serverless — erros de import chegam a prod (CI usa JSDOM que tem globals de browser; Node 20 puro não). Fix: `scripts/smoke-server.mjs` no CI em Node 20 -> [`anti-patterns/infra_and_deploy/AP-212.md`](./anti-patterns/infra_and_deploy/AP-212.md)
- **[AP-216]** Destruturação insegura de respostas do Supabase Auth (`getUser`/`getSession`) → crash com TypeError quando o objeto data está nulo ou indefinido. Sempre desestruturar `data` primeiro e usar encadeamento opcional `data?.user` -> [`anti-patterns/infra_and_deploy/AP-216.md`](./anti-patterns/infra_and_deploy/AP-216.md)
- **[AP-254]** ⚠️ **SUPERSEDED em 2026-07-19 pela spec 052** (Slice A #761 criou a coluna, Slice B #762 ligou o `NOT NULL`): `dose_instances` **TEM `medicine_id`** e ele é a fonte CANÔNICA da identidade — ler do protocolo via enrich é agora o BUG, não o fix. Histórico: o AP nasceu quando a coluna não existia e a identidade vinha do join; a leitura direta devolvia `undefined` → falha Zod silenciosa em `registerDose`. Mantido por rastreabilidade e porque o texto antigo **induziu erro em revisão automática** (RC6 da #762 citou este AP para acusar o diff de burlar o R-295, contra o banco real). Farol: AP que afirma AUSÊNCIA de coluna vira mentira no dia em que a coluna nasce — no C5 de toda spec que adiciona coluna, **grepar os APs/rules que negam a existência dela**. Leitura correta: helper `resolveInstanceMedicine` (@dosiq/core) -> [`anti-patterns/mobile_and_platform/AP-254.md`](./anti-patterns/mobile_and_platform/AP-254.md)
- **[AP-218]** Hermes (Android) sem ICU completo — `toLocaleString`/`toLocaleTimeString` retorna AM/PM silencioso em vez de formato pt-BR. Fix: formatação manual com `padStart` ou `date-fns/format` -> [`anti-patterns/mobile_and_platform/AP-218.md`](./anti-patterns/mobile_and_platform/AP-218.md)
- **[AP-219]** `PanResponder` com `onStartShouldSetPanResponder: true` captura todos os toques e bloqueia `TouchableOpacity` filhos silenciosamente. Fix: `onStart=false` + `onMove` com threshold `dx>10 && dy<5` -> [`anti-patterns/mobile_and_platform/AP-219.md`](./anti-patterns/mobile_and_platform/AP-219.md)
- **[AP-220]** `FlatList scrollEnabled=false` dentro de `ScrollView` — virtualização desabilitada + warning React Native. Fix: `View + .map()` para listas pequenas (≤50 itens) -> [`anti-patterns/mobile_and_platform/AP-220.md`](./anti-patterns/mobile_and_platform/AP-220.md)
- **[AP-230]** Pausa de protocolo com `markSkippedPaused(+24h)` deixa instâncias além das 24h como `pending` → `sweepMissedInstances` corre antes do cron e converte para `missed` indevidamente. Caso real: anticoncepcional 7d de pausa entre cartelas. Fix: `markAllFutureSkippedPaused()` sem cutoff temporal na pausa -> [`anti-patterns/mobile_and_platform/AP-230.md`](./anti-patterns/mobile_and_platform/AP-230.md)
- **[AP-252]** `getInitialNotification` no cold start tratava só o canal de alarme (`openAlarmScreen` rejeita outros) → ação "Registrar" da superfície (`dose-activity-v1`) com app morto era perdida. Fix: rotear por `pressAction.id` (surface-register → `handleAlarmAction`) + guard `useRef` 1x/ciclo. Cold-start handler deve cobrir TODOS os canais que fazem `launchActivity` -> [`anti-patterns/mobile_and_platform/AP-252.md`](./anti-patterns/mobile_and_platform/AP-252.md)
- **[AP-253]** Soneca não sobrevive ao resync: `syncAlarms→cancelAll()` (incondicional, roda em todo launch incl. fullScreen do alarme) mata o trigger de soneca; item snoozed era excluído do re-agendamento e `getWindow(now,end)` sem lookback exclui a dose vencida. Fix: `scheduleAlarm` ganha `fireAt`; `syncAlarms` re-arma snoozed no `snoozed_until`; `LOOK_BACK_DAYS=1`. Estado durável→trigger deve ser re-armável do DB -> [`anti-patterns/mobile_and_platform/AP-253.md`](./anti-patterns/mobile_and_platform/AP-253.md)
- **[AP-260]** Script de migração reescreve imports fora do domínio (colisão de basename) e sessão commita → `.js` some de imports Node ESM, `ERR_MODULE_NOT_FOUND` em runtime invisível pro vitest (040 F2/434144e). Fix: reverter arquivos fora do domínio + smoke Node -> [`anti-patterns/infra_and_deploy/AP-260.md`](./anti-patterns/infra_and_deploy/AP-260.md)
- **[AP-261]** Exports órfãos em package.json pós-rename .js→.ts → imports resolvem como `any` silencioso (strict 'verde ilusório') + `import-x/no-cycle` quebra no CI. Fix: exports no MESMO commit do rename (040 F2/b9086ec+e13a500) -> [`anti-patterns/infra_and_deploy/AP-261.md`](./anti-patterns/infra_and_deploy/AP-261.md)
- **[AP-262]** Erro rotulado "pré-existente" sem registro rastreável = erro órfão: atravessa sessões sem dono (040 F3, erro do core F2 resgatado só no gate). Fix trivial → corrigir na sessão; senão TODO/seção Dívida no PR. Resultado local-only não conta -> [`anti-patterns/infra_and_deploy/AP-262.md`](./anti-patterns/infra_and_deploy/AP-262.md)
- **[AP-263]** tsx/vitest/tsc-bundler mascaram import extensionless → smoke local verde, `ERR_MODULE_NOT_FOUND` no Node ESM Vercel (2ª ocorrência da classe R-282; 20 imports F3.2/3.3 derrubaram 4 rotas no preview). Fix: guard grep bloqueante no strict-island.sh -> [`anti-patterns/infra_and_deploy/AP-263.md`](./anti-patterns/infra_and_deploy/AP-263.md)
- **[AP-264]** Dependência de pacote privado do monorepo em package standalone (server/) quebra `npm ci --prefix` isolado do CI (404 registry). Runtime resolve via node_modules do root por lookup ancestral — não declarar -> [`anti-patterns/infra_and_deploy/AP-264.md`](./anti-patterns/infra_and_deploy/AP-264.md)
- **[AP-265]** Rename em massa não varre globs de configs de teste/CI: F4 renomeou src pra .ts/.tsx e 4 configs vitest (smoke/ci/lowram/coverage) ficaram com {js,jsx} → "No test files found" no CI remoto; validate:agent não exercita configs alternativas. Fix: grep de extensão antiga em todas as configs de tooling -> [`anti-patterns/infra_and_deploy/AP-265.md`](./anti-patterns/infra_and_deploy/AP-265.md)
- **[AP-266]** Aceitar fix de revisor de IA sem verificar a semântica do callee: Gemini marcou `DoseListItem._formatTakenTime` Critical com causação errada (`formatLocalDate` "inclui hora se passar `true`"); dev aplicou `(formatLocalDate as any)(…, true)`, compilou/passou 37 testes, mas o core ignora o 2º arg → retorna `YYYY-MM-DD` → `split(' ')[1]` undefined → TypeError. Crash pré-existente, o `as any` escondeu o TS2554. Ler o callee + citar a linha antes de aplicar; não usar `as any` p/ silenciar diagnóstico real -> [`anti-patterns/process_and_testing/AP-266.md`](./anti-patterns/process_and_testing/AP-266.md)
- **[AP-267]** Cast `(x.navigate as any)(…)` que abre statement após linha sem `;` → ASI funde as linhas (`f()(…)`) e crasha em runtime; tsc não pega quando alvo é `any` (call é "callable"). 3× no 040 F5 lote 6. Fix: prefixar cast statement-position com `;` + rodar jest do escopo (any mascara ASI) -> [`anti-patterns/react_and_ui/AP-267.md`](./anti-patterns/react_and_ui/AP-267.md)
- **[AP-268]** `as any` em options de navigator sem validar o literal no union do stack em uso: `presentation: 'fullScreenModal'` não existe no JS stack (só native-stack) — cast re-enterrou bug real de UI; erro de tipo em literal de config merece 30s de verificação antes do cast -> [`anti-patterns/mobile_and_platform/AP-268.md`](./anti-patterns/mobile_and_platform/AP-268.md)
- **[AP-269]** Afirmação de runtime não verificada em comentário de código ("CheckCircle2 nem existe em runtime" — falso, alias deprecated existe no bundle) propagou por 3 arquivos e induziu o Gemini a 2 highs falsos no PR #725; TS2305 = ausência nas declarações, não no runtime -> [`anti-patterns/process_and_testing/AP-269.md`](./anti-patterns/process_and_testing/AP-269.md)
- **[AP-270]** Fixture de teste com `toISOString()` p/ derivar dia-calendário = flaky por fuso (verde de dia, quebra pós-21h BRT — GMT-3 vira dia seguinte em UTC). Data em teste SEMPRE local (`formatLocalDate`/string explícita) ou relógio congelado em horário neutro. Versão "em fixture" do AP-240/R-020. Detectado 040 F6 6.5 (SparklineAdesao) -> [`anti-patterns/process_and_testing/AP-270.md`](./anti-patterns/process_and_testing/AP-270.md)
- **[AP-271]** Glob sem âncora em ignore file (`scripts/` no .vercelignore) casa em QUALQUER nível e excluiu `packages/core/scripts/` do upload → prepare do core quebrou o npm install na Vercel (MODULE_NOT_FOUND em arquivo que existia no commit). Dir que deve valer só na raiz = `/scripts/`; nenhum gate local exercita ignore de upload. Detectado 040 F6 (#731) -> [`anti-patterns/infra_and_deploy/AP-271.md`](./anti-patterns/infra_and_deploy/AP-271.md)
- **[AP-286]** Passo OPCIONAL acrescentado depois da gravação do wizard reabre a janela de duplicação: o setup já está no banco mas `onboarding_completed` ainda não foi marcado, e os guards de idempotência (`createdMedicineId`/`protocolCreated`) são estado de COMPONENTE — morrem no restart do app. 044 F4b: a tela de saldo inicial (coleta opcional) entrou entre a gravação e o `finish()` → matar o app ali recriava medicamento+tratamento. Reabertura do [[AP-283]] por caminho novo, sem violar uma linha dele. Regra: a conclusão pertence ao último passo que PERSISTE, não à última tela que APARECE — coleta opcional marca a flag ANTES de navegar. Farol: "dado do usuário no banco + flag de terminou ainda não" -> [`anti-patterns/react_and_ui/AP-286.md`](./anti-patterns/react_and_ui/AP-286.md)
- **[AP-287]** Carimbo de estado (`stock_paused_at` = "existe saldo congelado desde T") reusado para gravar a ESCOLHA INICIAL do onboarding → afirma um fato que nunca aconteceu (não há saldo para congelar). Conta que nasceu dose-only ficava indistinguível de conta congelada (`neverHadStock = !enabled && !pausedAt` dava false) → o toggle entrava no fluxo de RETOMADA (gap de minutos, silencioso), religava o FIFO sobre zero e a tela de saldo inicial (PO-3) NUNCA aparecia. Zero erro, zero teste vermelho: o fluxo errado executou com sucesso. Campo que carrega um FATO sobre o passado não pode ser escrito por caminho onde o fato não ocorreu — "o efeito colateral é o mesmo" ignora que o carimbo é uma ASSERÇÃO, e asserção falsa se vinga em quem a lê como verdade. Fix: `setStockTracking(enabled, {freeze})` (onboarding usa `freeze:false`) + `assessResume().hasFrozenBalance` como cinto de segurança (carimbo sem saldo → pede saldo inicial). Pego só no SMOKE — vive na fronteira onboarding-grava→settings-lê, que nenhum teste cruzava. 044 F4b -> [`anti-patterns/data_and_schema/AP-287.md`](./anti-patterns/data_and_schema/AP-287.md)
- **[AP-288]** Sob `edgeToEdgeEnabled` (Expo/Android), o Android NÃO redimensiona mais a janela com o teclado → o `adjustResize` do AndroidManifest vira letra morta e `KeyboardAvoidingView` com `behavior={undefined}` é NO-OP (componente montado, com cara de proteção, que não protege): teclado cobre inputs e rodapé. Usar `behavior='height'` no Android (é o que ChatScreen/DoseRegisterModal — as telas que convivem com teclado de verdade — já fazem). Junto: `keyboardVerticalOffset={60}` só faz sentido com header de NAVEGAÇÃO (AppBar próprio → 0, senão o rodapé descola); rodapé fixo fica DENTRO do KAV; `ScrollView` no KAV precisa de `flex:1` no `style` (só `flexGrow` no contentContainer → cresce e empurra os botões p/ fora da tela); safe area inferior é CONDICIONAL ao teclado (`useKeyboardVisible` + `insets.bottom` só com teclado fechado — `edges={['bottom']}` reservaria a faixa sempre e o rodapé boia). Dívida: ProtocolFormScreen, DeleteAccountScreen, MedicineAnvisaSheet, MedicineSelectorSheet usam `undefined` (mesmo bug latente). 044 F4b, pego no smoke -> [`anti-patterns/mobile_and_platform/AP-288.md`](./anti-patterns/mobile_and_platform/AP-288.md)
- **[AP-289]** Suite de teste que **não CARREGA** é cobertura zero silenciosa — pior que um gate vermelho, porque o vermelho grita e este mente por omissão. O Jest mobile reportava "325 passando, 0 falhando" (verde) enquanto **8 suites** morriam no import (`Cannot find module './contracts.js'`) e não entravam na contagem: `TodayScreen`, `StockScreen`, `alarmService`, `buildConfirmLogs`, `buildBulkOutcome`, `useMedicineDatabase`, `supabase.smoke`. Causa: o `moduleNameMapper` não resolvia a extensão `.js` que o **R-282 EXIGE** nos imports relativos de `packages/` (Node ESM puro aponta pro `.ts` em compile; Vite/Metro resolvem, Jest não) — mesma classe do AP-263. Fix de 1 linha (`'^(\.{1,2}/.*)\.js$': '$1'` antes dos aliases): 325 → **384 testes**, zero vermelho novo. **Lição:** um gate de teste tem DUAS asserções — `failed == 0` (todo mundo checa) e **`suites_carregadas == suites_no_disco`** (ninguém checa). Ao tocar resolver (`moduleNameMapper`/`transformIgnorePatterns`/`paths`/`exports`) ou ao adotar convenção de import nova, comparar a contagem de suites antes/depois: delta negativo silencioso é o sintoma. 044 F5 (dívida herdada) -> [`anti-patterns/process_and_testing/AP-289.md`](./anti-patterns/process_and_testing/AP-289.md)
- **[AP-290]** O glob do gate não cobre a árvore que o gate promete proteger. `validate:agent` (gate obrigatório pré-commit) roda `vitest.critical.config.js`, cujos `include` cobriam `src/**` e `packages/core/**` — e **nenhum** cobria `server/`. Resultado: a suíte inteira da outbox (043) e o teste do filtro dose-only dos alertas (044/**PO-4 — evidência colada de um Proof Obligation**) existiam, passavam, eram citados como prova, e **nunca rodaram no gate**. Incluir `../../server/**/*.test.*`: **114 → 142 suites, 1609 → 1900 testes**, zero vermelho. Sintoma que denunciou: escrevi 10 testes novos e o total continuou EXATAMENTE 1609 — **um número que não se move quando deveria é sinal**. Gate seletivo (critical/smoke/fast) é uma LISTA DE PERMISSÃO, e listas de permissão apodrecem: árvore nova nasce fora e a ausência não gera erro. Regra: o commit que cria testes numa árvore nova atualiza o glob do gate; escreveu N testes → o total sobe N, senão o gate não os viu. Irmão do AP-289 (lá a suite não CARREGA; aqui o gate nem OLHA) — mesma mentira, verde que não executa; a defesa é conferir a CONTAGEM, não a cor. 043 T023b -> [`anti-patterns/process_and_testing/AP-290.md`](./anti-patterns/process_and_testing/AP-290.md)
- **[AP-291]** Inventário de export escrito à MÃO contra schema que anda sozinho — mente por OMISSÃO, e nada falha (tsc: era `any`; lint: nada; teste: só checava que não estourou; runtime: ler propriedade inexistente é `undefined`, não erro). O export LGPD web, em prod havia meses, tinha 3 buracos silenciosos: (1) `biomarkers_log` (glicemia/peso/PA — **dado sensível**, em prod desde o 012) NUNCA saiu no bundle → descumprimento invisível do art. 18; (2) seção perfil inteira ausente; (3) **colunas fantasma** — o inventário pedia `medicines.dosage_mg`/`pill_count`/`protocols.times`, que NÃO EXISTEM no schema: o CSV tinha a coluna "Dosagem (mg)" **vazia em todas as linhas**. Modo de falha assimétrico: omitir = descumprimento invisível · `select('*')` = vazamento (levaria `verification_token` pra dentro de um arquivo que o usuário manda por WhatsApp) · coluna fantasma = campo vazio que passa por "usuário não preencheu". Defesas: allowlist explícita no read-path (nunca `*`) · teste de CONTEÚDO (quais seções/colunas), não de execução · UMA implementação p/ web+mobile (duas listas "iguais" divergem — família R-290) · auditoria contra `information_schema` (foi ela que achou os 3). **Farol:** campo sempre vazio p/ TODOS os usuários — a hipótese preguiçosa é "ninguém preenche"; a certa é "esse campo não existe com esse nome". Spec 008 / PR #743. Ver [[R-291]] -> [`anti-patterns/data_and_schema/AP-291.md`](./anti-patterns/data_and_schema/AP-291.md)
- **[AP-294]** Preferência de UX usada como permissão de acesso a dado do titular (LGPD). O export (008) gateava a seção de estoque por `stock_tracking_enabled` (044) — mas essa flag governa o que o APP MOSTRA, não o que o titular tem DIREITO de levar (art. 18 II/V). Como a 044 CONGELA o estoque (não apaga), quem desligou o controle mantinha lotes/compras no banco, e o export os omitia em silêncio nos 3 níveis (checkbox some, fetch devolve null, formatter grava "não controlado"). Achado no SMOKE, não por teste — os testes codificavam o comportamento errado ("dose-only não exporta estoque"). Correção: decidir pela EXISTÊNCIA do dado, não pela preferência — existe → exporta sempre; não existe → seção honesta "sem dados", nunca a AUSÊNCIA da seção; a preferência vira metadado (`control_enabled`). Farol: `if (preferenciaDeUX)` no caminho de export/portabilidade; teste que AFIRMA que um dado do titular "não é exportado" petrifica o furo. Irmão de [[AP-291]] (lá omissão por inventário à mão; aqui omissão por flag de UX). Spec 046 Slice D. Ver [[R-291]] -> [`anti-patterns/data_and_schema/AP-294.md`](./anti-patterns/data_and_schema/AP-294.md)
- **[AP-295]** Superfície LOCAL de dado de saúde não gateada por consentimento. A supressão do 046 foi feita no PUSH do servidor (R-292), mas as superfícies que o APARELHO materializa sozinho — alarme Notifee, ongoing notification (contador de dose Android), Live Activity/Dynamic Island (iOS) — montam no `AppRoot` ACIMA do `ConsentGateProvider` e não tinham gate nenhum. Um titular que REVOGOU, ao dar reload, via o aparelho reprogramar lembretes COM nome do medicamento e dosagem (dado sensível, LGPD art. 11) sem base legal; a trava da UI escondia a tela mas NÃO desarmava o trigger nativo já agendado. PO cravou: não é smoke fix cosmético, é violação de lei — a LGPD não distingue servidor de device, distingue tratamento com/sem consentimento. Correção (contraparte local da R-292): hook `useConsentSuppressed` lê a trilha direto (bridges estão fora do Provider) + re-checa em mount/foreground/troca-de-usuário E num `consentSuppressionBus` que o gate dispara a cada refresh (grant/revoke in-app não passam por foreground). Revogado ⇒ alarme desarma via `isAlarmEnabled=false`→`cancelAll`; superfícies contínuas tratam revogado como "sem usuário", reusando o teardown testado do logout. Fail-open consciente: suprime só quando a leitura CONFIRMA `revoked`. Farol: superfície com nome/dose montada acima do provider de consentimento; achar que suprimir o push do servidor basta; teardown keyed só em logout/`!userId`. Spec 046 Slice B, smoke. Ver [[R-292]] -> [`anti-patterns/notifications/AP-295.md`](./anti-patterns/notifications/AP-295.md)
- **[AP-296]** Feature espelhada web↔mobile com trava presente só de um lado (defesa em profundidade assimétrica): no gate de consentimento 046, o guard `throw` no signUp e o `gate.refresh()` no revoke existiam no mobile e faltavam no web — ambos VERDES porque o gate de UI web mascarava a ausência da trava de serviço, e o teste do lado web codificava o comportamento incompleto como esperado. Só a re-revisão de branch inteira do Gemini (#745) pegou. Fix: paridade + teste que AFIRMA o gate.refresh. Regra: feature paralela web↔mobile exige diff superfície-a-superfície; trava de um lado = checklist do outro; testar a trava no nível dela, não pelo fluxo de UI que a contorna. Irmão de [[AP-291]]/[[AP-294]] -> [`anti-patterns/process_and_testing/AP-296.md`](./anti-patterns/process_and_testing/AP-296.md)
- **[AP-303]** Estado INDETERMINADO de gate tratado como BLOQUEIO → mata offline-first. O guard raiz mobile (`NavigationTree`) consumia `resolveConsentGate`, que classifica `indeterminate` como **`locked: false`** ("não conseguimos LER a trilha; não libera NEM bloqueia — é carregando/erro"), mas **ignorava** isso e fazia early-return de tela de erro full-screen quando `mode === 'indeterminate'`. Offline (4G/3G instável = norma no BR) → leitura falha → `state: null` → app INTEIRO travado, quebrando o offline-first prometido nas landings (ver doses/estoque, ações offline p/ sync). Passou no smoke porque smoke roda online. Juridicamente vazio: LGPD trava **tratamento** (coleta/transmissão), não o titular VER dado já no device (art. 18, direito); a proteção real (não ressuscitar consentimento revogado) mora na LEITURA (`state: null` ⇒ nada escrito), não em barrar renderização — renderizar offline não materializa nada. Confusão `revoked` (fato → trava correta) vs `indeterminate` (desconhecido → NÃO trava). Fix: obedecer o `locked: false`; sob indeterminação renderiza normal, e o `AppState 'active'`→`refresh()` reaplica a trava real quando a rede volta. **Farol:** guard de UI que contradiz a decisão pura que consome; estado de loading/erro tratado como negação num app offline-first ("não sei" ≠ "não pode"); gate barrando LEITURA de dado local. Espelho de [[AP-295]] (lá sub-bloqueou; aqui sobre-bloqueou). Família 046: [[AP-294]] [[AP-296]]. Spec 046 hotfix -> [`anti-patterns/react_and_ui/AP-303.md`](./anti-patterns/react_and_ui/AP-303.md)
- **[AP-306]** Unidade de medida concatenada em string no CALL SITE → N grafias divergentes da mesma unidade e JSDoc que vira ficção. `doseUnit.ts` produz **três** grafias de mililitro no MESMO arquivo (`0,5ml` sem espaço · `0,5 mL` · `2,5 ml`), e o JSDoc do `formatConcentration` promete `'2,5 mg / 0,5 mL'` que a implementação não entrega — a doc estava CERTA, a implementação divergiu em silêncio, invisível porque os testes fixavam o NÚMERO e não o rótulo. Causa raiz não é a caixa da letra: **44 call sites fora do core montam unidade à mão** (medido 2026-07-18, web+mobile+bot) — padronizar a grafia sem centralizar a emissão só reseta o contador. Rótulo de unidade é string de apresentação: invisível a tsc, lint e a assertion frouxa (mesma família do [[AP-305]] — o pipeline valida a FORMA e nunca pergunta como é LIDO). Regra: unidade sai SEMPRE de formatador do core, e o teste do formatador fixa a string COMPLETA; JSDoc divergindo da implementação = descobrir qual dos dois envelheceu antes de "corrigir" o teste. 🔴 Armadilha do sweep: `DOSAGE_UNIT_LABELS` mapeia **valor de enum do banco → rótulo**; a CHAVE (`'mg/ml'`) tem CHECK em `medicines.dosage_unit` e um `sed s/ml/mL/` pega as duas colunas → `23514` no cadastro de líquido ([[AP-299]]). Convenção correta: `mL` (ANVISA/bula; `L` maiúsculo evita confundir `l` com `1`). Débito: 44 call sites + 35 arquivos de teste + 9 arquivos do core; spec Tier 2 a abrir pós-merge do 029 F5. Achado no smoke do PO -> [`anti-patterns/react_and_ui/AP-306.md`](./anti-patterns/react_and_ui/AP-306.md)
- **[AP-307]** 🔴 **Flag esconde a SUPERFÍCIE mas o GATE continua bloqueando por ela → bloqueio invisível.** A 044 F3 tirou o card de estoque do sheet de exclusão (`{stockTrackingEnabled && stockUnits > 0 ? ... : null}`) mas deixou o pré-check de `useMedicineDelete` bloqueando por `stockUnits > 0` sem consultar a flag. Usuário dose-only com saldo **congelado** (a 044 congela, não apaga — [[AP-294]]) via botão `[Apagar]` desabilitado + *"exclua as **dependências abaixo**"* + **lista vazia**: acusação sem réu, beco sem saída (Constituição IX). Invisível a tudo — tsc/lint não relacionam condição de render com condição de gate; o hook (90 linhas, regra de negócio) **não tinha um teste sequer**; RC5/RC6 da F3 revisaram a F3, e a omissão mora no arquivo **não tocado**; a **web já estava certa** desde a F3 (`Medicines.tsx:39`), quebrou só a paridade mobile ([[AP-296]]). A hipótese natural do fix ("zerar o estoque antes de apagar") era **desnecessária**: `pg_constraint.confdeltype` mostra `stock_medicine_id_fkey = c` (CASCADE) — o banco já zera; o único restritivo é `titration_steps_medicine_id_fkey` (`a`), cujo bloqueio (029 F5) segue valendo. Consultar o `ON DELETE` antes evitou lógica duplicando o que o schema garante ([[R-295]] vale p/ FK também). Fix: flag entra no pré-check, default `true` (fail-safe [[AP-277]]); matrix [[R-236]] passa a invalidar `@dosiq/stock-snapshot` (o CASCADE apaga lotes do snapshot). **Farol:** toda condição `flag && X` num RENDER exige perguntar *"existe um GATE que decide por X?"* — são UM fato em dois arquivos; mensagem que aponta p/ uma lista renderizada condicionalmente exige o invariante `bloqueado ⇒ lista não-vazia`; hook com regra de negócio e zero testes é onde flags novas passam. Espelho de [[AP-305]] no lado do bloqueio (lá capacidade declarada sem consumidor; aqui gate sem consumir a flag). Smoke do PO / PR #759 -> [`anti-patterns/react_and_ui/AP-307.md`](./anti-patterns/react_and_ui/AP-307.md)
- **[AP-314]** 🔴 **DDL destrutiva validada contra o REPOSITÓRIO derruba os BINÁRIOS já instalados.** O DROP das 4 colunas N1 (029 F6, #765) passou por um gate rigoroso — `grep` zero em `apps/ packages/ server/ api/`, 7 selects EXECUTADOS contra o PostgREST ([[R-295]]), varredura de views/proc/constraints/policies no banco, 3 suítes de guarda, estado medido (68 linhas byte-idênticas). Tudo verdadeiro. 36h depois, aba Hoje e aba Tratamentos mortas em todo aparelho não atualizado: o mobile **v0.27.0** na Play Store nomeia `titration_status` no select de `dashboardService:31` e `treatmentsService:113` → **`42703` permanente**, que retry e wifi não curam. **O `grep` mede o código que VAI rodar; o que quebra é o código que JÁ ESTÁ rodando — e esse não está no repo, está compilado no bolso do usuário.** Modelo mental herdado da web (browser baixa bundle novo no reload) aplicado a um DROP que atinge todos os clientes: **mobile instalado é a única superfície onde código velho chama a API por tempo indeterminado**. Dois mascaramentos no diagnóstico: só 2 abas caíram (Estoque/Perfil não tocam `protocols` → parece "bug de tela", não outage) e a tela **mentiu a causa** — `useTodayData.ts` mostra *"Cache expirado (> 24h)"* porque `handleCacheFallback` engole o erro original quando existe cache, tornando erro de schema indistinguível de offline num app offline-first. Fix: recriar as colunas **inertes** com os DEFAULTs medidos em produção (`'estável'`/`'[]'`/`0`/`NULL`) — restauração fiel, não aproximação; salvou o resultado o fato de o `EvolutionBadge` da v0.27.0 já derivar de `titration_steps` (sorte de timing, não desenho). Regra: antes de `DROP`/`RENAME`/aperto de tipo ou CHECK, `git grep` **versão a versão em campo**, não só `HEAD`; leitor vivo ⇒ adiar, expand/contract ou forçar update — nunca "o usuário atualiza". **Farol:** justificativa de segurança que diz "zero leitores no código" sem dizer **em qual versão**; `DROP` em tabela que o mobile lê; "algumas abas não carregam, outras sim" pós-deploy de schema (o recorte mapeia as tabelas tocadas). Débito: sem política de versão mínima nem telemetria de distribuição, expand/contract é obrigatório por padrão. Irmão de [[AP-300]] (mesma tabela, mesmo 42703, causa oposta: lá coluna que nunca existiu). Outage 2026-07-22 -> [`anti-patterns/data_and_schema/AP-314.md`](./anti-patterns/data_and_schema/AP-314.md)
- **[AP-318]** 🔴 **Bundle JS novo contra binário native VELHO + init fail-silent = falha vira silêncio, parece config.** Smoke do PR 1.3b (Firebase→Sentry/PostHog): iOS mandava crash pro Sentry, Android não. Palpite natural ("limitação do SDK/emulador Android") errado — o `android/` no device era **prebuild pré-swap**: `app/build.gradle:184-185` ainda aplicava `com.google.gms.google-services`+`crashlytics` e o autolinking tinha **zero** sentry. O Metro serve bundle JS novo (chama `Sentry.captureException`) para um APK velho **sem o módulo native do Sentry**. Passou porque o `Sentry.init`/`captureException` é `try/catch` fail-silent DE PROPÓSITO (ADR-090: observabilidade nunca derruba boot) → módulo native ausente vira **silêncio**, indistinguível de "funcionou". iOS parecia OK só porque tinha `prebuild --clean -p ios`+`run:ios` no gate; Android nunca reconstruído nesta branch. Raiz: modelo mental da **web** (reload baixa JS novo) aplicado a mobile, onde JS e native são 2 artefatos — módulo native novo/removido só entra com **rebuild**, não com reload. Irmão de [[AP-316]] (gate não cobre xcodebuild) e [[AP-314]] (binário velho JÁ rodando quebra). **Farol:** trocou dep native (pod/gradle/config plugin)? smoke SÓ vale sobre rebuild `run:ios` E `run:android`, nunca reload de JS; "funciona num, no outro não" pós-swap native = binário desatualizado antes de SDK/emulador; init fail-silent esconde módulo ausente (confirmar no `build.gradle`+autolinking, ausência de erro ≠ presença do módulo); resíduo do provedor ANTIGO no projeto gerado = prebuild velho, reconstruir. Smoke do PO / PR 1.3b -> [`anti-patterns/mobile_and_platform/AP-318.md`](./anti-patterns/mobile_and_platform/AP-318.md)
- **[AP-329]** 🔴 **Script de regen com `>` trunca o arquivo VERSIONADO antes de o comando existir.** `"supabase:types": "npx supabase gen types ... > packages/shared-data/src/database.types.ts"` — o shell abre o redirecionamento no instante 0, então o arquivo de 2.129 linhas consumido por web+mobile+server+api foi **zerado antes** de o `npx` (não instalado, exige login) pendurar sem escrever nada. Ficou com **0 bytes** e `git status` mostrava só ` M`, indistinguível de regen bem-sucedido; só apareceu num `wc -c`. Dano assimétrico: build quebra em arquivos que ninguém tocou e a investigação vai pro lado errado; sessão interrompida ali comita tipo vazio. Vale p/ TODO `<gerador> > <arquivo versionado>` (gen types, openapi, codegen, dump de schema) — quanto mais o gerador depende de rede/login, maior a chance de falhar DEPOIS de truncar. **Antídoto:** escrever em temporário e `&& mv` (o `&&` é o gate); ou gerar via MCP e escrever o payload. **Farol:** `>` apontando p/ caminho dentro do repo em script de package.json; arquivo gerado com ` M` + erro de build em arquivo não tocado ⇒ conferir `wc -c` antes de investigar. Recuperação aqui foi `git checkout --` (estava limpo); se estivesse modificado de propósito, o conteúdo teria sumido. Origem: 067 A2, regen da coluna `early_window_minutes` (diff correto = 3 linhas, R-289). Ver [[R-289]] · [[AP-318]] -> [`anti-patterns/tooling_and_build/AP-329.md`](./anti-patterns/tooling_and_build/AP-329.md)
- **[AP-328]** 🔴 **Allowlist de visibilidade no core esconde um estado REAL e deixa o render das pontas virar código morto.** `timelineService.VISIBLE_INSTANCE_STATUSES = {taken,missed,pending}` filtrava `skipped_user` — status escrito em prod pelo "Pular" do alarme (`quickDoseRegistration.ts:111,118`) — então a dose pulada não existia em superfície nenhuma: nem web, nem mobile, nem como `missed`, nem como falha de aderência. Uma dose destruída por alarme adiantado sumia do registro sem rastro (incidente 2026-08-14). O mobile já tinha o render PRONTO (`DoseHistoryList.tsx:24`, `DoseActionSheet.tsx:31`) — morto há meses, porque o dado nunca chegava; a web não tinha, e o fallback `STATUS_META[status] || STATUS_META.pending` transformaria a pulada em **"Pendente"** no instante em que o dado passasse (mentira sobre ato clínico, PIOR que o invisível — achado da RC2). Nenhum gate pega: `Set` de 3 literais é válido p/ tsc+lint, e existia teste **VERDE** codificando a premissa errada (`'skipped_* são ocultos'`). **Farol:** toda allowlist de leitura (`new Set([...])`, `.in('status',[...])`) é gate invisível — perguntar *quais valores o banco produz que não estão aqui, e onde aparecem?*; agrupamento por PREFIXO (`skipped_*`) apaga a distinção que importa (`skipped_user`=ato da paciente vs `skipped_paused`=estado do tratamento); **neutro no CÁLCULO ≠ oculto na EXIBIÇÃO** (R-248 vale p/ aderência, não p/ histórico); estado novo no core exige auditar o FALLBACK de todo mapa de status das 2 plataformas + as camadas de cor (o dot do calendário precisou de CSS fora da lista de arquivos da spec). Família [[AP-305]] [[AP-307]] [[AP-296]] [[AP-301]]. Spec 067 A1 -> [`anti-patterns/data_and_schema/AP-328.md`](./anti-patterns/data_and_schema/AP-328.md)
- **[AP-332]** 🔴 **Claim com predicado único sobre estado que dois caminhos gravam diferente.** Ao trocar "carimba depois do envio" por claim antes do envio (`UPDATE ... WHERE <pred> RETURNING id`, [[AP-221]]/[[R-288]]), o predicado foi tirado da query do caminho feliz — mas o fetch tinha DUAS queries e o estado tinha DOIS produtores: Telegram (`doseActions.ts:419`) zera `notified_at` ao adiar, mobile (`setSnoozedUntil`, `createDoseInstanceRepository.ts:517`) NÃO zera. `.is('notified_at', null)` descartava em silêncio toda soneca do mobile (0 linhas reivindicadas = "outro worker levou"): lembrete perdido, sem erro nem teste vermelho. **Antídoto:** um predicado por população admitida pela leitura, cada um se auto-negando no mesmo UPDATE (`snoozed_until IS NOT NULL` para a adiada); release restaura TODOS os campos que o claim tocou (zerar a soneca e devolver só `notified_at` some com a dose); erro de banco ≠ corrida perdida (senão instabilidade vira não-entrega). **Farol:** antes de promover coluna a predicado de claim, `grep` todos os escritores dela + contar as queries da leitura. Ver [[AP-221]] · [[R-288]] · [[AP-276]] -> [`anti-patterns/notifications/AP-332.md`](./anti-patterns/notifications/AP-332.md)
- **[AP-331]** 🟠 **"App no topo" ≠ "app ativo": `top-sleeping` com a tela apagada não emite `AppState 'active'`.** O full-screen intent do alarme lança a Activity com o device ainda bloqueado — o processo vira `fg`/`oom cur=0` e PARECE foreground (`dumpsys activity processes` mostra o sufixo real: `(top-sleeping)`; `dumpsys window` mostra `mAwake=false`), mas nenhum listener de foreground dispara. Consequência: fila offline não drena, e evidência carimbada só na gravação passa a medir *quando a paciente olhou o celular* em vez de *quando o fato ocorreu* — medido: 961s de defasagem numa dose crítica. O caso feliz ESCONDE o defeito (destravar por qualquer motivo antes do alarme deixa o processo `active` e a drenagem sai em 2-3s — foi o que sustentou a hipótese errada de que só aconteceria com app morto; a condição real é a mais comum: silenciar e voltar a dormir). Correção: carimbar o instante na ORIGEM e separar do instante da gravação (067 C, `occurred_at`); validar com o device BLOQUEADO, sem destravar; ler a fila com `run-as ... cat databases/RKStorage` antes do flush para separar "não capturou" de "não drenou". Ver [[AP-257]] · [[AP-327]] · [[R-309]] -> [`anti-patterns/mobile_and_platform/AP-331.md`](./anti-patterns/mobile_and_platform/AP-331.md)
- **[AP-330]** 🔴 **Caminho fail-open transforma falha de persistência em smoke verde.** Guarda da 067 A2 barrou o disparo adiantado e exibiu o aviso na gaveta exatamente como especificado — e `SELECT ... WHERE event='alarm_out_of_window'` voltava **0 linhas**. O caminho tem dois efeitos (avisar/UI + registrar/trilha) e é fail-open de propósito (a trilha cair não pode impedir o alarme errado de ser silenciado), então o efeito OBSERVÁVEL sobrevive à morte do não-observável: sucesso visível idêntico com e sem persistência. Defeito real: gatilho de smoke com id sentinela `'dev-early-alarm'` numa coluna `uuid` → `22P02` silencioso; fixture de *apresentação* reusada em PO de *persistência*. Denúncia: PO-15 ✅ e PO-3 ❌ no MESMO disparo. **Antídoto:** PO cujo `ac` diz gravar/registrar/emitir fecha com SELECT, nunca screenshot ([[R-305]]); fixture que atravessa persistência usa entidade REAL vinda de SELECT (só assim predicados que consultam o banco chegam a rodar); efeito observável + auditável = dois POs separados. **Farol:** módulo documentado como "fail-open"/"best-effort"/"nunca lança" que também escreve em tabela ⇒ a escrita exige PO próprio; constante `DEV_*_ID = 'dev-algo'` em teste que grava. Ver [[R-295]] · [[AP-300]] -> [`anti-patterns/process_and_testing/AP-330.md`](./anti-patterns/process_and_testing/AP-330.md)
