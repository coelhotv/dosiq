# DEVFLOW Contracts Index

## 🚀 Infra & Deploy (`infra_and_deploy`)
- **[CON-022]** `getUserTime` + dateUtils tz-aware API (param tz opcional default SP; non-breaking; ADR-049) -> [`contracts/infra_and_deploy/CON-022.md`](./contracts/infra_and_deploy/CON-022.md)
- **[CON-001]** medicineService.getAll() -> [`contracts/infra_and_deploy/CON-001.md`](./contracts/infra_and_deploy/CON-001.md)
- **[CON-002]** medicineService.create(medicine) -> [`contracts/infra_and_deploy/CON-002.md`](./contracts/infra_and_deploy/CON-002.md)
- **[CON-003]** medicineService.update(id, updates) -> [`contracts/infra_and_deploy/CON-003.md`](./contracts/infra_and_deploy/CON-003.md)
- **[CON-004]** stockService.getStockForMedicine(medicineId) -> [`contracts/infra_and_deploy/CON-004.md`](./contracts/infra_and_deploy/CON-004.md)
- **[CON-005]** logService.createLog(data) -> [`contracts/infra_and_deploy/CON-005.md`](./contracts/infra_and_deploy/CON-005.md)
- **[CON-006]** useCachedQuery(key, fetcher, options) -> [`contracts/infra_and_deploy/CON-006.md`](./contracts/infra_and_deploy/CON-006.md)
- **[CON-007]** parseLocalDate(dateString) -> [`contracts/infra_and_deploy/CON-007.md`](./contracts/infra_and_deploy/CON-007.md)
- **[CON-008]** medicineSchema (Zod) -> [`contracts/infra_and_deploy/CON-008.md`](./contracts/infra_and_deploy/CON-008.md)
- **[CON-009]** useTheme() -> [`contracts/infra_and_deploy/CON-009.md`](./contracts/infra_and_deploy/CON-009.md)
- **[CON-010]** protocolService.getAll() -> [`contracts/infra_and_deploy/CON-010.md`](./contracts/infra_and_deploy/CON-010.md)
- **[CON-011]** adherenceService.getAdherence(days) -> [`contracts/infra_and_deploy/CON-011.md`](./contracts/infra_and_deploy/CON-011.md)
- **[CON-012]** cachedMedicineService (SWR wrapper) -> [`contracts/infra_and_deploy/CON-012.md`](./contracts/infra_and_deploy/CON-012.md)
- **[CON-013]** queryCache.delete(key) -> [`contracts/infra_and_deploy/CON-013.md`](./contracts/infra_and_deploy/CON-013.md)
- **[CON-014]** Button component props -> [`contracts/infra_and_deploy/CON-014.md`](./contracts/infra_and_deploy/CON-014.md)
- **[CON-015]** logSchema (Zod) -> [`contracts/infra_and_deploy/CON-015.md`](./contracts/infra_and_deploy/CON-015.md)
- **[CON-016]** useRedesign() -> [`contracts/infra_and_deploy/CON-016.md`](./contracts/infra_and_deploy/CON-016.md)
- **[CON-018]** ⚰️ RETIRADO (PR-F4.1/S4.2b) — calculateAdherenceStats(logs, protocols, days, offsetDays). Inferência ±2h sobre logs aposentada; fonte única `dose_instances` via `computeAdherenceFromInstances` (ADR-054). Mantido p/ rastreabilidade -> [`contracts/infra_and_deploy/CON-018.md`](./contracts/infra_and_deploy/CON-018.md)
- **[CON-023]** createTimelineService({client}).getTimeline({userId,fromTs,toTs,tz,order,protocolsById}) → TimelineEvent[] + doseInstancesToEvents(instances,logs,{protocolsById}) adapter puro (FP-3/ADR-050) -> [`contracts/data_and_schema/CON-023.md`](./contracts/data_and_schema/CON-023.md)
- **[CON-024]** doseZones core API: classifyDose(scheduledFor,now,...,toleranceMinutes) → zona temporal + buildDoseItemsFromInstances(instances,protocols,tz) → DoseItem[] + splitDayTimeline(instances,protocols,{now,tz}) → {carryOver,today,lookAhead} (janela deslizante cross-dia simétrica; puro, dose_instances, web↔mobile; F4.3a+F4.3e) -> [`contracts/data_and_schema/CON-024.md`](./contracts/data_and_schema/CON-024.md)
- **[CON-025]** Biomarcadores (012 Fase C, ADR-060): `biomarkers_log` (id,user_id,type,value,value_secondary,unit,measured_at,context,source,notes,created_at; RLS user_id=auth.uid()) + `biomarkerLogSchema` (enums PT; superRefine PA exige value_secondary; Base+partial p/ update — R-274) + `createBiomarkerRepository({client,getUserId})` (list/getLatest/create/update/remove) + adapter puro `biomarkersToEvents(rows)→TimelineEvent[] type='biomarker'` (R-252, sem tocar builder). type/source extensíveis sem migração; CHECK só em context. Sem meta (SaMD/ADR-062). Adapter consumido no web E no mobile (033: `historyTimelineService` mobile usa `biomarkersToEvents` + `measuresRepo.list` para merge na lista do dia) -> [`contracts/data_and_schema/CON-025.md`](./contracts/data_and_schema/CON-025.md)
- **[CON-026]** `createDoseLogService({client,getUserId})` (spec 035, ADR-071): factory de core unifica registrar/desfazer/atualizar/excluir dose + estoque FIFO delegando atomicidade a 3 RPCs transacionais (`register_dose_atomic`/`update_dose_log_atomic`/`delete_dose_log_atomic`). JS retém só Zod + snap (`findAnchorInstance`); ancoragem estrita (double-click aborta txn) vs best-effort; `update` usa flags de presença p/ limpar nullable→NULL; tolerância unificada `DEFAULT_TOLERANCE_MINUTES`=120. Adapters web (`logService`) e mobile (`doseService`) viram cascas finas. Mata classe AP-231 (log órfão/furo de estoque). **031:** RPCs `register`/`update` ganham param aditivo `p_injection_site` (+ `p_has_injection_site` no update) — não-breaking (DEFAULT NULL); `+ getLastInjectionSite()` (sítio global por `taken_at`) na factory -> [`contracts/data_and_schema/CON-026.md`](./contracts/data_and_schema/CON-026.md)
- **[CON-028]** *(onda 1a entregue — spec 015, ADR-074)* `fetchChatbotContextData({supabase,getUserId})` → `ChatbotContextData` + `buildPatientContext(data)` → string, em `@dosiq/core/chatbot` (+ seam Zod `chatbotContextDataSchema`/`validateChatbotContextData`). Web `chatbotService` + Telegram `chatbotServerService` adotaram; forks removidos. Grouping por plano = onda 1b; consumidor mobile = onda 2. Fetcher = selects únicos (protocols c/ join `treatment_plan(id,name,...)` + doseInstances + treatmentPlans); builder puro = derivação + grouping por plano (reusa splitDayTimeline/isProtocolActiveOnDate/formatDoseItem). Seam Zod valida saída do fetcher. Consumidores: web `chatbotService` + mobile (novo) + Telegram `chatbotServerService`. Não-breaking p/ `api/chatbot.js` (recebe string). Paridade: mesmo data → mesma string -> [`contracts/data_and_schema/CON-028.md`](./contracts/data_and_schema/CON-028.md)
- **[CON-027]** *(accepted — spec 037 Slice 1; mobile consumer no Slice 2)* `createAnvisaDatabase({baseUrl,fileKey,storageAdapter,ttlMs,timeoutMs})` + `StorageAdapter{read,write}` — núcleo on-demand ANVISA platform-agnostic em `@dosiq/core` (manifest+TTL+fetch timeout+normalize+search). `load()` nunca lança (offline→[]). Web injeta adapter Cache Storage; mobile AsyncStorage. Consumidores: web `medicine/laboratoryDatabaseService` (casca) + mobile `useMedicineDatabase`. API pública dos consumidores inalterada -> [`contracts/infra_and_deploy/CON-027.md`](./contracts/infra_and_deploy/CON-027.md)

## 📱 Mobile & Platform (`mobile_and_platform`)
- **[CON-017]** resolveChannelsForUser({ userId, repositories }) -> `string[]` canais App/Telegram/Web -> [`contracts/mobile_and_platform/CON-017.md`](./contracts/mobile_and_platform/CON-017.md)
- **[CON-019]** dispatchNotification({ userId, kind, payload, data, channels, context }) -> Unified delivery gate -> [`contracts/mobile_and_platform/CON-019.md`](./contracts/mobile_and_platform/CON-019.md)
- **[CON-021]** Notification Metadata Schema (`protocol_id` vs `protocolId` resilience) -> [`contracts/mobile_and_platform/CON-021.md`](./contracts/mobile_and_platform/CON-021.md)
- **[CON-030]** *(accepted — spec 039 F2+F3)* Dose Activity Surface (`apps/mobile/src/platform/doseActivity/`). **Android (F2):** `doseActivitySurfaceService` + driver TRIGGER-DRIVEN (`doseActivityScheduler`: Notifee TimestampTrigger+allowWhileIdle, encadeado via onBackgroundEvent) + `DoseActivityBridge`. Canal DEFAULT `dose-activity-v1` silencioso; **SEM toggle** (extensão do alarme crítico CL-4 — "opt-in OFF" REVOGADO); id próprio `${doseInstanceId}:surface` (coexiste com alarme no T0 — "id=doseInstanceId" REVOGADO); marca-passo `reconcileDoseActivityFromAlarm`. **iOS (F3):** Live Activity + Dynamic Island via `@bacons/apple-targets` (Widget Extension Swift, ADR-075) + `DoseLiveActivityBridge`/`liveActivityService` (start/update/end nativo). Timer vivo server-free; App Intents Registrar/Adiar (iOS 17+)→App Group→RN com sessão viva (PO-SEC-2). Ambos consomem CON-029, cancel-on-resolve (AP-235), Registrar abre modal bulk (CON-026), modo discreto (PO-SEC-1). **iOS push-to-start (spec 041, ADR-076):** `server/notifications/apns/` (cliente APNs raw JWT ES256+HTTP/2 + `buildLiveActivityStartPayload` CON-029-compat + `dispatchLiveActivityStarts` no loop `checkReminders`) inicia a LA com app fechado (T0−60min, start-only; transições seguem 039). Token: `Activity.pushToStartTokenUpdates` (Swift iOS 17.2+) → `getPushToStartToken`/`registerPushToStartToken` → `notification_devices` provider `apns_liveactivity` (RPC `upsert_notification_device` reuso). Idempotência `dose_instances.la_push_started_at`. Best-effort + fail-open (nunca suprime alarme); IDOR guard server-side; discreto server-side (nome fora do payload) -> [`contracts/mobile_and_platform/CON-030.md`](./contracts/mobile_and_platform/CON-030.md)
- **[CON-029]** *(accepted — spec 039 F1, PR #690)* `deriveDoseActivityState(item, now, opts)` + `selectActiveDoseActivity(items, now, opts)` + `DOSE_ACTIVITY_STATES` em `@dosiq/core` — máquina de estados de dose das superfícies contínuas (iOS LA / Android ongoing). Mapeia zona de classifyDose→estado (upcoming/now/late/done/missed); 2 nulls distintos (inválido→null vs pós-tolerância→missed); seletor retorna ≤1 superfície priorizada (atrasada>crítica>now>próxima) agrupada por treatment_plan (groupSize). Puro, server-free, aditivo a CON-024 -> [`contracts/mobile_and_platform/CON-029.md`](./contracts/mobile_and_platform/CON-029.md)
