diff --git a/supabase/migrations/0099_drop_medicine_alias.sql b/supabase/migrations/0099_drop_medicine_alias.sql
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/supabase/migrations/0099_drop_medicine_alias.sql
@@ -0,0 +1,3 @@
+ALTER TABLE medicines
+  DROP COLUMN alias;
+COMMENT ON TABLE medicines IS 'Catálogo de medicamentos e suplementos do usuário';
diff --git a/apps/web/src/schemas/medicineSchema.ts b/apps/web/src/schemas/medicineSchema.ts
index 2222222..3333333 100644
--- a/apps/web/src/schemas/medicineSchema.ts
+++ b/apps/web/src/schemas/medicineSchema.ts
@@ -10,7 +10,6 @@ export const medicineSchema = z.object({
   id: z.string().uuid(),
   name: z.string().min(1),
-  alias: z.string().nullable().optional(),
   dosage_unit: z.enum(DOSAGE_UNITS),
 });
