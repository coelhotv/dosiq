# Expo generated native directories (managed workflow)
# Regenerated via: npx expo prebuild
ios/
android/

# Dependencies
node_modules/

# Expo
.expo/
dist/

# Local env
.env
.env.local

# Firebase credentials (nunca commitar — contêm chaves privadas)
google-services*.json
GoogleService-Info*.plist
